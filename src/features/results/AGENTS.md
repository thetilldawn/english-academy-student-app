# 시험 결과 기능 안내

- 먼저 `npm run map:feature -- results`로 결과 page, 조회 서비스, 학생 시험 흐름과의 연결을 확인한다.
- `domain`은 결과 해석 규칙, `api`는 응답 변환, `ui`는 표시만 맡는다.
- 응시 완료·오답·포인트 기록 규칙은 학생 시험 흐름의 DB 계약에 있으며 결과 UI에서 다시 계산하지 않는다.
