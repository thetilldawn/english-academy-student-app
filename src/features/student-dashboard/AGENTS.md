# 학생 메인 기능 안내

- 먼저 `npm run map:feature -- student-dashboard`로 학생 첫 화면과 관련 조회 경로를 확인한다.
- `domain`은 목록 분류, `api`는 응답 변환, `ui`는 현재 시험·완료 내역 표시를 맡는다.
- 학생별 배정·점수·오답은 공용 캐시에 넣지 않고 서버에서 권한을 확인한 뒤 최소 자료만 전달한다.
