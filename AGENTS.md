# 학생 앱 작업 지침

## 경계

- 이 폴더는 학생용 웹앱의 코드·스키마·테스트만 둔다.
- 교재·시험지·단어 Obsidian 원천과 실제 학생 개인정보는 이 폴더에 복사하지 않는다.
- 앱에서 필요한 어휘는 `영어/`의 검수된 내보내기를 입력으로 받는다.
- 실제 학생정보·시험결과는 Git이 아닌 승인된 Supabase 프로젝트에 저장한다.

## Git과 비밀정보

- 앱 구현을 시작할 때 이 폴더 안에만 로컬 Git 저장소를 만든다.
- `.env*`, API 키, 서비스 역할 키, 실제 학생 데이터는 커밋하지 않는다.
- 예제·테스트 데이터는 가짜 학생 또는 익명 식별자만 사용한다.
- 루트와 `영어/`의 Git 상태를 변경하지 않는다.

## 구현 원칙

- Next.js App Router, Vercel, 별도 Supabase 프로젝트를 기준으로 한다.
- 공개 회원가입을 만들지 않는다.
- 학생 코드는 평문 저장하지 않고 해시와 상태·만료·회전정보를 관리한다.
- 학생 세션 쿠키는 DB 만료시각과 맞춘 60일 만료·Secure·HttpOnly·SameSite 속성을 사용한다.
- 관리자의 코드 차단·회전은 해당 학생의 기존 세션을 모두 무효화해야 한다.
- 데이터 규칙과 핵심 로직을 UI에서 분리해 화면을 바꿔도 재사용할 수 있게 한다.

## 코드 길찾기와 기능 소유권

- 코드 변경 전에 [코드 길찾기와 변경 영향 확인](docs/코드_길찾기_및_변경영향.md)을 먼저 읽는다.
- `architecture/기능_소유권.json`에서 작업할 기능 ID, 현재 진입점, UI·상태·계약·server·DB·검사
  경로를 확인한다. 검색으로 우연히 찾은 한 파일만 고치지 않는다.
- 기능은 `npm run map:feature -- <기능 ID>`, 공용 기반은 `npm run map:owner -- <소유 범주 ID>`,
  실제 사용자 행동은 `npm run map:flow -- <실행 흐름 ID>`로 먼저 출력한다.
- 수정할 파일 아래에 더 가까운 `AGENTS.md`가 있으면 그 지침도 함께 적용한다.
- 기능 작업마다 경로, UI, Client 상태, 순수 계산, 사용 사례, 요청 계약, server, DB, 캐시·Streaming,
  검사의 변경 여부를 기록한다. 바꾸지 않는 면도 `확인만` 또는 `해당 없음`과 이유를 남긴다.
- 새 기능 폴더·Route Handler·server service·migration을 추가하거나 파일을 옮기면 같은 변경에서
  `architecture/기능_소유권.json`과 가까운 `AGENTS.md`를 갱신한다.
- `src/design-system` 공용 UI와 `src/lib/quiz` 공유 시험 계산도 변경 영향 지도의 검사 대상이다.
- `npm run verify:architecture`는 소유권 지도 경로와 계층 경계를 함께 확인해야 한다. 지도에 없는 새
  feature나 존재하지 않는 경로를 남긴 상태로 완료 처리하지 않는다. 이 명령은 작업 트리와 기준
  커밋 이후의 변경 파일도 함께 대조한다.
- 구조 검사기 자체는 `scripts/feature-map`에서 경로·import·실행 흐름·흐름 검증·변경 영향·
  공통 소유 목록·출력을 책임별로 나눈다. 새 검사 모듈도 소유권 지도와 가까운 안내서에 등록한다.
