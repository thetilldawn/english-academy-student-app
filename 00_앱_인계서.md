# 학생 앱 인계서

- 갱신일: 2026-08-09
- 현재 단계: `redesign` Preview 기능 보완
- 앱 Git 루트: 이 `학생 앱` 폴더
- 운영 주소: https://english-academy-student-app.vercel.app

> [!important] 현재 상태와 우선순위
> 미완료 상태와 실행 순서는 루트
> [작업지시 대장](../../00_작업지시/00_작업지시_대장.md)만 기준으로 한다.
> 이 문서 아래의 과거 `현재 작업`·`다음 작업` 문구는 당시 체크포인트 기록이며
> 우선순위가 아니다. 활성 작업은 이 인계서에 중복 선언하지 않는다.

## 경계

- 앱에는 코드, DB 스키마, 가짜·익명 시험 데이터만 둔다.
- 실제 학생 개인정보, 교재 원본, Obsidian 원본 단어 데이터는 Git에 넣지 않는다.
- 원본 자료와 단어원장은 상위 `영어/`가 기준이며, 앱은 검증된 연결 패키지만 가져온다.
- 현재 staging의 2025 단어 후보·exam-use projection은 앱 E2E fixture다. 공통 사전
  gold set, 승인 release 또는 Production 단어 import 근거로 승격하지 않는다.
- 앱 안정화 배포에는 Preview에서 승인된 코드와 필요한 migration만 포함하며 후보
  단어 데이터의 상태 변경·release 활성화·Production 적재는 제외한다.
- Google Drive 동기화와 원본 자료 정리는 사용자가 수행한다.

## 2026-08-09 정답 피드백·진행 시험 정렬 수정

- 정답 선택 뒤 화면 아래에 추가되던 `정답입니다.` 문구를 시각 화면에서 제거했다.
  같은 내용은 레이아웃을 차지하지 않는 스크린리더 알림으로 유지해 정답 선택 전후
  시험 카드 높이가 바뀌지 않는다. 기존 500ms 답 확인 시간은 유지한다.
- `이어 풀기` 상태의 첫 시험과 결과 확인 뒤 재시험 대기 상태는 각 구역의 다른
  항목보다 먼저 표시하고, 진행 마감이 가까운 순서로 정렬한다. 첫 시험은 실제 시험
  제한시간 마감, 재시험 대기는 배정 마감을 사용한다.
- DB·migration·학생 데이터는 변경하지 않았다. 관련 3파일 19테스트, TypeScript,
  변경 파일 lint를 통과했다.
- 기능 커밋 `2ed7aaaf875e635c7ef9c667874270070ac65ee6`을 `main`에 먼저 반영했다.
  Production Vercel check와 이후 `redesign` Preview check가 모두 `success`이며 운영
  주소는 HTTP 200이다. 두 원격 브랜치는 같은 기능 커밋을 가리킨다.

### 2026-08-10 오답 피드백 누락 보완

- 정답 문구만 화면에서 제거되고 오답·시간 종료 문구가 남아 시험 카드 높이를
  바꾸던 누락을 보완했다.
- 정답·오답·시간 종료 안내는 모두 레이아웃을 차지하지 않는 스크린리더 상태 알림
  하나로 통합했다. 선택지의 정답·오답 색상과 기존 500ms 확인 시간은 유지한다.
- 관련 2개 계약 파일 11테스트, TypeScript, 변경 파일 lint를 통과했다.
- 이번 보완은 `redesign` Preview에서 먼저 확인하며 사용자 승인 전에는 `main`과
  Production을 변경하지 않는다.
- 기능 커밋 `a3ffac2`를 `redesign`에 푸시했고 GitHub의 Vercel deployment check가
  `success`다. `main`과 Production 변경은 0건이다.

## 2026-08-09 배정 수량·역순 회귀 수정

- 운영 `DAY 57~60` 사례를 익명 집계한 결과 원본 175행 중 출제 검토를
  통과한 고유 항목은 161개였고, 다른 미응시 시험에 이미 포함된 77개를
  제외한 실제 최대는 84문항이었다. 데이터 유실은 없었다.
- 동일 단어의 활성 시험 중복 배정 차단은 유지한다. 배정 모달은 이제
  `원본 → 출제 검토 통과 → 다른 시험 중복 제외 → 출제 방식 제외 → 현재 최대`를
  같은 위치에서 보여준다.
- `DAY 60→54`처럼 역순 범위를 고르면 단원 연결뿐 아니라 일반 시험의 기본
  문제은행도 선택한 단원 방향을 따른다. 단원 안에서는 원본 순서를 유지한다.
- 미응시 배정 범위를 수정할 때 사용자가 제목을 직접 바꾸지 않았다면 자동
  시험 제목도 새 범위로 갱신한다.
- DB 스키마와 운영 데이터는 변경하지 않았다.
- 검증: lint, TypeScript, 101개 테스트 파일·577개 테스트, Next.js Production
  빌드 통과.
- 기능 커밋 `37648d34f43e76a49476f662da399264cf0728bf`을 `redesign`과
  `main`에 반영했다. Vercel Production deployment
  `6dVe2FKJjy9UJJDSUAsu9FL4C27M`은 `success`, 운영 URL은 HTTP 200이다.
- 운영 개인정보나 가짜 시험을 추가하지 않았으므로 실제 계정의 `DAY 60→54`
  배정 확인은 현장 검증으로 남긴다.

## 현재 기능

- 관리자 계정 로그인
- 학생 등록, 12자리 접속 코드 발급·보기·교체·차단
- 학생 코드를 인증한 180일 HttpOnly 세션
- 학생별 현재 단어장 선택과 다음 DAY 추천
- DAY 단위 단어 시험 배정
- 영어→한글, 한글→영어, 혼합 4지선다
- 전체 제한시간, 통과점수, 고정·무작위 문항 순서
- 배정 시 한 번 생성한 문제은행을 학생 응시 때 그대로 사용
- 첫 시험 뒤 틀린 문항만 한 번 재시험
- 시험 결과를 먼저 확인한 뒤 명시적인 재시험 버튼으로 진행
- 이전 오답 횟수를 시험 문항에 1회·2회 표식으로 표시
- 학생별 1회 오답·반복 오답을 하나의 목록에서 검색·필터·선택
- 시험 관리에서 현재 단어장의 실제 오답·2회 이상 오답 학생을 필터
- DAY 진도, 오답 복습, DAY+복습 혼합 시험을 각각 배정
- 만료시각까지 남은 시간을 학생·관리자 화면에 표시
- 기한이 지난 미응시 배정을 관리자 이력에 보존하고 늦은 응시를 DB에서 차단
- 관리자 Overview, 학생 관리, 시험 관리, 내역
- 미응시 배정과 실제 응시를 합친 관리자 공통 이력
- Overview의 첫 시험·최종 점수와 주문형 상세 모달
- 학생 내역·관리 탭 모달, 학생 검색·학교·학년 필터 배정 모달
- 관리자 동적 경로 로딩 경계와 메뉴 클릭 진행 표시
- 서버 오류의 작업명·오류코드·경로·Next 오류번호를 개인정보 제거 후
  Vercel 로그에 한 줄 JSON으로 기록
- 오류 화면에 DB 원문 대신 `next_<digest>` 오류번호를 표시
- PC·태블릿·모바일 반응형 화면

## 현행 단어 데이터

- 교재: 능률 VOCA 어원편 고등 2025개정
- 교재 원본 SHA-256: `9FB5B8307C5E695853E2E0E49DE07DD9CD20D29BC59C749DED4D2D07B4C92133`
- 교재 행: 3,001개, 고유 표제어 2,820개
- 단어원장 빌드 ID: `d5e4f8ea-ba44-5a1b-98bf-896694b225a9`
- 단어원장 lexeme: 15,610개
- 연결 패키지 SHA-256: `049C79295BC8F4FD8D41C3DECF6D6C7F73A3842C20CA18AF4C03490E204A8345`
- 정확 연결: 2,113행·1,977표제어
- 미연결: 888행·843표제어
- 다중후보 자동확정: 0건

## 문제은행 v2 규칙

- 브라우저는 표제어·뜻·정답 번호를 DB에 저장하라고 보내지 않는다.
- 앱은 목표 단어 ID, 네 선택지 ID, 방향, 순서만 전달한다.
- DB가 현재 단어장·DAY·검수 상태를 다시 확인하고 문구와 정답을 생성한다.
- 문항마다 원본, 단어원장, 연결상태, 사용가능 규칙과 해시를 스냅샷으로 보존한다.
- 선택지 표시값은 NFKC 정규화 뒤 서로 달라야 한다.
- 영어→한글 사용가능 2,935행, 검토 66행
- 한글→영어·혼합 사용가능 2,899행, 검토 102행
- 기존 학생 1명·배정 1건·완료 시도 1건·문항 10개는 `legacy_backfill`로 보존한다.
- 기존 문항에는 현재 원장 출처를 소급해 붙이지 않는다.

## DB와 배포

- Supabase 리전: 서울 `ap-northeast-2`
- Vercel 함수 리전: 서울 `icn1`
- GitHub `thetilldawn/english-academy-student-app`의 `main`과 Vercel 운영 배포가 연결되어 있다.
- 자동 배포가 차단되지 않도록 이 저장소의 커밋 작성자는 GitHub 소유 계정 `thetilldawn`을 사용한다.
- 단어원장·문제은행 v2 코드 기준 커밋: `331d53b`
- 오답 복습·미응시 영속화 코드 기준 커밋: `81f01ff`
- 운영 스키마 복구·오류 추적 코드: 이 인계서가 포함된 현재 `HEAD`
- 로컬 마이그레이션: 43개
- 현행 문제은행 마이그레이션:
  - [문제은행 v2](supabase/migrations/20260729115000_add_eligible_question_bank_v2.sql)
  - [외래키 인덱스](supabase/migrations/20260729121500_index_eligible_question_bank_foreign_keys.sql)
  - [구버전 호출 차단](supabase/migrations/20260729122500_deprecate_legacy_question_bank_rpcs.sql)
- 현행 오답·미응시 마이그레이션:
  - [오답 이력](supabase/migrations/20260730130000_add_vocab_wrong_event_history.sql)
  - [복습 배정](supabase/migrations/20260730170000_add_review_assignment_foundation.sql)
  - [혼합 배정](supabase/migrations/20260730203000_create_mixed_review_assignments.sql)
  - [미응시 영속화와 늦은 응시 차단](supabase/migrations/20260730223000_persist_missed_assignments.sql)
  - [미응시 확정 예약 작업](supabase/migrations/20260730224500_schedule_missed_assignment_finalization.sql)
  - [현재 단어장 실제 오답 요약](supabase/migrations/20260730233000_list_current_vocab_wrong_summaries.sql)
  - [첫 시험 오답 즉시 기록·재시험 초안 취소](supabase/migrations/20260730234500_record_initial_wrongs_and_cancel_review_drafts.sql)
  - [문제 순서 확장](supabase/migrations/20260730235000_expand_question_order_modes.sql)
  - [전체·문제당 시간 분리](supabase/migrations/20260730235100_add_assignment_timing_and_order_behavior.sql)
  - [시간설정 RPC 보안 분리](supabase/migrations/20260730235200_harden_assignment_delivery_rpc.sql)
- 2026-07-30 운영 장애 원인은 `20260729124734` 다음 12개 마이그레이션을
  건너뛴 채 마지막 2개만 적용되어 앱 코드와 DB 스키마가 어긋난 것이었다.
- 누락 12개는 의존 순서대로 각각 롤백 모의검증 후 적용했고,
  `20260729153508`부터 `20260730224500`까지 대상 이력 14개가 모두 연속이다.
- 필수 RPC 12개·필수 열 7개 누락 0, 기존 응시 2건·문항 30건·
  단어상태 29건 유지, 고아 배정·학생 연결 0건을 확인했다.
- 적용 순서는 `8C-1 DB → 8C-2 DB → 앱 배포`이며, Vercel Cron은 사용하지 않는다.
- 예약 작업 `english-academy-finalize-missed-assignments`는 매분 최대 250건을 `postgres`로 확정한다.
- 학생 화면 진입 때는 같은 함수를 최대 25건만 호출해 짧은 지연도 보정한다.
- 2026-07-30 08:42 KST 첫 예약 실행은 4ms에 성공했고, 독립 검산 시점까지 6회 성공·실패 0건이었다.
- 적용 당시와 독립 검산 당시 기한초과·미응시·미확정 처리 대기 건수는 0건이었다.
- 예약 작업이 늦어져도 화면은 마감시각으로 미응시를 즉시 표시하며, DB 트리거가 늦은 응시 생성을 차단한다.
- 예약 작업 중단은 운영 판단 뒤 `cron.unschedule('english-academy-finalize-missed-assignments')`로만 수행한다.
- 실행 이력은 `cron.job_run_details`에서 확인하며 자동 삭제하지 않는다.
- 2026-07-30 복구 후 예약 작업은 1개만 활성 상태이며 최신 실행은
  `succeeded`, 반환값은 `1 row`였다.
- 실제 오답 요약 RPC는 롤백 모의검증 뒤 운영 DB에 적용했다.
  기존 오답 이벤트 25회는 현재 단어장 기준 17단어·2회 이상 8단어로
  집계됐고, 수동 복습 대기열 0건과 분리됨을 확인했다.
- RPC는 활성 학생을 UUID 커서로 최대 500명씩 읽고, 기존 오답 이벤트
  인덱스를 사용한다. 운영 `EXPLAIN`은 실행 0.553ms·임시/디스크 I/O 0이었다.
- 환경변수와 Secret 값은 Git과 문서에 기록하지 않는다.
- 구조화 로그에는 이름·이메일·학생코드·UUID·답안·쿠키·헤더·Secret을
  기록하지 않는다. 화면 오류번호로 Vercel 로그를 검색한다.

## 검증 기준

- `npm run verify`로 lint, TypeScript, 전체 테스트, 운영 빌드를 한 번에 검사한다.
- 2026-07-30 기준 55개 테스트 파일·314개 테스트와 Next.js 운영 빌드 통과
- 관리자 목록은 상세 문항을 미리 읽지 않고 모달을 열 때만 `/api/admin/attempts/[id]`로 조회한다.
- Overview는 전체 학생·시험 목록을 중복 조회하지 않고 배정·응시 공통 이력 두 조회만 사용한다.
- DB 변경은 트랜잭션 롤백 시험 뒤 적용한다.
- 문제은행 v2 공개 RPC 생성·문항 해시·선택지 ID 정렬을 확인한다.
- 기존 학생·배정·응시·문항 수가 변하지 않는지 확인한다.
- 미응시 확정 함수는 `service_role`만 호출할 수 있고 한 번에 1~1,000건으로 제한한다.
- 미응시 확정은 `missed_at`과 `assignment.missed` 감사 이벤트를 함께 기록한다.
- 마감 뒤 미응시 배정은 Cron 실행 전에도 새 응시를 만들 수 없어야 한다.
- 외래키 누락 인덱스와 성능 경고가 0인지 확인한다.
- 원본·Obsidian·Drive 변경은 0을 유지한다.

## 현재 진입점

1. [앱 README](README.md)
2. [문제은행 v2 마이그레이션](supabase/migrations/20260729115000_add_eligible_question_bank_v2.sql)
3. [관리자 배정 서비스](src/lib/services/admin-service.ts)
4. [시험 엔진](src/lib/quiz/engine.ts)
5. [응시·결과 서비스](src/lib/services/quiz-service.ts)
6. [단어원장 연결 코드](src/lib/vocab/canonical-linkage.ts)
7. [상위 단어원장 연결검증 보고서](../../영어/90_이관%20기록/단어시스템/44_앱단어원장_연결검증_v1.md)
8. [미응시 영속화](supabase/migrations/20260730223000_persist_missed_assignments.sql)
9. [미응시 예약 작업](supabase/migrations/20260730224500_schedule_missed_assignment_finalization.sql)
10. [서버 오류 구조화 로그](src/lib/observability/server-log.ts)
11. [Next 요청 오류 수집](src/instrumentation.ts)
12. [현재 단어장 실제 오답 요약](supabase/migrations/20260730233000_list_current_vocab_wrong_summaries.sql)

## 다음 작업

1. Kakao 앱에 JavaScript SDK 도메인과 제품 링크 Web 도메인을
   등록한 뒤 실제 카카오톡 공유 확인
2. 운영에서 마지막 오답 행 선택, 첫 시험 직후 오답 표시, 새로고침,
   재시험 초안 취소와 일반 시험 대기 유지 실사용 확인
3. 전체 시험 시간과 문제당 시간의 새로고침 복구·미응답 오답 처리를
   운영에서 각각 확인
4. 발음 표본 사용자 승인 전까지 앱 발음 기능과 DAY 01 import를 차단
5. 예약 작업 실패 여부와 250건 초과 backlog를 운영 점검 항목으로 유지

## 2026-07-30 앱 개선 배치

- 앱 개선 구현 커밋: `eea13e6ec19f45986832ef4d18502ba08ca2bac0`
- 최초 운영 배포: `dpl_3RYKMxHGnNg2xDrV62iZw5KtgAFH` — `READY`
- 오답 목록의 하단 고정 작업영역에 가려지던 마지막 행을 보정했다.
- 첫 시험이 복습 단계로 넘어가는 즉시 1차 오답 이벤트를 기록한다.
- 재시험 초안을 취소하면 예약만 해제하고 오답은 일반 시험 대기열에
  `pending`으로 남긴다.
- 오답 단어 수와 누적 오답 횟수, 재시험 예약과 일반 시험 대기를
  다른 문구로 표시한다.
- 문제 순서는 오름차순·내림차순·무작위로 통일하고 기존 `fixed` 배정은
  오름차순 호환으로 유지한다.
- DAY 교재와 비-DAY 단원을 구분하고 최근 배정·최근 완료·다음 추천과
  추천 이유를 표시한다.
- 전체 시험 시간과 문제당 시간을 분리했다. 문제당 시간은 서버 시계로
  복구하며 초과 시 미응답 오답으로 기록한다.
- 긴 정적 설명은 키보드 초점과 호버를 지원하는 `?` 도움말로 옮겼다.
- 코드 공유는 브라우저 일반 공유를 제거하고 Kakao JS SDK 호출과
  메시지 복사 대체 기능으로 바꿨다.
- 운영 DB에는 위 네 마이그레이션을 적용했고 원격 이력 번호를 로컬
  파일명과 일치시켰다.
- 원격 보안 검사에서 새 공개 `SECURITY DEFINER` 경고 0건을 확인했다.
- 마이그레이션 SHA-256:
  - `20260730234500`: `66B3CA4C90709EF0B926979DEB3BEE3004A519C7E2BF6AE3A6BBEC4DB96ACF9C`
  - `20260730235000`: `0BF0CF0DCC35F0084F5A9503C8EB3F1F73ECBC147847A907E6110472DBA3BF07`
  - `20260730235100`: `8B7CF7C02D43003D29E6869C97F658B0F9C315A1E69C467467F1910F27C531D5`
  - `20260730235200`: `5483FB2E4D57CE923BCBEF746454E2013FF5C57C4E02A275E70CA13BFBF8443F`
- `npm run verify` 전체 통과: lint, TypeScript, 55파일·314테스트,
  Next.js 16.2.12 운영 빌드.
- GitHub CLI는 설치되어 있지 않지만 이 저장소의 기존 배포는
  Windows Git Credential Manager를 사용하는 `git push main`과
  Vercel Git 자동 배포 방식이므로 GitHub CLI가 필요하지 않다.
- 프로젝트 전용 Kakao JavaScript 키는 Git 제외 `.env.local`에
  `NEXT_PUBLIC_KAKAO_JAVASCRIPT_KEY`로 설정했다.
- 같은 키를 Vercel Production 환경변수에도 설정했다. Kakao JavaScript
  SDK 도메인·제품 링크 도메인이 모두 설정되기 전에는 메시지 복사로
  안전하게 대체한다.

## 2026-07-31 관리자 삭제·배정 정상화 배치

- 학생은 soft delete하며 코드와 세션만 폐기하고 시험·성적·오답 이력은
  보존한다. 과거 화면의 학생명은 `삭제됨`으로 표시한다.
- 미응시 배정 취소, 시험 전체 삭제, Overview·학생·시험 관리의 개별
  내역 숨김을 제공한다. 응시가 시작된 배정은 취소하지 않는다.
- 학생 태블릿·PC 상태 문구와 시험 관리 배정 버튼 색상을 보정했다.
- 정답 선택 뒤 피드백을 0.5초 표시하고, 문제별 제한시간도 다음 문제가
  표시된 0.5초 뒤 시작한다.
- 일반 시험은 정답과 네 선택지를 모두 선택한 DAY 범위에서 생성한다.
  DAY 09의 실제 33개 범위는 4~33문항 배정이 가능하다.
- 시험 관리에는 `현재 미해결 / 다음 시험 대기 / 추가 가능`을 분리해
  표시한다. 과거 교사가 선택한 현재 미해결 7개를 대기열로 복원했다.
- 운영 마이그레이션:
  - `20260731044902_admin_deletion_controls`
  - `20260731044917_delay_per_question_timer_after_feedback`
  - `20260731044930_reopen_selected_unresolved_review_queue`
- 적용 전후 학생 1·시험 6·응시 5·문항 122·오답 이벤트 69·현재 오답
  상태 70·활성 세션 3·진행 중 응시 1을 그대로 보존했다.
- 사용자 요청에 따라 이 배치는 전체 회귀검사와 운영 가짜 데이터 생성을
  생략하고, DB 구조·핵심 수치와 운영 배포 상태만 확인한다.

## 2026-08-06 리디자인 Preview 격리 배치

- 기준 브랜치: 최신 `origin/main`의 `redesign`; 기존 로컬 `main`과 그
  미커밋 단어 사전 작업은 변경하지 않는다.
- staging Supabase: `english-academy-student-app-staging`, 서울 리전,
  project ref `wojxpruvbjzbhrpmsbuy`. 운영 ref는
  `xdxhswjgksukjmpbzqgz`이며 서로 데이터를 공유하지 않는다.
- 운영 기준 마이그레이션 43개를 staging에 순서대로 적용했다. public
  테이블의 RLS 누락은 0개다.
- staging 가짜 자료: 관리자 1명, 학생 3명, DAY 01 12단어·양방향
  eligibility 24행, 새 시험·진행 중·완료 통과·완료 미달·미응시·취소·
  삭제 시험, 오답 대기 1회·2회 이상·배정 중·해결 이력을 구성한다.
- 모든 Vercel Preview는 staging URL·publishable key·server secret·학생
  코드 보안키를 사용한다. `PREVIEW_EXPECTED_SUPABASE_PROJECT_REF`가
  실제 URL ref와 다르면 `prebuild`가 실패한다. Production에는 이 검사를
  적용하지 않는다.
- Preview 카카오 키는 설정하지 않으며 메시지 복사 대체만 확인한다.
  Vercel Authentication의 Require Log In과 Standard Protection은 켜져
  있다.
- `redesign` 고정 브랜치 주소는
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`이다.
  같은 주소를 Preview의 `redesign` 브랜치 전용 `APP_ORIGIN`으로 추가했다.
- 최초 격리 Preview 배포 `dpl_6y8qcsJyXEBYuV65cUM45vEgwtut`는 `READY`이며,
  커밋 `30a17b1cd2fff8fa1298232b95ad234b493e2d2b`을 빌드했다.
- 디자인 기준은 `DESIGN_1.md`와 `final-mockup.html`이다. 색·간격·서체·
  상태 토큰, 세로 1열 선택지, 자동 다크 모드, 학생 900px·시험 620px
  최대 너비를 적용한다. 시험·오답·학생 API와 운영 DB 스키마는 바꾸지
  않는다.
- Preview 화면·기능 검증과 사용자 승인이 끝나기 전에는 `redesign`을
  `main`에 병합하거나 Production으로 배포하지 않는다.

### Preview 실사용 검증

- `dpl_57RYN3rUupy1RUfVrph6jbUFwNmc`가 `READY`이며 `redesign` 전용
  `APP_ORIGIN`과 staging 환경변수가 반영됐다.
- 최초 가짜 자료에는 출제 eligibility 24행만 있고 dataset capability와
  `word_index` 승인 연결이 없어 실제 생성이
  `english_to_korean_capability_unavailable`로 차단됐다. staging에만
  가짜 index build·source·lexeme·occurrence·entry link·양방향 capability를
  추가한 뒤 12문항 혼합 배정이 성공했다.
- staging UI에서 12문항 새 배정 생성, 미응시 배정 취소, 시험 전체 삭제를
  순서대로 실행했고 삭제 후 해당 시험과 내역이 목록에서 사라짐을 확인했다.
- 가짜 학생으로 첫 시험 4문항, 오답 피드백, 재시험, 최종 결과까지 실행했다.
  관리자와 학생 세션은 같은 브라우저에서 동시에 유지됐다.
- 오답 가짜 자료는 현재 계약에 맞춰 initial 오답 이벤트 4건, 현재 미해결
  단어 3개, 누적 1회 2개, 누적 2회 이상 1개, 다음 시험 대기 3개로
  화면에 표시된다.
- 390px·768px·1440px에서 가로 넘침과 깨진 대체 문자 0건을 확인했다.
  현재 OS의 다크 모드 화면은 직접 확인했고 라이트 모드는 토큰·CSS 계약
  테스트까지 통과했다. 사용자 Preview 승인 전 `main`에는 병합하지 않는다.
- 가짜 관리자 계정은 `admin.preview@example.com`이다. 비밀번호와 가짜 학생
  코드는 저장소에 기록하지 않고 사용자에게만 인계한다.

### 2026-08-06 Preview 피드백 보정

- 공통 전환시간은 `250ms ease-in-out`으로 통일한다. 버튼은 호버할 때
  위치나 크기가 변하지 않고 배경·글자·테두리 색만 자연스럽게 바뀐다.
- OS 자동 다크 모드를 유지하면서 사용자가 직접 바꿀 수 있는 스위치를
  학생 로그인, 관리자 로그인, 관리자 네비게이션, 학생 네비게이션에 둔다.
  선택은 브라우저에 저장한다.
- PC 사이드바, 태블릿·학생 상단바, 모바일 관리자 하단 네비게이션에는
  반투명 배경과 12px blur를 적용한다.
- 점수는 항상 같은 두 열을 사용한다.
  - 첫 시험만 끝난 검토 단계: `첫 시험`
  - 재시험 없이 끝난 시험: 중복 점수를 없애고 `최종`
  - 실제 재시험을 시작한 시험: `첫 시험 / 재시험`
  - 첫 시험 미달·0점·미응시는 빨간색
  - 완료 상태는 노란색, 최종 미달은 빨간색
- 재시험 여부는 점수 변화나 `retry_correct_count`로 추측하지 않고 기존
  `quiz_attempts.retry_started_at`을 읽어 판정한다. DB 스키마 변경은 없다.

## 후속 제품 로드맵 — 2026-08-06 사용자 메모 구조화

### A. 다음 UX 배치

1. 시험 관리 배정 흐름을 학생 선택 → 범위·유형 → 출제 수·시간 → 오답
   포함 → 최종 확인의 짧은 단계로 다시 정리한다.
2. 개요·시험 관리의 기본 정렬은 다음 우선순위로 통일한다.
   - 마감시각이 없는 미완료 시험
   - 마감이 가까운 미완료 시험
   - 진행 중·미응시 마감 시험
   - 완료 시험(완료시각 최신순)
   시험 유형 필터 안에서도 같은 순서를 적용한다.
3. 개요·관리·학생 카드의 정보는 `현재 할 일 / 마감 / 결과 / 다음 행동`
   네 줄 이내를 기본으로 하고, 세부값은 펼침 또는 상세 화면으로 옮긴다.
4. 승인된 발음 데이터가 들어온 뒤 교사 배정의 `발음 표기 허용`과 학생의
   `발음 표시 켜기/끄기`를 분리한다. 미승인 발음에는 기능을 노출하지 않는다.

### B. 틀린 단어·해석 시험지 출력

1. 학생 화면이나 관리자 오답 화면의 `시험지에 담기` 버튼은 바로 PDF를
   만들거나 Drive를 수정하지 않고 `worksheet_requests` 대기열에 학생,
   오답 단어 ID, 뜻 ID, 학교 범위, 요청 유형을 스냅샷으로 저장한다.
2. 생성기는 대기열에서 구조화 JSON·Markdown 기준본을 만들고, 같은 입력
   해시로 인쇄 PDF를 재현한다. AI는 이 구조화 기준본을 읽는다.
3. Google Drive는 기준 데이터베이스가 아니라 승인된 Markdown·PDF의
   보관·공유 목적지로만 연결한다. Drive 장애가 앱의 오답 기록이나 시험
   배정을 막지 않게 한다.
4. 첫 배치는 한 학생·오답 10개로 요청 → 미리보기 → PDF → 재출력까지
   검증한 뒤 학교별 해석 시험지로 확장한다.

### C. 커리큘럼·문항 유형 확장

1. 해석 커리큘럼이 확정된 뒤 학생 숙달도와 학교별 시험범위를 구조화하고,
   AI는 승인된 범위 안에서만 단어 시험과 해석 연습 후보를 제안한다.
   자동 배정은 하지 않고 교사 확인 후 배정한다.
2. 고교 지문·유형별 문제와 모의고사형 객관식 시험은 공통 문항 모델,
   근거 지문, 정답 근거, 난이도, 출처 검수 상태를 먼저 정의한 뒤 앱에 넣는다.
3. 문법 숙제는 같은 객관식 엔진을 재사용하되 공식 영어식 선택지와 해설,
   모바일 터치 크기, 오답 유형 태그를 별도 계약으로 둔다.
4. 학원에서는 종이에 쓰는 해석, 동사 찾기, 문장 구조 표시를 유지하고,
   앱은 배정·제출 확인·오답 누적·다음 자료 추천을 맡는다.

### 명시적 보류

- 위 A~C 기능은 이번 CSS Preview 승인과 `main` 병합에 포함하지 않는다.
- 해석 커리큘럼 원문과 교재 제작은 `영어/00_자료_인계서.md`를 기준으로
  재개한다.

### 2026-08-06 모바일 Preview 사진 피드백

- 관리자 최근 시험 카드에서 시험 제목에 이미 들어 있던 DAY와 별도 범위
  DAY가 반복되지 않도록 표시 제목에서 동일 단원 조각만 제거한다. 저장된
  시험명 원본은 바꾸지 않는다.
- 모바일 카드의 학생명, 시험명, 범위·시각을 세 줄로 정렬하고, 완료 점수와
  취소 배정의 `점수 -`처럼 값이 하나뿐인 경우 모두 빈 첫 칸 없이 왼쪽부터
  표시한다. 상태는 점수 영역 오른쪽 아래 기준선에 맞춘다.
- 최종점수가 통과점수보다 낮으면 `미통과` 빨간색으로 표시한다. 첫 시험에서
  바로 통과하면 `완료` 초록색, 실제 재시험 후 통과하면 `재시험 통과`
  노란색으로 구분한다.
- 화면 상태는 과거 `passed` 플래그가 아니라 현재 표시하는 최종점수와
  배정 당시 통과점수를 비교해 판정한다. DB 스키마와 원본 성적은 변경하지
  않는다.

### 2026-08-06 관리자 전체 카드 밀도·태그 보정

- Overview만 따로 꾸미지 않고 학생 관리, 시험 관리, 시험별 관리, 배정
  관리, 내역이 같은 `meta-tag`와 상태 태그 규칙을 사용한다.
- 학생명과 시험명은 제목으로 유지하고 학교·학년·단어장·시험 유형·DAY·
  문항 수·오답 수처럼 비교 가능한 속성만 수동형 태그로 분리한다. 선택형
  필터 칩과 메타데이터 태그는 다른 컴포넌트다.
- 시험명에 포함된 동일 DAY는 표시 제목에서만 제거하고 저장된 원본은
  유지한다. 시험 관리 학생 목록의 중복 `최근 완료` 줄과 긴 추천 설명은
  제거하고 상세 배정 창에서 확인한다.
- 관리자 목록 행은 고정 높이를 두지 않고 내부 3~5px, 정보 묶음 8px를
  기준으로 한다. 학생 카드·배정 행·시험 관리 행·내역 행의 패딩은
  9~12px로 맞추고, toolbar 이중 여백과 한 줄 빈 화면의 최소 높이를
  제거한다. 버튼과 입력의 44px 터치 영역은 유지한다.
- DB·API·시험 상태 판정은 변경하지 않는다. 검증은 lint, TypeScript,
  61파일·354테스트, 로컬 Next.js 운영 빌드를 통과했다.

### 2026-08-06 고3 장문독해 검토본 최소 Preview

- 운영 DB는 변경하지 않았다. staging에서 기존 대량 가짜 자료는 삭제하지
  않고 복구 가능한 soft-delete·비활성 상태로 숨겼다.
- 현재 화면에 보이는 자료는 가짜 학생 1명, 활성 단어장 1개, 검토 단어
  8개, 활성 시험 0개다. 기존 가짜 학생·시험·단어장은 필요하면 복원할 수
  있다.
- 검토본 `고3 장문독해 2025-03 검토본`은 `pending_review`이며 양방향
  eligibility 16행도 모두 `review_required`다. 사전 연결과 승인 전 자료를
  `ready/eligible`로 꾸미지 않는다.
- 시험 관리 상단에 검토본 8개의 표제어·표시 뜻과 `검토 전용` 태그를
  표시한다. 이 목록은 사전 연결·발음 승인 전에는 시험 배정 선택지에
  들어가지 않으며 학생 카드도 클릭 불가 `검토 중`으로 표시한다.
- 검토본을 시험으로 생성하려던 1회 시도는 DB의 canonical snapshot 게이트가
  `vocab_capability_snapshot_mismatch`로 차단했다. 가짜 canonical 해시를
  만들지 않았고 시험 레코드도 생성되지 않았다.
- 다음 시작점은 8개를 포함한 필요한 범위의 단어 사전 연결·독립 검수를
  완료한 뒤 정식 vocab-link 패키지로 `ready` 승격하고, 그때 최소 4문항
  시험 배정을 검증하는 것이다.

### 2026-08-07 학습 관리·학생 중심 개편 계획

- 최신 사용자 피드백을 기준으로 기존 `시험 관리`를 학생 중심 `학습 관리`로
  재구성하는 구현 전 계획을
  `docs/학습관리_학생중심_개편계획_v2.md`에 기록했다.
- 현재 2025 장문독해 검토본은 `pending_review`이므로 시험 배정 불가다.
  단어 사전 연결·뜻/발음 승인·staging import 후 최소 4문항 배정 검증이
  선행되어야 한다.
- 다음 구현 시작점은 계획서의 배치 A다. 이후 공통 학생 행·필터·정렬,
  고정 크기 학생 모달, 일괄 배정, 조치 중심 Overview 순으로 진행한다.
- Preview 가짜 데이터는 기본 학생 1명만 유지하고 다중 선택 검증 때만
  최대 3명으로 늘린다.

### 2026-08-07 학습 관리 1차 구현

- 작업 브랜치·경계: `redesign`에서만 구현했다. `main`, Production,
  운영 Supabase는 변경하지 않았다.
- 네비게이션의 `시험 관리`를 `학습 관리`로 바꾸고, 학생 관리·학습 관리·
  내역에 breadcrumb를 적용했다. 반복 페이지 제목과 설명은 제거했다.
- 학습 관리는 최상단 `단어 / 다른 학습` 탭, 통합 검색, 학교·학년·단어장·
  오답 상태 필터 태그, 학생 중심 목록으로 재구성했다. 읽기 전용 단어장
  검토판과 중복 시험별·배정 관리판은 제거했다.
- 공통 학습 정렬은 마감 없음 → 가까운 마감 → 진행 중 → 미통과·미응시
  마감 → 통과 완료 → 취소·삭제 순이다. Overview도 같은 판정을 사용해
  `미응시 마감 / 미통과·재시험 필요 / 마감 예정 / 마감 없음`만 표시한다.
- 학생 모달은 고정 작업면과 고정 헤더·탭, 본문 스크롤을 사용한다. 탭은
  `학습 관리 / 계정 설정 / 내역` 순이며 최근 5개와 전체 보기를 제공한다.
- 단어 학습 하위 화면에서 오답 목록을 확인하고, 페이지 이동 없이 기존
  검증된 배정 폼을 같은 학생으로 열 수 있다. 계정 설정에서는 이름·학교·
  학년 수정, 코드 관리, 차단·재개, 학생 삭제를 제공한다.
- 미응시·응시 전 항목에는 `배정 취소`, 종료 상태에는 `내역 삭제`만
  표시한다. 사용자가 의미를 알기 어려운 `시험 전체 삭제`는 학생 중심
  화면에서 제거했다.
- 학생 정보 수정은 동일 출처 요청·관리자 세션·Zod 입력 검증 뒤 서버에서
  실행하며 `student.profile_updated` 감사 이벤트를 남긴다. DB 스키마
  변경은 없다.
- 검증: ESLint 통과, TypeScript 통과, 62파일·357테스트 통과, 검증 전용
  가짜 환경값으로 Next.js production build 통과. 실제 staging 화면은 새
  Preview 배포 뒤 최소 가짜 학생 1명으로 확인한다.
- 보류: 2025 장문독해는 독립 2회 검수 불일치가 남아 `pending_review`를
  유지한다. `student_learning_sources`와 원자적 일괄 배정은 배치 D에서
  함께 구현한다. 브라우저가 학생별 API를 반복 호출하는 임시 일괄 배정은
  넣지 않는다.
- Git·Preview: 구현 커밋 `8a47a9a`를 일반 `git push origin redesign`으로
  올렸다. Vercel Preview `dpl_582g5zNsjcdNN31tqWwTZPgYuUo7`은 `READY`이고
  `redesign` 고정 주소가 이 커밋을 가리킨다. GitHub CLI와 Vercel CLI는
  사용하지 않았다.
- 배포 뒤 staging 읽기 전용 확인값은 노출 학생 1명, 활성 데이터셋 1개,
  활성 시험 0개, 과거 attempt 4개다. 이번 화면 검토용 DB 쓰기나 더미
  데이터 추가는 0건이다.
- 보호된 Preview의 관리자 로그인 화면 렌더링과 새 빌드 응답을 확인했다.
  관리자 내부 화면의 실제 시각 검사는 저장소에 없는 Preview 관리자
  비밀번호가 필요하므로 인증정보를 추측하지 않고 사용자 로그인 뒤로
  남긴다.

### 2026-08-07 학습 관리 배치 D — 원자적 일괄 배정·학습 자료

- 작업 경계: `redesign`과 staging ref `wojxpruvbjzbhrpmsbuy`만 변경했다.
  `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.
- 학습 관리 학생 목록은 기본 0명 선택, 개별 체크, 현재 필터 결과 전체
  선택을 제공한다. `다음 범위 일괄 배정`과 `틀린 단어 포함`은 학생별
  단어장·권장 DAY·최대 문항 수를 서버에서 미리 계산한다.
- 미응시·진행 중 시험, 완료된 단어장, 자동 범위를 알 수 없는 자료,
  오답 대기 없음은 학생별 사유를 표시하고 전체 저장을 막는다.
- 단건과 일괄은 같은 일반·혼합 문제 생성기를 사용한다. 일괄 저장은 최대
  30명의 완성된 문항 payload를 `create_bulk_vocab_assignments_v1` 한 번에
  보내며 학생 행 잠금과 기존 DB 게이트를 통과해야 한다. 한 건이라도
  실패하면 전체가 롤백된다.
- `student_learning_sources`를 추가해 주 단어장·시험 대비 단어장·교과서·
  부교재·모의고사·지문을 구분할 수 있게 했다. 기존 활성 학생의
  `current_vocab_dataset_id`는 `primary_vocab`으로 이관하고 이후 단어장
  변경도 트리거로 동기화한다.
- staging 적용 마이그레이션:
  `20260806221525_add_bulk_learning_management.sql`,
  `20260806223229_tighten_student_learning_sources_grants.sql`.
  두 번째 마이그레이션은 기존 기본 권한으로 남아 있던 authenticated 쓰기
  권한을 명시적으로 회수한다.
- staging 검증값: RLS true, 관리자 SELECT 정책 1개, 익명 SELECT/함수 실행
  false, authenticated SELECT true, INSERT/UPDATE/DELETE false, 기존 주
  단어장 대상 1명과 이관 1건 일치. 변경 후 Supabase advisor에서 새 보안
  오류 0건이며 새 인덱스 3개의 미사용 INFO만 확인됐다.
- 앱 검증: ESLint, TypeScript, 64파일·365테스트, 로컬 Next.js production
  build 통과. 빌드 환경값은 기존 로컬 파일을 현재 프로세스에만 읽었고
  저장소나 출력에 복사하지 않았다.
- Preview 화면 확인 전에는 `main` 병합과 Production 마이그레이션을 하지
  않는다. 다중 학생을 새로 만들지 않았으므로 현재 Preview에서는 학생
  1명의 선택·미리보기와 차단 상태만 확인한다.
- 다음 시작점: Preview에서 학습 관리 목록 정렬·체크박스·일괄 미리보기
  모달을 확인한다. 실제 다중 원자성 실사용 검증이 필요할 때만 가짜 학생을
  최대 2명 추가하고 검증 직후 숨기거나 삭제한다.
- 후속 보류: 오답만으로 구성된 별도 일괄 재시험, 비단어 학습 자료의
  유형별 범위 편집, 2025 장문독해 독립 검수·승인·시험 배정.
- Git·Preview: 구현 커밋 `33744d4`를 GitHub CLI 없이 일반
  `git push origin redesign`으로 올렸다. Vercel 배포
  `dpl_5KmFBxs2FUvSneUYEAeW9fMGDmdh`는 `READY`이며 redesign 고정 주소가
  이 커밋을 가리킨다.
- 기존 Preview 관리자 세션으로 `/admin/assignments`를 열어 학생 1명 기본
  미선택, 체크 후 선택 수·전체 해제·두 일괄 버튼 활성화를 확인했다.
  `다음 범위 일괄 배정` 미리보기는 pending_review 단어장 때문에
  `0명 가능 · 1명 확인 필요`와 구체 사유를 표시했고 저장 버튼은 잠겼다.
  이 검증에서 시험·학생·오답 데이터 쓰기는 0건이다.
- PC 화면의 일괄 미리보기 모달에서 설정 6개, 학생별 태그·오류, 하단
  취소·저장 동작이 잘림이나 가로 넘침 없이 표시되는 것을 확인했다.

### 2026-08-07 2025 장문독해 exam-use v1 앱 연결 — 적용 전 체크포인트

- 작업 경계: `redesign` worktree와 staging ref
  `wojxpruvbjzbhrpmsbuy`만 대상으로 했고, `main`, Production,
  운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.
- Obsidian Markdown＋YAML v1을 기준본으로 유지하고 앱에는 text
  `dictionary_id`·occurrence·release의 결정적 projection만 넣는 경로를 구현했다.
- actual package dry-run: 604 occurrence, 585 dictionary ID, 출제 601,
  검토 필요 1, 제외 2, 공식 음원 364, 듣기 비활성 240, DB 쓰기 0.
- 신규 migration은 기존 assignment·question·UUID eligibility 스키마를 건드리지 않고
  release·occurrence projection, service-only 문제·발음 sidecar snapshot,
  관리자 전용 일반배정 후보 RPC와 `create_assignment_with_delivery_v5`를 추가한다.
  후보 상태는 sidecar의 `reviewed_for_preview_v1`이며 기존 문제은행은 호환용
  `legacy_backfill`·version 1을 유지한다. 공통 사전 `approved`로 승격하지 않는다.
- 영어→한국어는 표제어 아래 한글 근사 발음과 독립 스피커를 표시하고 문항
  진입 시 공식 음원을 한 번 재생한다. 한국어→영어는 네 선택지 모두 공식
  음원이 있을 때만 선택지별 근사 발음·스피커를 켠다. 기존 500ms 정답 피드백과
  다음 문항 타이머 지연은 유지했다.
- 검증: ESLint·TypeScript 통과, 전체 69파일·389테스트 통과, 합성 4행으로
  import 멱등성→양방향 배정→snapshot→학생 응시 통합시험 통과,
  Next.js production build 통과.
- migration SHA-256:
  `0C35278093940D5683790BF7D29A5DD963E1EDDE094AC5023CC501D5405D20E6`.
- 사용자 명시 승인 후 migration과 실제 package를 staging에만 적용했다.
  dataset `d5b0a7e9-ea28-47de-94cf-c06b640ae995`, release
  `2e0f8780-a996-48a0-880e-3bd5fdb87471`이며 두 번째 import는 멱등이다.
- 실측: unit 14, entry 601, occurrence 604, 전체 dictionary 585, 출제 포함 dictionary
  582, 공식 음원 364, 듣기 비활성 240, shared eligibility/capability 각각 0.
- staging 가짜 학생 1명에게 공식 음원 4개로 양방향 4문항을 만들고 sidecar·release
  snapshot·실제 attempt를 확인했다. 검증 트랜잭션은 롤백해 남은 시험·snapshot·감사
  기록이 0건이며 PostgREST sidecar 관계도 service client에서 해석됐다.
- 다음 시작점: `redesign` push → Vercel Preview 배포 확인 → 관리자 화면에서
  `g12-long-reading-2025-exam-scope-v1` 단일 학생 일반 시험 배정 최종 확인.
- text-ID dataset의 일괄·혼합 오답·오답 전용 재시험은
  `00_작업지시/APP-20260807-04`로 분리했다. 기존 공통 v3 코어를 이번 migration에서
  전역 교체하거나 기존 발음 컬럼을 삭제하지 않는다.

### 2026-08-07 2025 장문독해 Pilot 4 실제 배정 검증

- 작업 경계는 `redesign` worktree와 staging ref `wojxpruvbjzbhrpmsbuy`뿐이다.
  `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.
- 승인 dataset `g12-long-reading-2025-approved-pilot-4-v2`를 `ready`·활성으로 적재했다.
  항목 4, 정확 사전 연결 4, occurrence 4, eligibility 8행이며 연결 import는
  `complete`다. 이전 8행 검토 dataset은 삭제하지 않고 비활성화했다.
- 가짜 학생 1명의 주 단어장을 승인 Pilot 4로 교체했다. 화면에 보이는 학생 1명,
  활성 학습 출처 1개, 활성 dataset 1개를 유지했다.
- 실제 `create_assignment_with_delivery_v4`를 4문항·학생 1명으로 실행해 문제 4,
  학생 연결 1, canonical lexeme 스냅샷 4, 출제 방향 스냅샷 2를 확인했다.
  트랜잭션을 롤백해 남은 Pilot 시험은 0개다.
- 앱 import는 후보 `applyAllowed=false`를 DB 연결 전에 차단한다. 완전 출제 가능한
  단어장은 capability `ready`, 일부 가능은 `limited`, 전부 제외는 `blocked`로
  계산하도록 고쳤고 고정 mapping-candidate 수 가정도 제거했다.
- 단어장 출처 메타데이터는 더 이상 `NE능률 / 2025개정` 고정값을 쓰지 않는다.
  import 패키지의 `sourceMetadata`를 사용하며 이번 자료는
  `exam4you · 고3 · 2025`, 교육과정 `null`이다.
- 검증: ESLint, TypeScript, 65파일·371테스트, 안전한 빌드 전용 공개 환경값을 쓴
  Next.js production build 통과. DB 쓰기 검증은 staging에서만 수행했다.
- 앱 연결 패키지 스냅샷:
  `A8CA805830CF7D6FDA4F21398A113F065BC5A49C58C42AE6AE7608BCF69FD976`.
- 다음 작업은 2025 승인 범위를 작은 배치로 넓힌 뒤 Preview UI에서 실제 수업 범위를
  배정하는 것이다. 585개 전체 candidate 자동 승격은 금지한다.

### 2026-08-07 2025 장문독해 exam-use v1 Preview 완료

- 앱 구현 커밋 `a5a34199d44634b6c99af7c987b09f6f21fe6ea9`을 GitHub CLI 없이
  일반 `git push origin redesign`으로 올렸다.
- Vercel 커밋 전용 Preview는
  `https://english-academy-student-ox5orooxl-thetilldawn-3859s-projects.vercel.app`이며
  상태 `READY`, 빌드 시간 38초를 확인했다.
- 커밋 전용 URL의 학생 로그인 화면 렌더링을 직접 확인했다. 이 확인에서는 시험·학생·
  오답 데이터를 추가하지 않았다.
- staging에는 dataset `d5b0a7e9-ea28-47de-94cf-c06b640ae995`, release
  `2e0f8780-a996-48a0-880e-3bd5fdb87471`가 활성 상태다. 단일 학생 일반 시험의
  양방향 4문항·sidecar·attempt 검증은 트랜잭션 롤백으로 잔여 레코드 0건이다.
- `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.

- 다음 활성 작업은 루트 작업지시 `APP-20260807-01`이다. text-ID dataset의
  일괄·혼합 오답·오답 전용 재시험은 `APP-20260807-04`에서 처리한다.

### 2026-08-08 학습 관리 UI 통합 Preview 체크포인트

- 작업 경계: `redesign`과 staging ref `wojxpruvbjzbhrpmsbuy`만 대상으로 했다.
  `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.
- 구현 커밋 `eb6916f463b2d632c89a9c0afa9c9370f6b7cb1d`을 GitHub CLI 없이
  일반 `git push origin redesign`으로 올렸다.
- Vercel 커밋 전용 Preview는
  `https://english-academy-student-19u1qk5ll-thetilldawn-3859s-projects.vercel.app`,
  고정 브랜치 Preview는
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`이며
  최신 배포 상태는 `READY`다.
- Overview를 미응시 마감·미통과/재시험 필요·곧 마감·마감 없음의 네 조치
  구역으로 정리하고, 행에서 기존 학생 상세 작업면을 페이지 이동 없이 연다.
- 학생 관리와 학습 관리가 같은 학습 자료 태그·검색·우선 확인 판정을 쓰고,
  학생 모달은 고정 높이의 `학습 관리 / 계정 설정 / 내역` 작업면을 사용한다.
- 자료별 `+`는 선택한 실제 dataset ID를 단어 배정·오답 화면으로 전달한다.
  내역 상세도 페이지 이동 없이 열며, 만료 항목은 마감 시각으로 정렬·표시한다.
- 학습 관리 목록에는 상태에 맞는 보기·배정·취소·내역 삭제만 노출한다.
  태블릿 768~1199px의 학생 카드 열과 하단 일괄 작업 바에 별도 레이아웃을
  적용했다.
- 검증: ESLint, TypeScript, 70파일·396테스트, Next.js production build 통과.
  `git diff --check`도 통과했다.
- 기존 Preview 관리자 세션으로 1280px 학습 관리 화면을 읽기 전용 확인했다.
  단어/다른 학습 탭, 검색·필터, 가짜 학생 1명, 배정 진입이 새 커밋에서
  렌더링됐고 가로 넘침은 없었다. 학생·시험·오답 데이터 쓰기와 더미 데이터
  추가는 0건이다.
- 앱 내 브라우저의 viewport override가 값을 받아도 실제 `innerWidth`를
  1280px에서 바꾸지 못했다. 따라서 390·768·820·1440px 실화면 최종 검사는
  코드·테스트 통과와 구분해 사용자 Preview 확인 대기로 남긴다.
- 미응시 배정 `수정`은 여러 학생이 공유하는 시험 원본을 바꿀 위험이 있어
  UI 버튼만 추가하지 않았다. 학생별 새 시험 생성→연결 교체→기존 연결 취소를
  한 트랜잭션으로 수행하는 후속 작업 `APP-20260808-01`로 분리했다.
- 현재 활성 작업 `APP-20260807-01`은 사용자 Preview 확인 전까지 완료로
  올리지 않는다. text-ID 일괄·혼합 오답 호환 `APP-20260807-04`도 그대로
  분리 유지한다.

### 2026-08-08 응시 전 학생별 배정 수정 API 완료

- 작업 경계: `redesign` worktree와 staging ref
  `wojxpruvbjzbhrpmsbuy`만 사용했다. `main`, Production, 운영 ref
  `xdxhswjgksukjmpbzqgz` 변경은 0건이다.
- 구현 커밋 `e5eca3c5def2285fb6395212e2b294c7bcbfa2f2`에서 응시 기록이 없는
  학생별 배정을 기존 공유 시험 원본과 분리해 새 시험으로 원자적 교체한다.
  학생 모달과 학습 관리 목록은 같은 편집 폼·API를 사용하고 저장 전에 범위,
  문항 수, 순서, 시간, 오답 포함 조건의 전후 차이를 표시한다.
- 일반·혼합·오답 전용 배정의 snapshot 처리, 오답 대상 연결, 원 연결 취소,
  감사 기록을 하나의 DB 트랜잭션으로 묶었다. 동일 idempotency key 재시도는
  학생 상태 또는 요청 당시 마감이 바뀐 뒤에도 기존 성공 결과를 반환하며,
  다른 payload의 키 재사용은 거부한다.
- staging에는 `replace_student_assignment_v1`,
  `index_assignment_replacement_foreign_keys`,
  `harden_student_assignment_replacement_v2`를 적용했다. 원격 v2 migration version은
  `20260807232925`다. v2는 `authenticated`만 공개 wrapper 실행 가능하고,
  v1은 `authenticated`·`service_role` 모두 차단했으며 private 요청 원장 직접 조회도
  차단한 것을 원격에서 확인했다.
- migration SHA-256:
  - v1: `6D96D34BCE985F9BF830E9C06AA920551CCEC079197DBA87C8EF0FECABE217E3`
  - FK index: `E7FFA69876E48A813F83B650D7FFB2861DBD377FF0B47E8C5D1C28E6F02C9B9D`
  - v2 hardening: `62B6558C24EBF3734AB035C06036931BE2BAE14BC137DE46F029C0F9A19A6592`
- 검증: ESLint·TypeScript·핵심 UI/API 53개·DB 통합/계약 19개·Next.js
  production build가 통과했다. 전체 74파일·431테스트 중 기존 `pg_cron`
  PGlite 계약 1개가 병렬 실행의 5초 제한에 걸렸지만 같은 파일 단독 실행은
  2/2, 1.85초로 통과했다. `git diff --check`도 통과했다.
- staging 가짜 학생 1명으로 실제 원 배정 취소, 대체 배정 생성, 동일 요청 재사용,
  원장 1행, 감사 기록 1건을 확인했다. 검증은 한 트랜잭션에서 전부 롤백해
  잔여 시험·감사 기록이 0건이다.
- 일반 `git push origin redesign` 뒤 GitHub의 Vercel 상태가 `success`인 것을
  확인했고 브랜치 Preview
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`
  관리자 로그인 화면이 정상 응답한다. Preview에서 새 더미 데이터는 추가하지 않았다.
- 다음 후보는 `APP-20260807-02` 시각 마감이다. 태그·정렬·간격·다크모드·
  내비게이션을 390·768·820·1440px에서 검수하며, text-ID 일괄·혼합 오답
  호환은 계속 `APP-20260807-04`로 분리한다.

### 2026-08-08 반응형 시각 마감 완료

- 구현 커밋 `32fd7c1`과 모바일 제목 보정 `ff9279e`를 일반
  `git push origin redesign`으로 브랜치 Preview에 반영했다.
- 학생 카드와 학습 관리 행에서 중복 상태·가짜 점수·과도한 태그를 제거했다.
  학생 정보, 학습 자료, 다음 범위를 별도 태그 그룹으로 나누고 자료 태그는 최대
  2개와 `+N`으로 제한했다. 행 행동은 상태에 맞는 `보기` 또는 `배정` 하나만 두고,
  수정·취소·삭제는 상세 작업면에 유지했다.
- 상태 의미는 첫 시험 통과 초록 `완료`, 기준 미달·0점·미응시 빨강 `미통과`,
  실제 재시험 노랑 `재시험`으로 통일했다. 재시험이 시작되기 전에는 `미통과`,
  시작된 뒤에만 `첫 시험 / 재시험` 점수 열을 표시한다.
- 일반 전환은 `250ms ease-in-out`, 학생 상세 슬라이드는 `300ms ease-in-out`이며
  버튼 hover에는 위치·크기 이동이 없다. 내비게이션은 라이트·다크 모두 반투명
  배경과 `blur(12px)`를 쓴다.
- 390·768·820·1440px, 라이트·다크에서 관리자 학생 관리·학습 관리·Overview·
  내역과 학생 화면을 실제 Preview DOM·계산 스타일로 검사했다. 전 구간 가로 넘침과
  깨진 대체문자는 0이었다. 학생 콘텐츠 여백은 각각 12px, 18px, 18px, 최대 너비
  900px 중앙 정렬이다.
- 학생 상세 모달은 820px에서 784×760, 390px에서 약 390×844로 학습 관리와
  계정 설정을 오가도 외곽 크기가 유지됐다. 모바일 상단 제목도 `학습 관리`로
  바로잡았다.
- 전체 76파일·442테스트, TypeScript, ESLint, production build가 통과했다.
  제목 보정 뒤 내비게이션 계약 9개와 TypeScript·ESLint를 다시 통과했다.
- Preview에서 기존 가짜 학생만 사용했고 명시적인 생성·수정·삭제와 새 더미 데이터
  추가는 0건이다. 일부 GET 경로가 기존 만료 상태 정리 RPC를 호출할 수 있어
  엄밀한 DB 쓰기 0으로는 기록하지 않는다. 화면 캡처 도구는 시간 초과됐지만 실제
  DOM·계산 스타일 검사는 완료했다.
- 브랜치 Preview는
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`다.
  `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.

## 2026-08-09 일괄 배정·해석 범위·시험 화면 보완 (redesign Preview)

- 작업 기준: `redesign` 브랜치와 staging Supabase
  `wojxpruvbjzbhrpmsbuy`. 운영 Supabase와 `main`은 변경하지 않았다.
- 구현 커밋 `a3fe75c`를 GitHub CLI나 Vercel CLI 없이 일반
  `git push origin redesign`으로 올렸다. Vercel Git 연동의 Preview 대상이다.
- 일괄 배정은 일반 시험을 release-aware v5, 틀린 단어 포함 시험을
  v7 저장기로 통일했다. 미리보기와 저장이 다음을 동일하게 계산한다.
  - `전체 오답 / 현재 선택 범위 오답`
  - 최대 500개
  - exam-use `dictionary_id`, 기존 canonical UUID, 표제어 fallback
  - 현재 활성 release에서 실제 출제 가능한 항목
- staging 적용 마이그레이션:
  - `fix_exam_use_bulk_assignments_v2`
  - `add_student_reading_context_sync`
  - `count_distinct_initial_wrong_attempts`
- 학생 상세의 수동 `해석 시험지 범위 업데이트`는 선택한 오답의 불변
  worksheet snapshot과 학생 이름·학교·학년·영민정음 범위를 DB에 먼저
  보존한다. Drive 연결이 켜져 있으면 학생별 JSON 한 파일을 갱신하고,
  실패해도 DB snapshot은 유지한다.
- 실제 Drive 쓰기는 아직 비활성이다. Preview 전용 비공개 폴더와 OAuth
  refresh token을 받은 뒤 `.env.example`의 `GOOGLE_DRIVE_*` 값을 Vercel
  Preview에만 넣어야 한다. Preview 폴더 ID가 다르면 빌드와 런타임이
  모두 차단된다.
- 학생 시험 화면은 문제·선택지의 높이와 스피커 열을 고정했고, 긴 문구는
  두 줄 안에서 축소한다. 영어 선택지의 발음 줄도 항상 같은 공간을
  예약한다.
- 오답 선택지는 `shape-v1` 정책으로 같은 record type, 한국어 뜻 형태,
  영어 형태와 길이가 가까운 후보를 우선한다. 정식 `sense_id/pos/CEFR`
  난이도 계약은 공통 단어 사전 승인 뒤 후속으로 남긴다.
- 학생 결과 화면은 답안 이력을 노출하지 않고 정답 선택지만 보여준다.
  초회 오답 시험 1회는 노란 왼쪽 막대, 2회 이상은 빨간 막대다. 같은
  시험의 재시험 오답은 누적 횟수에 다시 더하지 않는다.
- 검증: lint, TypeScript, 91파일·509테스트, Next.js 운영 빌드 통과.
  PGlite 전체 스키마와 exam-use 통합 테스트도 통과했다. 사용자가 제외한
  롤백 실패 주입 테스트는 수행하지 않았다.
- 세부 기록: [20260809 작업 기록](docs/20260809_일괄배정_해석범위_시험화면.md)

### 2026-08-09 UI 체계·알림·목록·세션 정비

- 작업 경계는 `redesign` worktree와 staging ref
  `wojxpruvbjzbhrpmsbuy`다. `main`, Production, 운영 ref
  `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.
- 수동 `app-toast`를 제거하고 `sonner@2.0.7`과 루트 Toaster 하나로 통일했다.
  서버 작업 결과는 2초 동안 PC·태블릿 우측 상단, 모바일 상단 중앙에 표시한다.
  입력칸 오류는 기존처럼 필드 가까이에 남긴다.
- 공통 Button·Tabs·Modal·Select 계약을 적용했다. 버튼 높이는 36/44/58/36px,
  문구 한 줄, 위치 이동 없는 250ms hover다. 모달은 고정 헤더·스크롤 본문·필요한
  경우 고정 하단 작업 영역을 사용한다. 학생 상세의 단어 배정은 별도 dialog를
  열지 않고 같은 dialog 안의 하위 화면과 `← 뒤로`로 전환한다.
- 한국어 UI는 공식 `pretendard@1.3.9` 패키지 내부 WOFF2를 자체 번들한다.
  Source Serif 4는 영어 문제·영어 선택지·검토 표제어에만 남겼다.
- 학생·관리자 목록은 공통 정렬기로 `응시할 시험 → 미응시·미통과 → 완료`를
  사용한다. 취소·삭제는 현재 목록에서 제외하고 내역에서만 확인하며, 내부 시험
  종류와 dataset slug는 중앙 표시 함수에서 사용자 이름으로 변환한다.
- 화면 문구는 `src/content/ko/`의 타입 있는 위치별 key-value 파일로 이동했다.
  API 오류 코드와 감사 기록 같은 서버 계약 문구는 이 경계 밖에 유지한다.
- staging에 `20260808180535 add_notification_receipts_and_rolling_sessions`를
  적용했다. SQL SHA-256은
  `BF8D32D00865089CD195F74F5747E017ADB99681B15531D8CF8BCCFB7C581B3E`다.
  `notification_receipts`는 RLS를 켜고 browser role 권한과 RPC 실행권한을 모두
  차단했으며 service role만 원자적 수령·학생 세션 갱신 RPC를 호출한다.
- 학생 세션은 정상 인증 때마다 60일로 연장하고, 관리자 Supabase 세션에는 학생용
  고정 만료 상수를 적용하지 않는다. 학생·관리자 proxy와 로그아웃은 역할별로
  계속 분리한다.
- 검증은 ESLint, TypeScript, 전체 87파일·499테스트, production build,
  `git diff --check`를 통과했다. 기존 PGlite 예약 작업 테스트는 전체 병렬 실행의
  초기화 시간을 반영해 테스트 제한만 5초에서 15초로 조정했다.
- 로컬 브라우저 360·768·1440px, 라이트·다크에서 공개 학생·관리자 로그인 화면을
  확인했다. 가로 넘침·오류 overlay·console 경고는 0이고 실제 body font는
  `Pretendard Variable`이다. 보호 화면의 데이터 변경은 하지 않았다.
- 현재 `npm audit`에는 기존 Next/PostCSS의 `nanoid` 1건과 개발 도구의
  `brace-expansion`, `js-yaml`을 합쳐 high 3건이 남아 있다. 새 Sonner·Pretendard
  의존 경로가 아니며, 승인 없이 `npm audit fix`를 실행하지 않았다.
- 앱 작업은 대기 상태다. 루트의 다음 승인 후보는 `MAT-20260807-01`이며,
  text-ID 일괄·혼합 오답 호환은 계속 `APP-20260807-04`로 분리한다.

### 2026-08-08 틀린 단어 해석 시험지 요청·export 완료

- 구현 커밋 `b5f1148`에서 학생 상세 오답 목록에 서로 독립적인 `다음 시험`과
  `해석 시험지` 선택 목적을 추가했다. 해석 시험지 요청은 다음 시험 대기열을 바꾸지 않는다.
- 관리자 전용 요청 생성 API와 이름 없는 구조화 JSON export API를 추가했다.
  요청은 학생·dataset·entry 집합으로 멱등이며 최대 50개 고유 오답만 받는다.
- staging ref `wojxpruvbjzbhrpmsbuy`에 다음 migration을 적용했다.
  - `20260808010555_wrong_word_worksheet_requests.sql`
  - `20260808015000_index_wrong_word_worksheet_foreign_keys.sql`
  - `20260808015500_cover_wrong_word_worksheet_composite_fk.sql`
- migration SHA-256은 본 migration
  `6FE392104D14205A10C58F8F036202300E7928AF4926C91EE73790DA86E79045`,
  FK index `F94A5F61C79E0732C535DC645DCDB0901D85CBE19D5386C384A8F01C4D594CD0`,
  composite FK `243DBBD4EBB7B7D09C5E57202824428E9DD5D114346DBF27AC2EC99F2D48F97D`다.
  rollback SHA는 `FD66EAED1695C3280B79C5319256BD93AD83B2B4002FF10810054254842D2C52`다.
- staging 트랜잭션에서 순서를 바꾼 동일 항목 요청이 같은 ID·content hash와
  `reused=true`를 반환하고, export에서 학생 이름이 빠지며 사전·뜻·출처 occurrence가
  유지되는 것을 확인했다. 롤백 후 request·item·감사·검증용 오답 잔여는 모두 0행이다.
- 새 테이블은 RLS가 켜져 있고 관리자 SELECT 정책과 명시적 grant를 사용한다.
  RPC는 `SECURITY DEFINER`, 빈 `search_path`, anon 실행 불가 계약이다.
- 검증은 ESLint, TypeScript, 77개 파일·450개 테스트, production build를 통과했다.
  Preview에서 관리자 학생 상세의 `다음 시험 / 해석 시험지` 목적 분리를 읽기 전용으로
  확인했고 새 데이터는 만들지 않았다.
- 브랜치 Preview는
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`다.
- `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.

### 2026-08-09 Preview 안정화 검증 완료·Production 승인 대기

- 최종 Preview 커밋은 `0286040`이며 브랜치 URL은
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`다.
- 학생 결과에서 한 번 틀림 노란 막대, 두 번 이상 틀림 빨간 막대, 정답-only 카드를
  실제 시험으로 확인했다. review-only 신규 시험 제목과 태그는 DAY 범위를 붙이지 않고
  `오답 재시험 · N문항`으로 표시한다.
- 시험 선택지는 360·768·1440px에서 높이 76px, 음원 열 46px, 가로 넘침 0을 확인했다.
  공식 음원이 네 선택지 모두 있을 때만 듣기를 켜고, 없는 표현을 TTS로 대체하지 않는다.
- 해석 범위는 가짜 학생 오답 3개를 두 번 저장해 요청 1개·item 3개·동일 해시·revision
  증가를 확인했다. Drive 미설정은 `not_configured`이며 DB 저장 성공과 분리된다.
- 새 배정·마감 8시간 알림은 학생 두 탭 중 한 탭만 수령했다. DB receipt는 학생 새 배정
  1건·마감 1건·관리자 새 배정 1건이었다. 59일 세션은 갱신되고 61일 세션은 거부됐으며
  합성 세션과 일회성 배정은 정리했다.
- `npm.cmd run verify`는 ESLint·TypeScript·93파일 527테스트를 통과했고, 외부 폰트
  네트워크가 허용된 환경에서 Production build도 통과했다.
- 운영 ref `xdxhswjgksukjmpbzqgz` 사전 점검 수치는 학생 4명, 배정 20개, 활성 학생 배정
  14개, 진행 중 응시 0개, 단어장 1개, 단어 3,001개, 오답 event 194개, review queue
  68개다. 아직 운영 DB·`main`·Vercel Production은 변경하지 않았다.
- 배포는 19개 호환 migration을 먼저 적용하고 구 v4/v6 작성 RPC를 유지한 채 새 앱을
  배포한다. Production READY 확인 뒤에만
  `20260809110000_retire_legacy_assignment_writers_after_redesign.sql`로 구 작성 RPC를
  회수한다. 전체 목록은 루트
  `00_작업지시/DEPLOY-20260809-01_redesign_Production반영.md`가 기준이다.
- staging 전용 `20260807015653 add_exam_ready_vocab_pronunciation`, 단어 후보 import,
  release 활성화는 이번 Production 반영에서 제외한다.
- 다음 시작점은 사용자의 운영 변경 명시 승인이다. 승인 전에는 운영에 쓰지 않는다.
- Production 반영이 끝난 뒤 단어 작업은 다른 작업에서 확정한 최신 설계를 먼저 병합한
  다음 진행한다. 현재 단어 사전 후보와 발음/TTS 작업은 이 배포에 포함하지 않는다.

### 2026-08-08 관리자 UI 회귀 수정 완료

- 구현 커밋 `5c63369`을 일반 `git push origin redesign`으로 Preview에 반영했다.
- Gowun Batang을 관리자 UI에서 제거하고 한글 제목·버튼·내비게이션·학생명을
  Pretendard·시스템 산세리프로 통일했다. Source Serif는 영문·시험 숫자용으로 남겼다.
- PC 좌측 하단에는 실제 관리자명과 로그아웃만 두고 테마 스위치는 우측 상단으로
  복원했다. 1200px 미만에서도 실제 관리자명을 표시하며 768~920px에서는 브랜드를
  숨겨 상단바 폭을 확보한다. 보이는 `다크` 문구는 제거했다.
- `dataset-display.ts`로 내부 `승인 Pilot`·기술 edition을 사용자 표시에서만 숨긴다.
  원본 dataset key·title·edition과 DB 연결은 바꾸지 않았다.
- 학생 카드는 `주 단어장 / 다음 범위 / 우선 확인`을 같은 68px 라벨 그리드로
  정렬하고 source divider와 580px 정렬 분기를 제거했다.
- ESLint, TypeScript, 전체 78파일·457테스트, production build가 통과했다.
  최종 접근성 보정 뒤 관련 20테스트와 TypeScript를 재검증했다.
- 브랜치 Preview 1280×720에서 좌측 하단 관리자 영역, 우측 상단 스위치, 카드 행
  정렬을 확인했다. 가로 넘침 0, divider 0, 내부 Pilot 문자열 노출 0이다.
- Preview에서 데이터 생성·수정·삭제는 하지 않았다. `main`, Production,
  운영 ref `xdxhswjgksukjmpbzqgz`, staging 스키마·데이터는 변경하지 않았다.

### 2026-08-08 단어장 catalog·학생 학습 이력·버튼 규칙 완료

- 구현 커밋 `95d9dbb`을 `git push origin redesign`으로 Preview에 반영했다.
- 학생 카드의 범위 태그를 내용폭으로 복원하고 `주 단어장`을 `최근 단어장`으로
  변경했다. 학생 상세 세 탭은 공통 250ms 전환, 선택 상태색, 선택 탭 하단 연결,
  하단 radius 0 계약을 사용한다.
- 단어장·단원 catalog sidecar를 추가해 `중등 / 고등 / 고등 모의고사 / 수능`을
  구조화 metadata로 구분한다. 2025 고3 장문독해 전체 시험용 dataset만 신규 배정
  가능하고 legacy·Pilot은 과거 이력 전용이다.
- 학생 상세의 `학습한 단어장`은 실제 일반·혼합 응시로부터 이전 단어장과 마지막
  범위·최근 학습 시각·응시 횟수를 최근순으로 계산한다. 프로필의 최근 단어장 변경은
  이력을 지우지 않는다.
- 공통 `Button` 크기는 small 36px, default 44px, large 58px, icon 36px이고
  폼 컨트롤은 46px이다. 일괄 배정 모달은 16px 섹션 간격과 콘텐츠 높이 행을 쓴다.
- staging ref `wojxpruvbjzbhrpmsbuy` 원격 migration은
  `20260808115609 add_vocab_catalog_projection`이다. 적용 SQL SHA-256은
  `364235FA788BDF94A73F6ABBA325473524450046134FB16DA95FEFC122A1E681`,
  rollback은 `097BDE50426A7507BB289125DC110B6E29E66575529E3E44F71C31B47EEB7A4B`다.
- 전체 82파일·472테스트, ESLint, TypeScript 포함 production build,
  `git diff --check`가 통과했다.
- Preview에서 기존 가짜 학생 1명만 읽기 전용으로 확인했다. 새 더미 데이터와
  생성·배정·저장·삭제는 0건이다. `main`, Production, 운영 ref는 변경하지 않았다.
- 현재 활성 작업은 없다. 다음 승인 후보 `APP-20260807-03`과 보류 작업
  `APP-20260807-04`는 사용자 지시 전까지 시작하지 않는다.

### 2026-08-08 배정 모달 도움말·문항 계산·문구 카탈로그 완료

- 구현 커밋은 `48403ed`, 빈 토스트 보정은 `f85c48e`, 문항 수 라벨 접근성
  보정은 `31d41f5`, 수동 문항 수를 늦은 자동 계산 응답이 덮지 않게 한 보정은
  `7afe2a5`다. 모두 일반 `git push origin redesign`으로 Preview에 반영했다.
- 공통 `HelpTip`을 20px 원형 native Popover로 바꿨다. hover·focus·click·Esc와
  viewport 위치 보정을 지원하며 browser top layer를 사용하므로 모달 overflow에
  잘리지 않는다. 도움말 버튼과 input/select는 명시적 `htmlFor`/`id`로 분리했다.
- 불필요하게 상시 노출하던 배정·일괄 배정·단어장 검토·오답 반영·키보드 단축키
  설명을 `?` 도움말로 옮겼다. 오류·로딩·빈 상태·입력 차단 사유·삭제 결과·로그인
  코드 안내는 계속 화면에 직접 표시한다.
- `틀렸던 단어 추가`는 같은 단어장 안에서 `전체 단어장 / 현재 범위`를 고르고,
  현재 선택 조건으로 실제 포함 가능한 `전체 / 1회 / 2회 이상` 수를 표시한다.
  다른 단어장 오답을 섞는 기능은 `APP-20260807-04`로 계속 분리한다.
- Preview와 생성이 모두 exam-use projection을 사용하도록 통일했다. catalog가
  비어 있던 `g12-long-reading-2025-exam-ready-v1`은 신규 배정에서 내리고,
  실제 601개 exam-use 행이 연결된 `g12-long-reading-2025-exam-scope-v1`만
  신규 배정 가능하게 바로잡았다.
- staging ref `wojxpruvbjzbhrpmsbuy` 원격 migration은
  `20260808132320 fix_assignable_exam_use_dataset`이다. 로컬 migration
  `20260808130404_fix_assignable_exam_use_dataset.sql` SHA-256은
  `0C52D6971490E6D8C7B713E7C7F92BAB9F2A14FE6773DA7E43585634032C901D`,
  rollback SHA-256은
  `F4900820A75020E4735E6F9BB3F85534AF3CDE93D237DCB48D0DFC96B5E09DC9`다.
- 화면 문구는 `src/content/ko/` 아래 `admin-shell`, `admin-overview`,
  `admin-students`, `admin-learning`, `admin-history`, `student-app`, `common`
  TypeScript key-value catalog로 분리했다. 각 키 위의 주석이 화면 위치를 설명하며,
  키 구조를 유지하고 따옴표 안의 값만 바꾸면 실제 화면에 반영된다. 서버 오류 코드와
  감사 로그 문구는 catalog 경계 밖에 둔다.
- 외부 toast 패키지는 설치하지 않았다. native manual Popover 기반 앱 토스트를
  사용하며 메시지가 없을 때는 DOM에 빈 status/alert를 만들지 않는다.
- ESLint, TypeScript, 전체 84파일·478테스트, production build와
  `git diff --check`가 통과했다.
- Preview에서 기존 가짜 학생 1명만 사용해 신규 배정 가능 단어장이 1개로 보이고,
  첫 범위 46개가 자동 계산되며 10문항으로 줄여도 오류 없이 배정 버튼이 활성화되는
  것을 확인했다. 실제 배정·저장·삭제와 새 더미 데이터 생성은 하지 않았다.
- 브랜치 Preview는
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`다.
  `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.

### 2026-08-09 단어사전 ID 기반 일괄·오답 배정 호환 완료

- 작업지시 `APP-20260807-04`를 완료했다. 일반 v6, 혼합 v8, 일괄 v3,
  배정 수정 v3이 exam-use active release와 `dictionary_id` snapshot을 공통으로 사용한다.
- 오답 event/state/queue에는 틀렸을 당시 release·occurrence를 그대로 보존한다. 이후 단어장
  release에서 entry가 바뀌어도 같은 `dictionary_id`의 현재 occurrence로 다시 출제하며,
  assignment question/target에는 현재 entry와 현재 release snapshot을 저장한다.
- 첫 migration의 CRLF 의존 동적 재작성 문제를 direct exact-v5와 줄바꿈 정규화 보정으로
  제거했다. 배정 수정은 선택 queue 순서와 문항 꼬리 순서를 연결한 뒤 trigger에서 사전 ID를
  다시 검증한다.
- current RPC는 regular v6, mixed v8, bulk v3, replacement v3, identity v1이다.
  구 regular v4/v5, mixed v5/v6/v7, bulk v1/v2, exact v4, replacement v1/v2는
  `authenticated`와 `service_role`에서 실행할 수 없다.
- staging ref `wojxpruvbjzbhrpmsbuy` 원격 migration은 다음 세 개다.
  - `20260808230549 add_exam_use_exact_review_assignments`
  - `20260808231046 fix_exam_use_exact_direct_persistence`
  - `20260808233220 fix_mixed_review_dictionary_remap`
- 로컬 migration SHA-256은 각각
  `1085B7F2DC4C4326F0F9703725AF207802F3676A82B649EE6F238EA7E56DEFBB`,
  `06DB8B437955BC9A3514CC11971AC2675EED369923FA1E741166C43BAFD87C59`,
  `58BB4FAC84B3023AA3BC3F4F3B2CBE67F8CC740DD1326788DDEBF9AAC07961D4`다.
- `npm.cmd run verify`로 ESLint, TypeScript, 전체 92파일·521테스트와 production build를
  통과했다. 별도의 실패 주입 rollback 테스트는 추가하지 않았다.
- staging DB에는 이전 최소 가짜 학생 E2E 잔여물이 없고, 최종 migration 뒤에는 데이터
  추가 없이 current/retired RPC 권한, active release remap 본문, replacement v2 linker를
  확인했다.
- Supabase advisor는 security INFO 33·WARN 5, performance INFO 77이다. 신규 identity RPC
  WARN은 내부에서 active-admin을 검사하는 의도된 SECURITY DEFINER 경로이며, 나머지는 기존
  worksheet/import/auth 설정 경고다.
- `main`, Production, 운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.

### 2026-08-09 목록·상태·상세 화면 UI 안정화 Preview 검토 대기

- 작업지시 `APP-20260809-07`의 구현 커밋은 `bb8b998`이다. 일반
  `git push origin redesign`으로 브랜치 Preview에만 반영한다.
- 시험·계정 상태는 공통 `StatusBadge`, 점수는 `AttemptScoreSummary`, 마감·배정·상태
  시각은 `ActivityStatusTimeline`으로 통일했다. 첫 통과는 초록 `완료`, 재시험 전은
  빨강 `미통과`, 재시험 중과 재시험 통과는 노랑, 재시험 실패·미응시·시간 종료는
  빨강으로 표시한다. 좁은 목록 점수에는 `점`을 붙이지 않는다.
- 학생 상세 학습 내역과 전체 내역은 행 전체를 눌러 상세를 연다. 학습 관리 학생 행은
  정보 영역 클릭·Enter·Space가 체크를 바꾸고 별도 `보기` 버튼은 학생 상세를 연다.
  체크박스·보기·배정 작업은 서로 이벤트를 공유하지 않는다.
- 내역 주소는 `attempt.{attemptId}`와
  `assignment.{assignmentId}.{studentId}`를 사용하며 기존 UUID 응시 주소도 계속
  읽는다. `@detail` 병렬 슬롯과 `(.)results` 인터셉팅 라우트로 앱 내부에서는 기존
  학생 모달·필터·체크·스크롤을 둔 채 상세만 겹쳐 연다. 직접 URL·새로고침은 같은
  본문을 전체 페이지로 표시한다.
- 상세 헤더의 `결과 목록`은 하나만 남기고 sticky로 고정했다. 상세 취소·삭제 뒤에는
  브라우저 뒤로 이동이 끝난 다음 부모 서버 데이터를 갱신하며, 직접 접속 상세에서는
  전체 내역으로 새로 이동해 삭제된 주소를 다시 갱신하는 경쟁을 막았다.
- `.status-pill`, `.assignment-student-row`, `.admin-history-row`의 최종 기본 정의를
  하나씩 두고 광범위한 회색 `span` 덮어쓰기를 제거했다. 360px 선택 행은
  `체크 / 정보 / 보기` 3열을 유지하고, 768px에서도 마감과 상태는 세로 두 줄이다.
  키보드 포커스와 라이트·다크 선택 탭 외곽선도 복원했다.
- `npm.cmd run verify`로 ESLint, TypeScript, 96파일·543테스트와 production build를
  통과한 뒤 마지막 라우팅·배정 시각 보정을 포함해 전체 96파일·544테스트와 build를
  다시 통과했다. `git diff --check`도 통과했다.
- 인증된 관리자 Preview 브라우저 검사는 사용자 승인 단계로 남아 있다. 로컬에서는
  공개 셸의 360·768·1440px 가로 넘침과 라이트·다크 토큰만 읽기 전용으로 확인했고,
  staging 데이터 생성·수정·삭제는 하지 않았다.
- DB 스키마와 API 계약, staging·운영 데이터, `main`, Vercel Production,
  운영 ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다. 다음 단계는 Preview에서 부모
  모달 유지·뒤로/앞으로·직접 URL과 세 화면폭을 확인한 뒤 사용자 승인을 받는 것이다.

### 2026-08-09 redesign 운영 반영 완료

- 사용자 운영 반영 승인에 따라 `redesign`의 `13d5720fa7537f41a4bdd20d6212f5bedecbcc70`을
  일반 `git push origin redesign:main`으로 `main`에 반영했다. 기능 배포 GitHub deployment는
  `5815503704`, Vercel Production 상태는 `success`이며 운영 별칭은
  `https://english-academy-student-app.vercel.app`이다.
- 운영 Supabase ref `xdxhswjgksukjmpbzqgz`에 선행 migration 19개를 순서대로 적용한 뒤
  Production READY를 확인하고 `20260809110000_retire_legacy_assignment_writers_after_redesign.sql`을
  적용했다. 원격 이력의 마지막 버전은
  `20260809043614 retire_legacy_assignment_writers_after_redesign`이다.
- 적용 뒤 원본 행 수는 학생 4명·활성 3명, 배정 20개·활성 8개, 학생 배정 20개·미취소
  18개, 응시 16개·진행 중 0개, 문항 1,582개, 오답 event 194개, 상태 1,250개,
  review queue 68개로 사전 점검과 같았다. 활성 학생 세션 7개도 유지됐다.
- 신규 backfill은 알림 receipt 18개와 학생 학습 출처 3개다. 운영에 넣지 않기로 한
  Preview 단어사전 release·occurrence, 단어장 catalog, 해석 시험지 요청·항목은 모두 0건이다.
- 현행 regular v6, mixed v8, bulk v3, replacement v3은 `authenticated`와
  `service_role`에서 실행 가능하다. 구 regular v4/v5, mixed v6/v7, bulk v1/v2,
  exact v4, replacement v1/v2는 두 역할 모두 실행할 수 없다.
- 사후 회수 migration SHA-256은
  `D3C2A0D79954876D6CF5CC27C3710771E95D8D7AC8647D6D366A71994B3D63A8`,
  배포 창 되돌리기 파일
  `supabase/rollback/20260809110000_retire_legacy_assignment_writers_after_redesign.sql`의 SHA-256은
  `7B52EF4617B1AA8E7DA7A31C989F468A3DA9EAEE77233A2F0FE736C10B1371C8`이다.
- 첫 대형 SQL 전달 시 도구 출력 축약 문자열이 섞여 migration 1회가 문법 오류로 거부됐다.
  해당 호출은 트랜잭션 전체가 rollback됐고 이력·테이블이 생성되지 않은 것을 확인했다.
  이후 모든 SQL을 원본 UTF-8 byte 단위로 다시 전달해 20개 migration을 정상 적용했다.
- 전체 검증은 ESLint, TypeScript, 96파일·544테스트와 Production build를 통과했다.
  운영 공개 화면과 관리자 로그인은 200, 미인증 학생 세션 API는 예상대로 401이며,
  최근 Supabase API 로그 63건은 200 62건·201 1건·4xx/5xx 0건이다.
- security advisor는 INFO 33·WARN 5다. WARN 중 4개 SECURITY DEFINER 함수는 본문 첫 단계에서
  `private.is_active_admin()`을 검사하는 관리자 경로이며 나머지 1개는 기존 leaked-password
  protection 설정이다. performance advisor는 INFO 94이고 WARN·ERROR는 없다.
- 운영 검증을 위해 학생·시험·오답 데이터를 생성하거나 변경하지 않았다. 배포 전체 기록과
  원격 migration 매핑은 루트
  `00_작업지시/DEPLOY-20260809-01_redesign_Production반영.md`를 기준으로 한다.

### 2026-08-09 모바일 목록·모달 운영 회귀 수정 완료

- 작업지시 `APP-20260809-08`을 완료했다. 사용자 지시에 따라 Preview 승인 단계 없이
  로컬 검증 뒤 기능 커밋 `a0208f7415c711869423f6f20f9e79fca8a9c0c0`을
  `redesign`과 `main`에 함께 반영했다.
- 관리자 학생 카드의 점수와 상태·시간을 960px 이하에서 한 열로 쌓아 모바일 가로
  넘침과 날짜 잘림을 제거했다. 공통 상태 뱃지와 타임라인의 서버 계산은 바꾸지 않았다.
- 학생 시험 목록에서 마감된 미응시를 `마감된 시험` 구역으로 분리해 완료 뒤에
  표시한다. 진행 가능한 시험·미통과·완료의 기존 정렬은 유지한다.
- 학생 시험 카드의 페이지 전용 `detail-chip`을 공통 `MetaTag`로 교체하고 높이·패딩·
  수직 정렬을 고정했다.
- 기존 학생의 `코드 보기`는 두 native dialog를 중첩하지 않고 학생 상세 모달 내부
  화면으로 연다. `← 뒤로`는 계정 설정과 부모 모달을 유지하고, 최상위 닫기는 전체
  학생 상세만 닫는다. 신규 학생 생성 뒤 코드는 독립 창을 계속 사용하되 불투명
  backdrop·배경 클릭 닫기·스크롤 잠금·복사 상태 초기화를 적용했다.
- ESLint, TypeScript, 전체 97파일·548테스트와 Production build를 통과했다. 로컬과
  운영 공개 셸을 360px로 확인했으며 `scrollWidth = viewport 360`, 오류 overlay와
  브라우저 콘솔 오류는 0건이었다.
- GitHub deployment `5816278500`의 Vercel Production 상태는 `success`다. 운영 별칭은
  `https://english-academy-student-app.vercel.app`이다. DB migration·운영 데이터 변경·
  가짜 학생 생성은 없었다.

### 2026-08-09 시험 언어별 발음 열·모바일 정렬 수정

- 작업지시 `APP-20260809-09`에서 발음 열을 방향이 아니라 실제 화면 언어에 맞춰
  배치했다. `english_to_korean`은 영어 문제에만, `korean_to_english`은 영어 선택지에만
  발음 버튼 또는 영어용 placeholder를 둔다.
- 한글 문제와 한글 선택지에서는 46px 스피커 열과 한글 선택지 아래의 빈 16px 발음
  행을 모두 제거했다. 영어→한글 선택지는 전체 카드 폭을 사용하고 글자가 수직 중앙에
  놓인다.
- 영어 위치에서 승인 음원이 없을 때는 같은 영어 레이아웃끼리 흔들리지 않도록 기존
  placeholder를 유지한다. 네 영어 선택지 음원이 모두 승인됐을 때만 스피커를 일괄
  표시하는 기존 계약과 자동재생·답 저장 로직은 바꾸지 않았다.
- 방향 타입은 중복 문자열 대신 `lib/quiz/engine.ts`의 canonical `QuizDirection`을
  사용한다. 방향별 발음 열 계약 테스트를 추가했고 ESLint, TypeScript, 전체
  97파일·549테스트와 Production build를 통과했다.
- DB migration, 시험 문항 snapshot, 발음 데이터, 학생·시험·오답 데이터 변경은 없다.
  사용자 지시에 따라 Preview 승인 없이 같은 기능 커밋을 `redesign`과 `main`에 직접
  반영하며 배포 ID와 최종 상태는 루트 작업지시를 기준으로 한다.

### 2026-08-09 추천 범위 충돌·시험 반응형 운영 수정

- 운영 로그와 DB를 읽기 전용으로 대조했다. 최근 배정 화면의 capacity 조회는 모두
  200이었고 실제 생성 RPC는 호출되지 않아 DB 장애가 아니라 클라이언트 사전 차단으로
  확인됐다.
- 원인은 미응시·진행 중 시험의 첫 단원을 다시 `추천 범위`로 자동 입력하면서, 같은
  시험의 활성 단어를 중복 차단하는 계산과 충돌한 것이다. 운영 DAY 01은 원본 44개와
  양방향 eligible 44개로 데이터 자체는 정상이다.
- 새 배정에서는 `assigned / resume / manual` 추천을 기본 범위로 재사용하지 않고 시작·
  끝 범위를 명시적으로 선택하게 했다. 범위 계산 결과가 정상일 때 수동 문항 수도 실제
  최소·최대 안으로 맞추며, 4문항 미만이면 미응시·진행 중 시험의 중복 단어 때문임을
  구체적으로 표시한다.
- 학생 시험 화면은 반복 오답의 `! / !!` 문구와 접근성 연결은 유지하되 단어 뒤의 중복
  붉은 장식 면을 제거했다. 답변 전에는 비어 있는 feedback DOM을 만들지 않는다. 한글
  문제·선택지는 발음 열을 만들지 않고 영어 위치만 기존 안정 열을 유지한다.
- DB migration과 운영 데이터 쓰기는 없다. ESLint·TypeScript·전체 98파일 557테스트가
  통과했고, 격리 환경의 Google Fonts 다운로드 실패 뒤 네트워크 허용 조건에서 Production
  build가 통과했다.

### 2026-08-09 역방향 범위·직전 범위·주간 일괄 배정 Preview 반영

- 작업지시 `APP-20260809-11`에 따라 `redesign` Preview와 staging에만 반영했다.
  `main`, Vercel Production, 운영 Supabase ref `xdxhswjgksukjmpbzqgz`는 변경하지 않았다.
- 개별 단어 시험에서 별도 역순 체크박스 없이 시작 `DAY 60`, 끝 `DAY 54`처럼 직접
  선택한다. 선택한 `primaryUnitIds` 순서를 일반·혼합·배정 수정·일괄 배정의 UI,
  서비스와 DB가 같은 방향으로 보존한다. 중복·누락·지그재그 범위는 거부한다.
- 일괄 배정의 `이전 배정과 같은 DAY 수`는 학생별 최근 유효 범위의 길이와 방향을
  이어받는다. 예를 들어 직전 `DAY 60→54` 다음은 `DAY 53→47`이다. 미응시·진행 중·
  재학습 대상은 기존 범위를 먼저 유지하고 활성 시험 중복 차단도 그대로 적용한다.
- `다음 7 DAY · 시험 1개`는 추천 시작점부터 현재 진행 방향으로 최대 7 DAY를 한
  시험에 묶는다. 단어장 경계에서는 남은 DAY까지만 줄이고 미리보기에 실제 범위와
  문항 수를 표시한다. 7개의 예약 시험을 자동 생성하는 기능은 이번 범위가 아니다.
- staging에 `preserve_reverse_assignment_ranges`를 remote version `20260809093348`로
  적용했다. 앱 migration은
  `supabase/migrations/20260809120000_preserve_reverse_assignment_ranges.sql`, 되돌리기는
  `supabase/rollback/20260809120000_preserve_reverse_assignment_ranges.sql`이다.
  SHA-256은 각각
  `048D4185CA5F00516DAB4011A5A859FDD594B090079D4C44FB3998DFD9F40D10`,
  `7DF4BB13F9AA28964DF47F0554220705D598DB5179AE269F6008246507CDB9A5`다.
- 기능 커밋 `c959f33f1135668afa2d6ab30a67346b5f2fdb56`을 `redesign`에만 push했고,
  해당 커밋의 GitHub Vercel deployment check가 `success`임을 확인했다. 고정 Preview는
  `https://english-academy-student-a-git-863164-thetilldawn-3859s-projects.vercel.app`이며
  Vercel Authentication 보호를 유지한다.
- ESLint, TypeScript, 전체 101파일·573테스트, 네트워크 허용 Production build와 PGlite
  전체 migration 통합검사 16개를 통과했다. staging에서 새 public RPC v9/v4/v4와
  `authenticated` 허용·`anon` 거부, 오름차순 `+1`·내림차순 `-1` 방향 판정을 확인했다.

### 2026-08-09 학생 시험 타이머·반응형 Preview 회귀 수정

- 작업지시 `APP-20260809-12`에서 실기기 사진의 우측 상단 빈 빨간 사각형을 수정했다.
  원인은 30초 이하 경고 타이머와 진행 막대를 같은 CSS 규칙으로 묶어 글자색과 배경색을
  모두 `--fail`로 지정한 후반 cascade였다. 타이머는 `--no-bg` 배경과 `--fail` 글자,
  진행 막대만 `--fail` 배경을 사용하도록 선택자를 분리했다.
- 전체 화면인 `.quiz-shell`까지 620px로 제한하던 후반 선택자에서 shell을 제거했다.
  이제 shell은 기존 전체 viewport grid를 유지하고 최대 620px인 `.quiz-card`만 중앙에
  놓인다. 긴 시험명에서도 타이머가 줄어들거나 줄바꿈되지 않도록 고정 flex와 최소 숫자
  폭을 적용했다.
- 문제당 시간 방식에서 답 피드백 500ms 뒤 다음 문제로 전환할 때, 서버가 반환한 새
  `questionDeadlineAt`과 `remaining`을 같은 React 전환에서 갱신한다. 직전 문제의 0초가
  다음 문제에 남아 즉시 시간 초과 요청을 보내는 경로를 차단했다.
- DB, 시험 snapshot, 학생 데이터, `main`, Production은 변경하지 않는다. ESLint,
  TypeScript, 전체 101파일·574테스트와 Production 방식 build를 통과했다. 최종 기능
  커밋과 Vercel Preview 상태는 루트 작업지시 `APP-20260809-12`를 기준으로 한다.

### 2026-08-09 APP-11·12 Production 반영

- 사용자 `메인에 적용` 승인에 따라 작업지시 `DEPLOY-20260809-02`를 완료했다.
  dirty 상태인 별도 로컬 main 작업폴더는 checkout·merge·reset·stash하지 않고 clean
  `redesign` worktree에서 원격 `main`만 fast-forward했다.
- 앱보다 먼저 운영 Supabase `xdxhswjgksukjmpbzqgz`에
  `20260809121009_preserve_reverse_assignment_ranges`를 적용했다. 새 public/private
  mixed v9, bulk v4, replacement v4는 `authenticated`·`service_role`만 실행 가능하고
  `anon`은 실행할 수 없다. 내부 방향 helper는 외부 세 역할 모두 실행할 수 없으며
  실제 연속 DAY 표본에서 오름차순 `+1`, 내림차순 `-1`을 확인했다.
- migration 전후 학생 4명, 응시 21건, 오답 대기 68건은 동일하다. 배정은 점검 사이
  25건에서 26건으로 1건 늘었으나 migration에는 배정 생성 DML이 없고 같은 시각 운영
  API 사용이 기록되어 있어 배포 중 실제 관리자 사용으로 분리 기록한다. 운영 검증용
  학생·시험·오답 데이터는 만들거나 변경하지 않았다.
- 기능 커밋 `648efb4a0890c31c30b104a54d6575d1a8bec07d`에서 `origin/main`과
  `origin/redesign`이 같아졌고 GitHub Vercel Production check는 `success`다. 운영 URL
  `https://english-academy-student-app.vercel.app`은 HTTP 200, Server `Vercel`, 확인 당시
  X-Vercel-Id는 `icn1::icn1::zncfc-1786277606515-ed5c52139470`이다.
- 배포 직후 최근 15분 Supabase API 표본 100건은 전부 200이고 PostgreSQL 로그 40건은
  ERROR·FATAL·PANIC 0건이다. Vercel runtime errors와 error/fatal logs는 연결 계정의
  조회 권한 403으로 확인하지 못했으므로 0건이라고 단정하지 않는다.
- security advisor는 기존 INFO 33·WARN 5, performance advisor는 INFO 88·WARN/ERROR
  0이다. 새 public writer는 SECURITY INVOKER라 신규 advisor 경고를 만들지 않았다.
- 배포 창의 즉시 rollback을 위해 구 public/private mixed v8, bulk v3, replacement v3의
  `authenticated`·`service_role` 권한은 당장 회수하지 않았다. 새 private 함수가 구
  private 함수 구현을 내부 호출하므로 함수 자체는 삭제하면 안 된다. 추후 권한 회수는
  별도 migration으로 하고, 구 앱 rollback 시에는 구 public writer 권한을 먼저 복원한다.
