# 학생 앱 인계서

- 갱신일: 2026-07-29
- 현재 단계: 로컬 MVP·서울 Supabase·비공개 GitHub 첫 푸시 완료
- Git 루트: 이 `학생 앱/` 폴더
- 실제 학생정보: 0건
- Supabase 실제 쓰기: 서울 프로젝트 1개 생성, 마이그레이션 2개 적용
- Supabase 실제 자료행: 0건
- GitHub 외부 쓰기: 비공개 저장소 생성·`main` 첫 푸시 완료
- Vercel 외부 쓰기: 0건

## 외부 연결 상태

- 프로젝트 MCP 설정: `../.codex/config.toml`
- 등록 서버: GitHub, Supabase, Vercel
- 인증: OAuth 완료
- 외부 쓰기 도구: 실행 시 승인
- 설정 문법 검증: 통과
- 앱 커넥터 실호출: GitHub 사용자, Supabase 프로젝트, Vercel 팀 조회 통과
- 도구 미발견 원인: 인증 문제가 아니라 `codex_apps` 지연 로딩 검색 문제
- 상세: [외부 연결 검증](docs/외부_연결_검증.md)
- Supabase 원격 적용: [Supabase 원격 적용 검증](docs/Supabase_원격적용_검증.md)

## 구현 결과

- 관리자 직접 생성 계정 로그인
- 학생 생성과 고정 접속코드 발급·재표시·교체·차단
- 학생 코드 인증 뒤 180일 HttpOnly 쿠키
- 코드 교체·차단 시 기존 학생 세션 전체 무효화
- 학생별 시험범위·문항·시간·통과점수·재시험 설정
- 영어→한글 / 한글→영어 4지선다 50:50
- 즉시 피드백과 최초 오답 1회 재풀이
- 최초 점수·재풀이 결과·미해결 단어·교사용 문항 상세
- 제한시간 서버 확정과 같은 배정의 동시 진행시험 차단

## 능률보카 2025 기준

- 사용자 지정 최신 위치: Google Drive `다른 컴퓨터/내 노트북`
- 구조화 파일: `능률VOCA 어원편 고등 (2025개정)_어휘리스트.xlsx`
- 노트북본과 로컬본 SHA-256 일치:
  `9FB5B8307C5E695853E2E0E49DE07DD9CD20D29BC59C749DED4D2D07B4C92133`
- 기준 시트: `어휘리스트_의미축소`
- 3,001행, 핵심 빈값 0, 고유 표제어 2,820개
- 가져오기 드라이런 통과, DB 쓰기 0
- 상세: [능률보카 2025 최신본 검수](docs/능률보카_2025_검수.md)

## 핵심 진입점

1. [README](README.md)
2. [설정·배포](docs/설정_및_배포.md)
3. [보안·시험 규칙](docs/보안_및_시험규칙.md)
4. [MVP 검증 결과](docs/검증결과.md)
5. [DB 마이그레이션](supabase/migrations/20260728134932_initial_student_app_mvp.sql)
6. [어휘 가져오기](scripts/import-vocabulary.ts)

## 검증

- `npm run lint`: 통과
- `npm run typecheck`: 통과
- `npm run test`: 5개 파일·20개 검사 통과
- `npm run build`: 통과
- DB 적용 전 독립 검토: 배정 학생 INSERT 문법 오류 수정, FK 인덱스 10개 보완
- 배포 의존성 보안 감사: 알려진 취약점 0건
- 공개·학생 코드·관리자 로그인 모바일 화면: 모두 200, 가로 넘침 0, 오류 0
- 실제 Supabase 적용 검증: 테이블 13개·RLS 13개·정책 13개·보안 경고 0개

## 외부 연결이 필요한 다음 작업

1. Hosted Auth `Allow new users to sign up` 끄기 — 현재 켜짐 확인
2. 준비된 `.env.local`의 빈 `SUPABASE_SECRET_KEY`에 사용자 직접 입력
3. 관리자 Auth 계정과 `admin_profiles` 등록
4. 능률보카 검수본을 `--apply --mark-ready`로 가져오기
5. 가짜 학생으로 코드 로그인·시험·차단 E2E 확인
6. 필요하면 GitHub App에 새 저장소 접근 허용
7. Vercel 프로젝트 연결·환경변수 등록·배포

서울 Supabase 스키마는 준비됐지만 데이터는 0건이다. GitHub 비공개
저장소의 `main` 첫 푸시는 완료됐다. Vercel 프로젝트는 0개다.
관리자 계정은 사용자가 직접 만든다.
Secret key는 채팅·Git·보고서에 기록하지 않는다.

루트와 `영어/`에는 `.git`이 없고, 앱의 `.vercel/`은 Git에서 제외한다.
`.env.local`에는 공개 연결값과 앱 비밀값 4개가 준비됐으며
`SUPABASE_SECRET_KEY`만 비어 있다.

## 보류

- 필수형용사 500: 구조화 검수본 미제공
- 모의고사 단어: 추후 추출
- 영영풀이·예문·워크북형 문제: 단어시험 MVP 이후
