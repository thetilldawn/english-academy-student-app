# 단어장 찾기 개편 — APP-20260905-02

## 동작과 경계

- 기존 단일·일괄 배정창의 긴 단어장 선택을 검색·학교급/자료 종류 버튼·학년 상세 조건·
  최근 선택 목록으로 전환한다. 같은 Dialog 안에서 화면을 바꾸며 기존 폼과 controller를 유지한다.
- 검색·필터·뒤로·취소·Escape·같은 항목 재선택은 기존 배정 action을 호출하지 않는다.
  다른 항목을 확정한 경우만 기존 changeDataset을 한 번 호출한다. 검색은 폼 밖이다.
- 오답 시험은 기존 controller.datasetOptions(미배정 오답이 있는 후보)와 개수를 사용한다.
- 최근 선택은 이 브라우저의 단어장 ID 최대6개만 버전1 형식으로 저장한다. 배정 완료 이력이나
  학생 정보가 아니다. 손상·용량 초과·차단된 저장소에서도 선택이 동작한다.
- 학교 연결 정보는 없으므로 제목으로 학교를 임의 분류하지 않는다. 학교명은 제목 검색으로
  찾을 수 있다. catalog 누락의 고등 기본값은 미분류로 표시하며 전체 검색에서 제외하지 않는다.
- 실제 학년 g10/g11/g12와 이전 H1/H2/H3 표기는 고1/고2/고3로 정규화해 같은 필터를 사용한다.

## 변경 영향

- 경로: 기존 배정창. 새 운영 URL 없음.
- UI: assignment-dataset-trigger/picker와 같은 기능의 CSS; 기존 단일·일괄·오답 연결.
- Client: client/controllers/use-assignment-dataset-picker의 초점·창 수명·최근 선택.
- domain: 최근 ID 검증·중복 제거·현재 후보와 교차 확인. 외부 계층 의존 없음.
- presentation: 현재 catalog의 순수 검색·분류·정렬·학년 표시. 범위 배정 계산과 분리.
- application/계약: 기존 확정 콜백과 저장 계약 그대로.
- server/DB: 변경 없음. 실제 학생·시험·자료·권한 변경 없음.
- 캐시·로딩: 준비된 목록에서 검색. 다른 자료 확정 후만 기존 단위 지연 조회.
  창 수명 재사용, 이전 응답 취소, private no-store 정책 그대로.

## 로컬 검증 (2026-09-05)

- 관련37검사, 전체371파일 1,986검사 통과. 타입·기능 지도·UI 구조·요청 캐시·Next.js16.2.12 build 통과.
- lint는 이전 작업의 git-ignored .codex-tmp 임시 화면을 제외한 실제 배포 소스 전체를 검사했다.
  실행: `npm run lint -- --ignore-pattern '.codex-tmp/**'`.
- 로컬 가짜19권 + 실제 VocabAssignmentPlanner/순수 계산/controller/UI로 검증.
  임시 화면이 요청을 가로채므로 외부 DB 접속·배정 저장 없음. 배정 저장 버튼은 누르지 않았다.
- 검색·학교급/자료 종류/학년 복합 필터 중 추가 조회0. 확정 시 선택한 단위1회·최근 시험1회.
- 실제 입력 25단위 전체·회차당5단위·시험일 끔·95점: 검색 Enter로 배정되지 않음,
  Escape와 같은 단어장 재선택 뒤 값·초점·조회 횟수 유지.
- 오답은19권 중 후보2권과 미배정 오답7개/3개만 표시. 새로고침 뒤 최근 선택 복원.
- 390×844에서 긴 제목 두 행의 수평/수직 넘침0, 페이지 수평 넘침0, 스크롤 뒤 제목·취소 유지.
  1280×900에서 복합 필터 결과1권·추가 조회0·페이지 수평 넘침0.
- 로컬 임시 서버 시작/코드 이동 중 HMR 오류는 수정 후 새로고침했다. 최종 조작 구간
  2026-09-05T09:52:00Z 이후 브라우저 오류·경고0. 초기 실패를 운영 오류로 혼동하지 않는다.
- 독립 읽기 리뷰의 실제 학년 코드 노출 P2를 수정했고 재검토에서 추가 P1/P2 없음.

## 배포

- 기준 운영판74e1a59에서 codex/wordbook-picker-20260905 기능 브랜치.
- 이번 승인 범위는 로컬 검사·커밋·Preview. main·Production은 변경하지 않는다.
- 실제 Preview 커밋·주소·검증 결과는 앱 인계서에 누적한다.
