# 학생·배정 개선 운영 반영

## 2026-09-08 운영 준비 실행

- APP03 R14 `5bfba77`: 중복 취소 제거. 로컬3020검사·34페이지 빌드·390/1240 실제화면·완료관문 통과.
- Production에 `NEIS_API_KEY` Secret만 추가. 이전 Preview 브랜치 Secret은 유지, DB·쿠키·로그인 설정은 변경하지 않았다.
- 지정 학교표시 조회함수 migration1건 성공. 적용후 SHA `b95115411d010221f9090c7a118f4a02ad9f336889b44a73aa2d556cff356aec`은 검증된 staging과 일치. 권한·소유자·보안방식·검색경로 보존.
- 직후 보호18영역 행수/해시 전부 동일. 보안3/성능2 기존 안내 그대로, 신규0. 실제학생 쓰기0.
- 배포/운영 화면/15분후 확인은 아직 수행중이며 아래 완료조건을 생략하지 않는다.

사용자 2026-09-08 `확인 취소` 제거 후 `바로 프로덕션으로 넘겨` 요청에 따른 배포다. [현재 기록](../../architecture/work-records/records/DEPLOY-20260908-03.json) R1~R4/D1~D3/T1~T5, [기존 Preview 근거](../verification/DEPLOY-20260908-02.md)와 APP03-R14 후속 검사를 연결한다.

## 대상과 순서

- R1: 현재 Preview의 학생/배정 개선과 중복 버튼 보완만 기존 Preview 브랜치와 main에 순차 게시한다. 운영은 환경을 구분한 새 빌드이며 Preview 산출물을 그대로 승격하지 않는다.
- R2: 제공 학교키를 기존 프로젝트 Production Secret으로만 추가한다. 현재 Preview 브랜치 설정은 보존하고 DB/세션/쿠키 비밀값을 복사하거나 바꾸지 않는다. 키는 Git/브라우저 자료/문서에 기록하지 않는다.
- R3: `20260907235924_admin_history_school_labels.sql` 한 개만 운영에 적용한다. 원본 함수·권한·해시·18영역 집계는 적용 전에 보관하고 직후 대조한다. 시험 SQL201~204·과거 migration·전체 db push·자료 태그 수정은 금지한다.
- R4: 기존 테스트학생으로 학교검색/정상0건·정보저장/목록갱신/원복·초안·확인창과 학생 화면을 확인한다. 새로운 배정·응시·답 제출·점수·실제학생 정보는 쓰지 않는다.

## 관문과 보호

T1 현재 로컬검사/코드지문 → T2 운영 함수 최소 변경 → T3 Preview/운영 환경·빌드 → T4 실제 운영 화면 → T5 동일 커밋·보호값/배포15분후 확인이다. 구현과 배포 커밋은 구분한다. 독립 검토9파일59검사로 환경 격리와 조회 권한을 재확인했다.

운영 착수 main `ee7be43`, 기존 프리뷰 `b5feaf4`, 로컬 기록까지 `17d3648`. 운영 PG17.6.1.147 정상, 진행 상태2건은 9월5일/8월15일 시작한 기존 기록이다. 타이머/채점/응시 함수는 바꾸지 않는다. 운영 함수의 적용전 SHA `79994ae652285d20cb9a42afb8ed475752dffe37bf748f25e4589c23084f42c6`은 프리뷰의 적용전 함수와 같다. 보안3/성능2 기존 안내와 신규 여부를 구분한다.

학교 조회는 실제 검색0건/실패/미입력을 구분하고 직접 입력을 유지한다. 조회·저장 실패 안내와 복구는 기존 공용 상태/UI를 유지한다. 운영 `/api/preview-identity`의404는 정상이며 JSON 성공을 요구하지 않는다. 모든 실패·미실행·전체 서비스 지표 미측정은 통과와 구분한다.

오류가 나면 먼저 앱만 이전 운영 `ee7be43`로 복구 가능한지 판단한다. 조회2필드 추가는 구버전과 호환된다. DB 정의 복구가 필요할 때만 적용직전 원본 함수로 선택 복구한다. 실제학생 활동·정상감사/버전을 삭제하거나 통째 되돌리지 않는다.

공식 기준: [환경별 변수](https://vercel.com/docs/environment-variables), [Next.js 배포](https://nextjs.org/docs/app/getting-started/deploying), [Supabase 변경 안내](https://supabase.com/changelog). 새 의존성/요금제/서비스를 추가하지 않는다.
