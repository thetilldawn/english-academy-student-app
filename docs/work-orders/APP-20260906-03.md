# APP-20260906-03 일정 부품의 표시·계산·입력 분리

- 입력: APP02의 닫기 검사 이후 일정 분리로 이어간다는 보고에 대한 사용자 ㄱㄱ. 기존 실행계획 1-B이며 로컬 전용이다.
- 2026-09-06 05:46 KST 추가 지시: 6시간 승인 순서대로 연속 진행한다. 11:46 KST 이후 새 구현을 시작하지 않고 안전하게 정리한다. 실행이 끝나면 이 작업의 제한된 자동 이어가기가 재개하며 기존 권한 범위는 넓어지지 않는다.
- [기록 v1](../../architecture/work-records/records/APP-20260906-03.json), [개발 기준](../개발_지시서와_검증_기록.md), [실행계획](../APP_20260905_캐시와_컴포넌트_분리_실행계획.md), [선행 검증](../verification/APP-20260906-02.md)을 따른다.
- R1: 두 일정 UI에서 전체 controller를 제거하고 필요한 값·오류·동작만 전달한다. 완료: 실제 단일/일괄 사용처 연결, 독립 렌더와 의존 경계 확인. T1/T2/T3/T4/T6.
- R2: 숫자·표시·입력 수명·저장 요청을 유지한다. 완료: 날짜 없는 5회 분할, 반복 승인/초과/축소, 요일 우선, 계산용 날짜 숨김, 회차 수정, 양식 성공 비움/실패 유지/저장 잠금 회귀. T1/T2/T3/T5/T6.
- R3: 로컬 기본 검사 뒤 실제 화면만 확인한다. 완료: 전체 검사·타입/lint/구조/빌드·실제 화면·현재 근거 기록. T3/T4/T5/T6.
- D1/D2: 기존 순서와 로컬 범위는 합의대로 유지한다. 미결정 0건. DB/학생/원자료/비용/의존성/커밋/업로드/배포 변경 금지.
- D3: 요약 수치 → 기존 domain/vocab-schedule.ts. 단어장/범위 이름과 회차 표시 행 → presentation/vocab-schedule-view.ts. 시간 양식 이름·비움·알림 → 전용 client 훅. 실제 조립 계층은 vocab-range-assignment-sections.tsx. 두 일정 UI는 명시적 입력만 받는다.
- ConditionalReveal은 자식을 계속 마운트하고 숨김/inert만 전환한다. 이름 입력 훅을 조립 계층에 두어도 일정 숨김/복귀와 시험 종류 전환의 기존 수명을 유지한다. 업무 일정 제어기를 훅에 다시 넣지 않는다.
- D4: 남은 Preview 제한 안내와 시험 유형 정책은 후속 단계다. 이번에는 기존 제한·문구를 유지하며 미검토 문항을 활성화하지 않는다.
- T1: 순수 요약과 표시 변환의 경계값·선택 순서·빈 상태·미리보기 우선순위·원본 불변 검사.
- T2: 좁은 props의 독립 렌더·날짜/요일/시간 변경 콜백·관련 오류 위치, 양식 성공/실패/잠금과 기존 숨김 수명 검사.
- T3/T4: 전체 기존 배정/수정/오답·API 요청·권한/정답보호·캐시 회귀, 타입/lint/구조·변경 연결·요청 캐시·새 격리 빌드.
- T5: 독립 읽기 전용 설계/코드 리뷰. T6: 실제 로컬 단일/일괄의 일정 표시·선택·숨김/복귀 확인. 수치 계산을 브라우저에서 반복 추측하지 않는다.
- 입력 오류는 기존 FieldError와 필드 근처 안내를 유지한다. 조회 실패/정상0건/인증/404는 기존 경계 그대로이며 변경하지 않는다. 양식 저장 실패는 이름을 유지하고 기존 안전한 안내를 표시한다.
- 생성/배정/응시/조회/검색/수정/삭제/기록: 새 흐름 없음. 기존 배정 일정 표시와 시간 양식 저장만 연결하고 응시/삭제는 T3 회귀로 보존한다. 새 공유 캐시·초기 로딩·스트리밍·모달·스타일 없음.
- 복구: 이번 파일 변경만 역패치한다. 이전 완료 검증은 당시 지문으로 보존하고 새 코드에 무확인 재사용하지 않는다. 실제 DB/비용/모든 화면 폭은 측정하지 않은 한 미검증이다.

## 실행 근거

- [검증 기록](../verification/APP-20260906-03.md): 순수 계산/표시 변환/입력 훅과 좁은 두 UI의 실제 연결을 확인했다.
- 전체 381파일/2,219개, 리뷰 후 관련18개, 타입/lint/구조/캐시·새 격리 빌드·실제 단일/일괄 입력 수명·완료 관문 통과.
- 로컬 검사 서버·포트·탭 정리. 일정 업무 규칙·유형 제한·캐시 정책·실제 DB·업로드·배포 변경 없음.
