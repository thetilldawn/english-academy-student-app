# 관리자 삭제·취소 기능 검증 및 배포 기록

- 기록일: 2026-07-31
- 작업 브랜치: `codex/admin-deletion-controls`
- 상태: 운영 DB 적용 완료, 이 문서를 포함한 `main`이 운영 앱 기준

## 2026-07-31 DAY 배정·오답 대기 추가 수정

- 운영 로그의 실제 실패 원인은 문항 수가 아니라
  `choice_not_eligible_for_direction`이었다.
- 일반 시험의 정답은 선택 DAY에서 고르면서 오답 선택지는 단어장
  전체에서 고르던 불일치를 수정했다.
- 일반 시험의 미리보기와 실제 생성 모두 정답·선택지 후보를 선택
  DAY 범위로 통일했다. DAY 09는 33개 범위에서 4~33문항을 배정할
  수 있다.
- 과거 교사가 `다음 시험에 추가`했던 단어가 미응시 뒤에도
  미해결이거나 이후 다시 틀린 경우, 최신 이력 한 건을 `pending`으로
  복원한다.
- 한 번도 교사가 선택하지 않은 오답은 자동 대기시키지 않는다.
- 시험 관리에 `현재 미해결 / 다음 시험 대기 / 추가 가능` 수를
  분리해 표시한다.
- 범위·출제방식·문항 수·마감시각을 바꾸면 직전 저장 오류를 지운다.
- 추가 코드 커밋:
  `15a3f8b0fca518abf73468a68c61e83ec0182ecb`
- 추가 마이그레이션:
  `supabase/migrations/20260731020000_reopen_selected_unresolved_review_queue.sql`
  - SHA-256:
    `3E76340C776791F1C3DA8C2379E9ABCE52EC50A54FA640CC48F5EA779FE26BCB`
- 사용자 요청에 따라 전체 회귀검사와 운영 가짜 데이터 검증은
  이번 배치에서 생략했다. 변경 정적 검토와 독립 잠금·중복 검토만
  수행했다.

## 4지선다 문항 전환 추가 변경

- 답을 선택한 뒤 정답·오답 피드백을 0.5초 표시하고 다음 문제로 이동한다.
- 문제별 제한시간은 피드백 0.5초가 끝난 뒤 다음 문제부터 계산한다.
- 전체 시험시간 모드는 기존처럼 시험 전체 시간이 계속 흐른다.
- 사용자 요청에 따라 이 추가 변경은 별도 테스트·화면 검증을 생략했다.
- 추가 마이그레이션:
  `supabase/migrations/20260731013000_delay_per_question_timer_after_feedback.sql`

## 구현 결과

- 학생 삭제
  - 학생 코드 제거, 활성 세션 폐기, 일반 목록·새 배정 대상 제외
  - 미응시 배정은 취소하고 오답 대기는 보존
  - 진행 중 응시는 `expired/completed`로 종결해 고아 상태를 남기지 않음
  - 성적·문항·오답 원본은 보존하고 과거 내역 이름은 `삭제됨`으로 표시
- 시험 관리
  - 시험별 관리 목록에서 `시험 전체 삭제`
  - 학생별 이력에서 미응시 `배정 취소`
  - Overview·학생 관리·시험 관리 공통 이력에서 전체 시험 삭제와 개별 내역 삭제
  - 진행 중 응시가 있는 시험은 전체 삭제 거부
- 내역 삭제
  - 원본 시험·성적·오답은 보존
  - 관리자 이력에서 선택한 수신자 또는 응시 행만 숨김
  - 내역을 숨기는 순간 응시가 시작된 경우 `409`로 거부하고 새로고침 안내
- 반응형·색상
  - 학생 화면 `완료`, `새 시험`, `미응시 마감`, 마감 카운트다운 줄바꿈 방지
  - 시험 관리의 배정 버튼 글자를 흰색으로 유지
  - 학생 관리 상태 배지 색상을 후순위 선택자가 덮지 않도록 수정
- 장기 조회
  - 배정·응시·숨김 이력을 각각 1,000건 단위로 전체 페이지 조회

## DB 안전장치

- 학생·시험은 soft delete만 허용하고 물리 `DELETE`는 트리거로 차단
- 삭제된 학생·시험의 수정, 새 배정 연결, 새 응시 생성 차단
- 학생 → 시험 순서의 부모 행 잠금으로 삭제·응시 시작 경합 방어
- 취소된 수신자 행도 이력 보호 트리거로 물리 삭제 차단
- 공개 RPC는 활성 관리자만 실행하고 명시적 권한과 RLS 적용
- 활동이 한 건이라도 생기면 롤백 스크립트가 중단되도록 보호

## 산출물과 해시

- 마이그레이션:
  `supabase/migrations/20260731010000_admin_deletion_controls.sql`
  - SHA-256:
    `F494E6F9B8F7166DA6BDBB02890D1DC102C0FBC839FFE6905622D9F5AC028B04`
- 비상 롤백:
  `supabase/rollback/20260731010000_admin_deletion_controls.sql`
  - SHA-256:
    `0B6B1B8174429A272444A65F75909810E50A143265C4B1D82582C873948BEC4F`

## 검증

- ESLint 통과
- TypeScript 통과
- 전체 Vitest 58파일·337건 통과
- 최종 DB 통합 대상 18건 통과
  - review 단계 진행 중 학생 삭제 종결
  - initial 단계 진행 중 학생 삭제 점수 산출·종결
  - 다중 학생 배정에서 다른 학생 상태 보존
  - 미응시 배정 취소, 세션 폐기, 코드 제거
  - 진행 중 시험 전체 삭제 거부
  - 학생·시험 물리 삭제 차단
  - 내역 숨김 멱등성·응시 시작 경합 거부
  - 무활동 비상 롤백 실행
- Next.js 16.2.12 webpack 운영 빌드 통과
- 브라우저 실측
  - 768px, 1024px, 1199px
  - 상태 배지 `white-space: nowrap`, `flex-shrink: 0`
  - 마감 카운트다운 `white-space: nowrap`, `flex-shrink: 0`
  - 카드와 문서 가로 넘침 0

## 운영 적용 전 스냅샷

- 학생 1
- 시험 6
- 학생별 배정 6
- 응시 5
- 문항 122
- 오답 이벤트 69
- 현재 오답 상태 70
- 활성 학생 세션 3
- 진행 중 응시 1
- 신규 삭제 열·RPC·숨김 테이블 0

## 운영 적용 결과

- 원격 마이그레이션:
  - `20260731044902_admin_deletion_controls`
  - `20260731044917_delay_per_question_timer_after_feedback`
  - `20260731044930_reopen_selected_unresolved_review_queue`
- 적용 전후 보존 수치:
  - 학생 1, 시험 6, 학생별 배정 6
  - 응시 5, 문항 122, 오답 이벤트 69, 현재 오답 상태 70
  - 활성 학생 세션 3, 진행 중 응시 1
- 신규 삭제 열 5개와 숨김 이력 테이블 생성을 확인했다.
- 서버의 문제별 다음 문항 시작시각에 500ms 지연이 반영됐다.
- 현재 미해결 오답은 39개·2회 이상 7개다.
- 과거 교사가 선택한 현재 미해결 오답 7개가 다시 `pending`으로
  복원됐으며, 해결된 단어가 대기에 섞인 건은 0개다.
- 전체 회귀검사와 운영 가짜 데이터 생성은 사용자 요청에 따라 생략했다.
  운영 DB 구조·수치와 운영 배포 상태만 확인했다.

## 다음 시작점

1. 실제 학생 계정에서 DAY 09를 4~33문항으로 한 번 배정한다.
2. `틀렸던 단어 추가`에 현재 미해결 39개·다음 시험 대기 7개가
   구분되어 표시되는지 확인한다.
3. 필요할 때만 삭제·취소·내역 숨김 기능을 실제 운영 데이터에 사용한다.

## 금지사항

- 기존 마이그레이션 전체 push 또는 이력 재작성 금지
- 운영 DB 승인 전에 앱 코드만 단독 배포 금지
- 학생·시험·성적·오답의 물리 삭제 금지
- 원본 교재·Obsidian·단어 사전 작업 변경 금지
