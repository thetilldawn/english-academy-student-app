# R7 화면 소유권과 점진 표시

- 상태: 로컬 구현·자동검증 완료, 최종 Preview 검증 대기
- 최초 구현: `39c99a5`
- 안전·경계 보완: `a789d7d`
- DB migration: 없음

## 구현 결과

- `learning-points`, `results`, `assignment-queue`의 다른 기능 소비는 각 기능의 `public-*` 진입점으로 고정했다.
- 결과 화면의 음원 재생은 시험 실행기 내부 코드에서 분리해 공용 음원 수명 관리와 공용 버튼을 사용한다.
- 관리자·학생 보호 화면과 주요 비동기 화면은 빈 fallback 대신 `RouteLoadingState` 또는 화면별 skeleton을 표시한다.
- 학생 추가와 학생 목록은 서로 독립된 `Suspense` 경계와 패널별 실패 화면을 사용한다. Next.js 이동·인증 오류는 `unstable_rethrow`로 다시 전달한다.
- 배정의 범위·조건·일정·요약·필터 하위 UI는 전체 controller 대신 필요한 값과 동작만 받는다.
- 기능 지도에서 `removeIn`이 `R7`으로 시작하는 임시 기능 간 연결은 0건이며, `Suspense fallback={null}`도 0건이다.

## 자동검증

- 전체 테스트: 341파일·1,788건 통과
- 구조 검사: 4파일·65건 통과
- 기능 지도: 10기능·15 page·47 Route Handler·12 상세 흐름
- lint·typecheck·React RSC 요청 캐시 검사·Next.js 16.2.12 production build 통과
- 구조 보호선 추가:
  - R7 임시 기능 간 연결 재등장 금지
  - 배정 하위 표시 UI의 전체 controller 수신 금지
  - 학생 두 패널의 일반 오류 격리와 Next.js 이동 오류 재전달
  - 공용 음원 재생기의 재사용·재시작·정리

## Preview에서 다시 확인할 항목

`a789d7d`는 원격 push가 승인되기 전이므로 아래 항목은 아직 최종 완료로 올리지 않는다.

- 360·768·1440px, 밝은·어두운 화면의 겹침·가로 넘침·큰 들썩임
- 키보드 초점·탭 순서·읽기 이름과 모션 줄이기
- 학생 추가 패널 또는 목록 패널 하나가 실패해도 다른 패널이 유지되는지
- 관리자 개요·내역·학생·배정과 학생 메인·시험·결과의 최신 배포 동작
- R6 대비 행 수·버퍼·응답 크기·DB 시간·패널 표시 시간

## 복구

- R7 최초 상태로만 되돌릴 때: `39c99a5`
- R7 이전 상태로 되돌릴 때: `699ddcb`
- DB 변경이 없으므로 rollback SQL은 없다.

