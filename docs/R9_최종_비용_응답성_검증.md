# R9 최종 비용·응답성 검증

- 상태: 진행 중 — 로컬 자동검증 완료, 최종 Preview 배포·실검증 대기
- 최종 로컬 코드: `a789d7d`
- DB migration: 없음
- Production·실제 학생 변경: 0건

## 자동검증 결과

- lint 통과
- typecheck 통과
- 전체 테스트 341파일·1,788건 통과
- 구조 검사 4파일·65건 통과
- 기능 지도 10기능·15 page·47 Route Handler·12 상세 흐름 통과
- React 실제 RSC 요청 캐시 검사: 같은 요청 중복 제거, 새 요청 재조회 확인
- Next.js 16.2.12 production build 통과
- `git diff --check` 통과

## 롤백 전 canary Preview 관측값

아래 값은 `727d593` Preview의 1회 예열 뒤 7회 측정이며, 최종 롤백 배포 성능으로 간주하지 않는다.

| 화면 | 첫 화면 껍데기 중앙값 | 최댓값 |
|---|---:|---:|
| 관리자 개요 | 210ms | 222ms |
| 학생 관리 | 230ms | 262ms |
| 단어 배정 | 199ms | 205ms |
| 내역 | 213ms | 225ms |
| 개요↔학생 정상 이동 | 184ms | 359ms |

반복 새로고침 부하에서는 `/admin/results`의 실제 `응시 전` 구역이 7회 모두 약 18.4~21.2초 안에 나타나지 않았다. 냉각 뒤에는 표시됐지만 이 표본은 성능 보장으로 쓰지 않고 미해결 제한으로 남긴다.

같은 canary Preview에서 관리자 주요 화면과 배정 일반·오답 탭, 오답 후보 63개, 학생 목록·상세를 360·768·1440px와 밝은·어두운 화면으로 확인했고 확인 구간의 가로 넘침과 브라우저 warning/error는 0건이었다.

## 최종 Preview 관문

현재 원격 push가 보안 승인 단계에서 차단되어 아래 관문은 미완료다.

- `a789d7d`와 정확히 일치하는 Vercel Preview `READY`
- 전용 run ID 가짜 학생의 관리자 흐름과 학생 로그인 연결
- 학생 메인·시험 시작·답 저장·결과·재시험·다음 회차
- 관리자 개요·내역·학생·배정 반영
- 360·768·1440px, 어두운 화면, 키보드·읽기 이름·모션 줄이기
- 브라우저·Vercel·Supabase 오류 확인
- 비대상 가짜 학생의 사전·사후 지문 일치
- 전용 fixture 정리와 인앱 브라우저 탭 0개

## 비대상 지문 기준

최종 Preview 검증 뒤 같은 정렬·직렬화 방식으로 다시 계산한다.

| 영역 | 행 수 | SHA-256 |
|---|---:|---|
| 학생 | 1 | `0aa2a775d1f24a045cab095ca679f647a5b8fa2acd25b8c2c971b37598b55c1a` |
| 배정 | 28 | `4a182fb137dea8d3ff2b18b078346dc0ccff2d2bc7470e3ef3bc334b36241a80` |
| 응시 | 25 | `909d181329cab916d09881076f688c406f38aafc4b9c683fd1fcc17df17d06a7` |
| 포인트 | 1 | `26ef15107052c6774cf3216aa6f44872ffe7fe30bba7f51c2c86df7eb9801ab3` |
| 오답 | 278 | `f335d838ecf15475c8ad7317c7ca527523cc1d6b1fc0780d1af1b079757341ac` |
| 오답 큐 | 13 | `e25c38e0acc21010dfe878cd4ab52b5c88b21bc47c6c2a4fd8a1b86eb86317c0` |
| 비공개 이어지는 배정 | 1 | `44cd179462688c6da8b14b4b3ef82e3645a49c81a9ee91a4b1c01c5ecf880cb6` |

## 알려진 제한

- Vercel Protection 때문에 CLI Playwright 인증 E2E가 앱 로그인까지 도달하지 못했다. 인증 화면은 인앱 브라우저로 확인한다.
- Preview 활성 관리자 1명으로 관리자 2명 교차 검증은 불가능하다.
- 기존 R9 fixture는 접속 코드가 없어 학생 로그인용으로 사용할 수 없다.
- 서버 완료 시간·읽은 버퍼·RSC 전송량을 모두 같은 run에서 확보하기 전에는 성능 개선 완료로 표현하지 않는다.

## 복구

- R8 롤백만 되돌릴 기준: `727d593`이지만 안전상 재활성화하지 않는다.
- R7~R9 전체 이전 기준: `699ddcb`.
- DB 변경이 없으므로 rollback SQL은 없다.

