# APP-20260909-01 검증 기록

## 결과

로컬 구현·검증 완료. 운영 반영은 [별도 배포 기록](DEPLOY-20260909-01.md)에서 판정한다.

- T1: 공통 최종 선택기30검사 통과. 정확 단어·품사·변이·원음에 연결된 복원값만 표시·강세를 바꾼다.
- T2: 전체 DB 구조를 실제로 올린21검사 통과. 출처·행 해시·품사·중복·권한 차단 유지.
- T3: 연결 및 소비자12검사 통과. 과거 단어 보기·현재 응시·영어 선택지·결과가 같은 선택기 사용. 잘못된 예약 출처4종의 정확/일반 조회 우회 차단.
- T4: 전체3039통과·실패0·기존 선택1미실행. 타입·lint·기능지도·UI구조·설계/변경 연결32건·요청 캐시2회·격리 Turbopack 빌드34페이지 통과.
- T5: 읽기 전용 독립 검토의 일반 조회 우회1건을 수정하고 재현 차단 확인. SQL의 기존 출처·행·품사·해시·음원·서비스 전용 권한 조건 보존. 학생/답안/점수 쓰기 없음.
- T6: 자료 원본→실제 전달 함수500행, 누락/중복/품사/행해시/출처해시/변이/음원 변조 차단6검사와 격리 거래13검사 통과. 독립 검토421파일 해시 일치·500연결 오류0·보호12 유지.

환경: Windows / Node24.19.0 / Next16.2.12 / Vitest4.1.10 / PGlite0.5.4.

## 원인과 복구 경계

원래 PDF에는500개 발음과 굵은 강세가 있었다. 사전411파일은 이전 해시와 동일하다. 원본 오염이 아니라 수집 반환/앱 전달에서 발음이 누락된 결함이다.

기존 사용자 지정1·확정 승인11 보존, 사전74/PDF414 출처488행 복원. 그중163행은 이전 표시·강세와 같아488개를 모두 오발음 교정이라고 표현하지 않는다. 새 발음/MP3 생성과 사전 원본 수정0. 실제 청음 승인이 아닌 원본 표기·강세 복원이다.

묶음 SHA-256: `8f508fe3284d66e1b5da9aae1aa3b1d5a4db7aa30840489b01e0b00dc9174173`.
실제 원문은 앱 Git에 넣지 않고 루트 WORD 지시서 및 자료 v11의 입력·거래·복구 근거로 연결한다.
조회 실패/정확 연결 불일치 때 기존 경로를 보존하며 빈 결과·오류·404·권한 안내는 변경하지 않았다.

## 로컬 실행 경과와 한계

- 초기 SQL 조립 문제가 로컬 검사에서 검출돼 수정했다. 실패 SQL은 원격 실행하지 않았다.
- Node24.11 결과는 일부 검사 미완료로 종료해 완료 근거로 쓰지 않았다. 지원되는24.19에서 전체를 다시 검사한 결과를 사용한다.
- 일반 빌드는 로컬 주소 설정 부재로 중단됐다. 실제 환경 파일을 옮기거나 보안 검사를 풀지 않고, 비밀정보 없는 새 소스 복사본과 가짜 로컬 연결·외부 요청 차단으로 빌드를 완료했다. 기존 환경 파일은 그대로다.
- 기존 선택 미실행1개는 과거 음원 릴리스 전체를 원본으로 다시 가져오는 선택 검사다. 이번488행은 실제 DB 구조 검사와 제한 거래 검사로 별도 확인했다.
- 실제 화면·원격 반영·전후 보호 비교는 DEPLOY 기록에서 확인한다.

374개 구현·검사 입력 지문:
`5573be67741d271ddf942a691977befb08b2c3892f7f3eb771bfc178b92e966a`.
검증 기록 자체는 지문에서 제외한다.
