# DEPLOY-20260910-02 운영 반영 검사

2026-09-10 사용자 운영 반영·기존 테스트학생 검증 승인. 아직 배포 완료가 아니다.

- T1: APP03 전체3246·배정875·최종UI29 통과, 타입/lint/구조·격리빌드 근거 보존. 실행 코드 변경 없음. 현재 확인값 대조 후 기록한다.
- T2: Preview/Production 배포 전.
- T3: 기존 테스트학생 한 건 확인. 운영 관리자 로그인 필요. 비교에만 테스트 학년을 바꾸고 원복한다.
- T4: 배포 전후 보호 자료 수치·해시 비교 예정. 기존 시험·답안·점수 수정 없음.
- T5: 독립 검토에서 추가 차단 결함 없음. tmp/조사 폴더는 전송 제외, 구 관리자 화면은 새로고침. 배포 후 오류/15분 확인은 미실행.

이전 운영72de501a1e86f36fa770294138a4dca55db812b1, dpl_FauoMzG8QQ67gDJk5iAarNcgHfWL. 원격 main/Preview 모두 같은 버전 읽기 확인. 신규 DB migration·의존성·환경 변경 없음.

공식 지침: [Vercel Git 배포](https://vercel.com/docs/git), [Supabase 권한](https://supabase.com/docs/guides/database/postgres/row-level-security), [공식 변경 기록](https://supabase.com/changelog). 변경 기록은 markdown 접근 제한으로 HTML을 확인했으며 기존 API에 새 변경을 도입하지 않는다.

## 사전 배포와 설치 오류 보완

- dc68a5d Preview dpl_963TZqaQsgXYActizi2LtsmxbRrA / GitHub6370885665는 Ready(2026-09-10 11:46:18UTC,68초). 고정 주소의 학생 인증 화면 표시 확인. Preview 식별 JSON 직접 열기는 브라우저가 차단했고 별도 HTTP는 Vercel 로그인으로 이동하여 인증 후 관리자 기능 확인으로 세지 않는다.
- PR1의 첫 검사34473049912는 npm ci에서 기존 개발용 선택 의존성 @emnapi/wasi-threads 2.0.1/2.1.0 불일치로 실패했다. 앱 변경 회귀가 아니며 최초 운영과 package/lock 차이0인 기존 상태다. 검사를 생략하거나 설치 경고를 숨기지 않았다.
- npm11.6.2로 별도 복사본의 잠금 목록을 재계산했다. 실제 변경은 해당 항목 버전·주소·무결성 세 값으로 제한했고 jsdom/vitest의 부수 peer 플래그는 보존했다. 직접 앱 의존성·DB·환경·학생 실행 코드 변경 없음. 수정된 정확한 lock의 npm ci --ignore-scripts --no-audit --no-fund --dry-run은 성공(479항목 설치 계획)이며 실제 Linux 설치 완료와는 구분한다.
- 수정 후 전체3246검사 통과·기존 선택1미실행·실패0. 타입/수정파일lint·기능 지도/UI 구조 통과. 새 격리 사본에서 Next16.2.12 컴파일·타입·34페이지 빌드 통과. 기존 앱 환경파일/빌드 디렉터리 보존. 전체 변경 연결 검사와 Linux CI는 뒤에서 결과를 기록한다.
- 독립 검토: 선택 WASM 개발 의존성 경로의 기존 문제로 확인. 잠금파일은 DEPLOY02 R4/T6/D5의 보완이며 APP03 앱 실행 코드에 추가 수정은 없다.

## CI 실행 환경 보완

- 2f12199의 Preview dpl_9GpDbu5XoKdPVfswDcAxx4Wm2zD8 Ready, 고정 주소 학생 로그인 표시 확인. 운영72de501은 아직 그대로다.
- PR34474528014/job102862012385는 Linux Node24.13/npm11.6.2 실제 설치·lint·타입·구조·안전 검사를 통과했다. 전체검사는 교재 원자료 부재 3파일과 첫 UI 검사 5초 초과로 실패(3222통과·4실패·21미실행). 로컬 전체3246통과 및 전체 설계/변경 연결44건 통과와 구분한다.
- T7: 원자료를 공개 저장소에 추가하지 않았다. 영어 구역 자체가 없으면 원자료 검산8건만 조건부 미실행한다. DB 스키마/권한, 환경 차단 및 잘못된 입력 거부는 자료 없이도 실행한다. 구역이 존재하는 로컬의 일부 자료 누락은 계속 실패한다.
- 같은 4파일 검사: 원자료 있는 로컬36통과·0실패·0미실행, 원자료 없는 격리 사본28통과·0실패·8미실행. UI 첫 single/bulk 전체 조립만15초, assertion/개별 UI 대기/전역 제한시간은 보존. 수정파일lint 통과. 독립 읽기 검토에서 이 분리를 승인했다.
- 운영 활동 구분: 배포 전 실제 학생 응시1건이12:02:56~12:06:54UTC에 정상 완료되고 자동 후속 배정1건이12:06:54UTC에 생성됐다. 이로 인한 답안·점수 등12영역 증가는 배포 영향이 아니며 복원/삭제하지 않는다. 학생·코드·단어 원본 등11영역은 동일했다. 당사자 이름/답안은 기록하지 않는다.
