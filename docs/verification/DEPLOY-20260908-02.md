# 학생·배정 개선 프리뷰 검증

2026-09-08. [배포 지시서](../work-orders/DEPLOY-20260908-02.md) R1~R4/T1~T5의 실제 반영 근거다. APP02/03/04의 과거 로컬 기록은 당시 사실로 남기며, APP02의 남아 있던 실제 저장 검사 T9는 이 통합 기록에서 충족했다.

## 반영 대상

- [검증한 프리뷰](https://english-academy-student-95jsdu7sk-thetilldawn-3859s-projects.vercel.app/admin/assignments)
- 실행 커밋: `b5feaf4255e47307688c4da42b5c7a6f3d60ef98`. Vercel `dpl_3jU3hURbyzqhXxnUuywMstkYmhau`, Preview, Ready, Next16.2.12, 빌드1분9초.
- 작업별 구현 커밋 APP02 `f9ffba1` → APP03 `4e84c31` → APP04 `1ec7b55`. 최종 결합 코드만 게시했다. 중간 커밋 각각의 빌드 통과를 주장하지 않는다.
- 학교 검색키는 Secret·현재 Preview 브랜치 단독. Production/Development 선택 없음. 새 연결/팀/유료 전환/보호 해제 없음.
- 승인된 학교·학년 조회 migration 한 개만 staging에 적용했다. 운영 DB·main·운영 환경은 변경하지 않았다.

## 검사 결과

| 검사 | 실제 확인 | 결과 |
|---|---|---|
| T1 로컬 입력 결속 | 기존448파일3020통과/기존선택1skipped, 타입/lint/34페이지 빌드와890입력 동일. 게시 뒤6파일37검사 추가 통과 | 통과 |
| T2 조회 변경 | 실제 원본 함수 대비 학교·학년 두 projection만 추가. owner/ACL/security invoker/빈 search_path 유지. 적용 전후18자료 해시 동일 | 통과 |
| T3 서버 연결 | Preview 설정·배포 SHA 확인, 학교3곳/정상0건 조회, 실제 저장을 staging에서 다시 읽어 DB 연결 확인 | 통과 |
| T4 실제 화면 | 아래 모바일/PC·저장·배정·확인창 검사. 새 시험 배정/응시 없이 초안은 모두 폐기 | 통과 |
| T5 보호·게시 | main ee7be43 고정. 18영역 행수 동일, 테스트 대상 외79학생 전체 행 해시 동일, 시험·점수·단어·음원 등17영역 해시 동일 | 통과 |

- 학교 선택 후 테스트 학생 저장 성공. 상세 헤더·저장 안내·뒤쪽 학생 목록에 같은 학교가 반영됐다. 기존 학교값으로 다시 저장했고, 닫기/재열기와 DB 읽기로 이름·학교·학년 원복을 확인했다. 정상 수정시각·프로필 버전과 감사2건은 지우거나 역변경하지 않았다.
- 학생 목록 검색 후 상세 왕복에서 검색어·1명 목록·포인트29 유지. 오래 열린 배정은 빨간 오류 대신 이전 목록 안내를 보이며 초안은 유지됐다. 배정 학생 이름은 공용 학생 내역 탭으로 열리고 그 안의 배정 링크도 동작했다.
- 단일 배정의 학교+고1+교과서+표시 제목 검색은 해당 교과서2권만 표시했다. 실제9권 중 학교전용7/학교미분류2/명시공통0을 확인했다. 태그 값은 수정하지 않았으며 미분류를 임의 공통으로 넣지 않았다.
- 단어장 미선택은 먼저 고르라는 안내, 지원하지 않는 방향은 비활성 탭의 키보드 초점 안내로 표시됐다. 별도 빨간 자료 없음 오류로 오인시키지 않았다.
- 장문독해 전체601개/14범위, 회차당100개 → 가능한7회. `00100` → `100`, `05` → `5` 확인. 서로 다른 모의고사는 별도 범위 태그이며 DAY는 의미가 같은 연속 범위만 묶는다.
- 날짜2개 선택 시 회차 제목 옆100개, 학생 태그, 우측 공개/마감 날짜 표시 확인. 형용사500개는 날짜 없이5단위씩 나눠5회차×100개, DAY01~05부터21~25까지 표시됐다. 실제 배정 저장은 누르지 않았다.
- 확인창은 PC440×231.2px, 뒤쪽 배정창/페이지에 blur3px와 어두운 배경. 계속 작성은100개 입력을 보존하고 버리고 닫기는 초안을 폐기했다.
- 모바일 선택 바구니0/1/2명 높이 모두165.6px. 390×844/1240×900 가로 넘침0. 확인한 앱브라우저 warning/error0. 보기 크기는 원복하고 새 Preview 탭을 남겼다.

## 보존 근거와 한계

- 함수 SHA256 `79994ae652285d20cb9a42afb8ed475752dffe37bf748f25e4589c23084f42c6` → `b95115411d010221f9090c7a118f4a02ad9f336889b44a73aa2d556cff356aec`. 원본/보호 집계는 ignored `.codex-tmp/deploy090802-*`에 보존한다.
- 신규 보안·성능 안내0. 기존 보안96/성능78 안내는 이번 변경 대상이 아니며 보안 완전성 통과로 표현하지 않는다. [기존 정책 안내 기준](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy), [기존 인덱스 안내 기준](https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys).
- 관련 현재 입력811개, 지문 `05e6a10d493ff85814fc829c6cb53957b2634baf2b0e513adfecc36983af5914`. [실행 기록과 파일 해시](../../architecture/work-records/records/DEPLOY-20260908-02.json). 기록/설계 변경은 입력 일치 검사로 구별한다.
- 식별 JSON API의 브라우저 직접 열기는 클라이언트 차단, 외부 익명 읽기는 Vercel 로그인으로 이동하여 직접 JSON 확인은 못 했다. 이를 통과로 세지 않았다. 배포 정보 화면의 정확 SHA와 실제 staging 저장 대조로 연결을 검증했으며 보호를 우회/해제하지 않았다.
- 실제 Preview의 새 등록·로그아웃 강제·저장 실패 주입·물리 터치·전수 음원 청음·전체 서비스 성능 측정은 하지 않았다. 실패·권한·취소·비밀 노출 방어는 동일 코드의 로컬 자동검사 근거를 사용한다.
- 독립 기록 검토에서 migration 직전·직후 보존과 테스트학생 저장 뒤 최종 보존의 시점을 구분하도록 문구1건을 보완했다. 다른 주요 모순은 발견하지 않았다. 기록 형식29건·현재 완료 기록1건의 자동 검사도 통과했다.
- 구현 코드·DB 조건은 검사 뒤 추가 변경하지 않았다. 최종 검사 문서는 실행 커밋과 구분해 로컬 커밋으로 기록하며, 새 운영 배포나 다른 보류 작업은 자동 시작하지 않는다.
