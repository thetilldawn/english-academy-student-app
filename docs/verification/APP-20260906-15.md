# APP-20260906-15 발음 대기 중단 검증

기준: [지시서](../work-orders/APP-20260906-15.md). 기존 APP14 검사를 대신 사용하지 않고 이번 변경을 검사한다. 현재 상태·순위는 루트 대장 한 곳만 따른다.

## 2026-09-07 로컬 최종 검사

- 공용 요청별 음원 대기 훅·정지와 표시 부품을 연결했다. 서버 채점·API 필드·DB·공개·점수·재시험 규칙 변경0.
- 플레이어56검사 통과: 세 출제유형×첫시험/재시험×정오12조합, 첫 클릭/저장전/실패 중단0, 시작지연과 늦은 이벤트, 자연150ms/무음750ms, Tab·마지막 이동1회, 느린 시간확인·복구와 전체시간/유한마감/무제한3회귀 포함.
- 가짜자료/로컬도구 보호39검사 통과. 명시적 검사 옵션과 정확한 가짜6시험에만 메모리 답/시간확인을 허용하며 기본 읽기 모드·외부 요청 차단 유지. 독립 읽기 검산68건에서도 확정 P1/P2없음.
- 전체426파일2,730검사 통과, 기존 선택 외부원자료1건 skipped. 타입·lint·UI구조·요청캐시·변경기록·기능지도·Next16.2.12 최적화빌드33정적페이지 통과.
- 최초 전체 실행은 지정 병렬 수4를 빼고 빌드와 동시에 실행하여 기존 검사10건이5초를 넘겼다. 제한을 늘려 숨기지 않고 프로젝트 지정4로 다시 실행해 모두 통과했다. React ref 렌더 읽기 lint1건도 state 표시/이벤트 ref 확인으로 수정 후 재검사했다.
- 로컬 앱브라우저: 여섯 가짜시험 각각1→2문항, 답 저장6/시간확인6/다음답 자동저장0. 좁은467×986 화면의 오답 재시험에서 선택지 위치 클릭도 중단에만 사용. 실제 미디어 playing/pause 관측·오류0, 콘솔warn/error0. 음원·다음문제 늦은 입력 수치는 ignored `.codex-tmp/app15-local-browser-20260907.json` 보존.
- 실제 Next 인증 경유: 익명 응시조회401, 가짜학생200, 첫시험 미응답2문항 정답null, 서버키노출0. 재시험은 기존 첫시험에서 이미 공개된 정답을 유지하는 계약이다.
- 최종 지문158입력 `9bf474aceb232313a8ec3a5769f4f46d983c6fcda6a2d0d518c4e1107cf74ecd`. 전체검사 뒤 기능지도에 새 검사 파일1개를 등록했고, 전후 해시 차이는 지도1개뿐이다. 관련 지도/기록112검사를 재실행했으며 실행 코드/테스트/설정은 동일하다. 앞 기록의 `a7837b30674fb1d52b1e9d035aad89806fe78faff0fba6198f474d0b56503f6d`는 지도 등록 전 값으로 정정한다.

## 검사 범위의 한계

- 실제 손가락 장치·누른 상태에서 자연 종료가 겹치는 브라우저 경쟁은 미검증. 터치 pointerdown/up→click와 같은 입력 소비는 컴포넌트 검사, 실제 브라우저는 클릭 경로로 구분한다.
- 검사음은 원본 발음 대신8초 저음량 로컬 WAV다. native Audio를 유지하며 지정 가짜 URL만 연결했다. 실제 발음 정확도 검사가 아니며, 자연종료150ms는 자동검사 근거다.
- 기존 복구 GET 응답은 Cache-Control 헤더를 명시하지 않는다. 클라이언트의 cache:no-store와 동적 인증 조회는 유지하며 이번 패치에서 공유캐시/저장캐시를 추가하지 않았다. 응답헤더 검사를 통과한 것으로 계산하지 않는다.
- 로컬 검사 기록 시점에는 Preview/운영 배포 전이었다. 이후 배포 근거는 아래에 별도로 기록한다. 실제 학생의 시험 시작/답 제출0, 배포 전 운영 진행중 첫시험0·review2를 읽기 확인했다.

## 2026-09-07 Preview·Production 반영

- 동일 구현 커밋 `189e5d309901bf227947afffc24227b7c8cf6623`을 승인된 기능 브랜치와 main에 일반 push했다. DB migration·환경변수·요금제·접근 보호 변경0.
- Preview 배포6296943648은 2026-09-06 19:18:14 UTC 성공. [해당 Preview](https://english-academy-student-75hqa9qdi-thetilldawn-3859s-projects.vercel.app/admin)에 기존 관리자 인증으로 접속해 개요 응시전2/미응시·미통과92/완료7, 콘솔 경고·오류0을 확인했다. 배정·시험 쓰기0, 보호13표의 전후 행 수·SHA가 일치했다.
- Production 배포6297006679는 19:24:49 UTC 성공. [운영 학생 화면](https://english-academy-student-app.vercel.app/student)의 기존 테스트 학생에서 포인트0·공개예정2·확인필요14·완료3·종료1과 콘솔 경고·오류0을 확인했다. 배포 직후 보호13표 전후 일치. 실제 학생 시험 시작/답 제출0.
- 운영 주소에서 새 안내 코드2개와 실제 시험 진행 코드1개를 HTTP200으로 읽어 로컬 최적화빌드 파일과 바이트 SHA가 모두 같은지 확인했다. 이는 배포된 코드 확인이며 운영 학생으로 시험을 다시 풀었다는 의미가 아니다.

| 배포 파일 | 바이트 | 로컬·운영 공통 SHA-256 |
|---|---:|---|
| `07-4ttpynrziy.js` | 25,562 | `88d8e786c16644273b0cfcd3ff88962a360c2b19cb8bdf8372331d1831b93b11` |
| `3a5i6von0c8h5.js` | 26,244 | `26b1e8c5631d64d649e6b52f7e8de77de1d31ccaf14b112cc86256ebacfdae90` |
| `19-c7ax3lc1qt.js` | 38,442 | `0d26a74bcd4194a6c2b6891fa0b015ad4107007bad89defe85d009ae7bb5d90f` |

### 원격 검사와 남은 한계

- Preview smoke run34054467431/job101543673589는 앱 검사를 시작하기 전 승인 브랜치 대조에서 실패했다. `APPROVED_PREVIEW_REF`가 비어 있으며 이벤트 브랜치와 승인값이 맞지 않는다는 안전 검사 오류다. 뒤의 설치/브라우저 단계는 skipped이므로 통과로 계산하지 않는다. 작업 범위를 넓혀 브랜치 보호나 승인값을 바꾸지 않았다.
- Production 이벤트 run34054817792는 Preview 전용 workflow라 skipped이다. 호스팅의 실제 두 빌드 성공, 로컬 자동검사, 앱브라우저 확인과 CI 상태를 구분한다.
- Vercel 전체 배포/런타임 오류 조회는 기존 권한에서403이다. 관측한 화면 콘솔 이외 전체 서비스 오류율·지연을 측정했다고 주장하지 않는다. Preview의 API 직접 탐색은 브라우저 `ERR_BLOCKED_BY_CLIENT`지만 HTML 로그인·관리자 화면은 정상이다. 접근 보호를 해제하거나 새 인증 연결을 만들지 않았다.
- 최종 독립 검토에서 저장 실패 설명의 “선택 보존”이 기존 코드와 다름을 발견했다. 서버 복구를 우선하고 최종 실패 시 선택 표시를 초기화한다는 기존 동작으로 지시서/RDT만 정정했다. 실패를 정상 완료로 바꾸거나 이번 패치에서 저장 동작을 변경하지 않았다.
- 로컬 전용 서버와 자식 프로세스를 정확한 실행 경로/PID로 확인해 종료했다.3037/3038/3040 리스너0, 원래 `.next` 복구, 새 검사 빌드는 ignored `.codex-tmp/app15-local-verified-next-20260907`로 보존했다. 자료 삭제0.

## 배포 15분 뒤 최종 확인

- 운영 ready19:24:49 UTC 이후19:39:52에 학생 화면을 새로 조회하고,19:40 이후 학생 목록 정상·포인트0·공개예정2/확인필요14/완료3/종료1·콘솔 경고/오류0을 확인했다. 최종 기록 시각 `2026-09-06T19:41:25Z`.
- Preview/운영 각각13개 보호표의 행 수와 전체 행 내용 SHA-256이 배포 전과 같았다. 운영 진행중 first0·review2도 유지되었다. 단순 수 합계만 비교하거나 개별 학생 자료를 출력하지 않았다.
- 최종 감사 원본은 ignored `.codex-tmp/app15-release-final-20260907.json`에 보존했다. T5의 배포 전 not_run은 당시 커밋과 위 로컬 시점 기록에 남기고, 실제 확인 근거로 현재 완료 결과를 갱신한다.
- 구현 이후 최종 변경은 지시서·검증·인계/RDT 정정뿐이다. 검사한 실행 코드와 배포 코드189e5d3은 동일하며 최종 기록만을 위해 재배포하지 않는다.
- 최종 `check-work-records --phase complete --work APP-20260906-15` 통과. 설계+변경 연결18건도 통과했으며 자동검사가 물리 장치·사용자 동의·전체 운영 안정성까지 증명하는 것으로 표현하지 않는다.
