# 캐시·단어장 최종 배포 검증

## 로컬 검증

- 입력: 기존 운영 74e1a597f72dc9b8e72a41c215dd96a0390c79b7, 로컬 기준 bc48313bc53840a44ab6505a8238eaabf7667eae와 APP-20260905-06 및 APP-20260906-01~13 누적 변경.
- 사용자 승인: 캐시·구조 먼저, 마지막 학생 가리기 후 안전하면 Preview/Production. 기존 공개 GitHub에 앱 코드·개발문서 업로드 승인. 실제 학생·시험 기록 변경과 DB·새 비용·팀 생성은 제외.
- 2026-09-06: 설정·학생 패널 2파일33검사, 전체419파일2618검사, 타입, lint, 기능 지도, UI 구조, 요청 캐시2회, Next16.2.12 격리 ON 새 빌드 통과.
- 변경 연결 검사가 발견한 관리자 layout/배정 화면의 관련 흐름 누락을 DEPLOY 기록에 보완했다. 실행 코드는 변경하지 않았다. 최종 변경 연결 재확인 결과는 아래에 추가한다.
- 최종 설계+변경 연결15건 통과. 인계/통합 검증 문서 포함 최종 변경305파일을 APP236/DEPLOY69로 정확히 한 번씩 귀속했다.
- 독립 읽기 리뷰: 활성화 정책/다섯 분기 일치, 인증 이후 계정별 Provider, 학생OFF→배정OFF/내역 독립, P1/P2 지적 없음.
- 운영 기준 대비 소스 후보346파일의 대표 비밀키·학생/원자료 확장자 검사0. DB SQL·의존성 lock 변경0. 자동 패턴은 개인정보 안전을 완전히 증명하지 않으며 최종 staged 파일 목록을 함께 확인한다.
- APP13: 일반 영어/뜻 중 하나만 가리기, 250ms 블러, 영영풀이 유지, 예문 대상만 가리기. 로컬 모바일499/PC1440 및 직접 페이지/모달 확인. 토글3회 HTTP·자료 요청0. 현재 운영에 배정된 예문43/43 원문 정렬/복원 일치(읽기 전용), 일반/미래 모든 자료 승인으로 확대하지 않음.
- 과거 APP 검증은 당시 근거로 보존. 이번 전체 검사만 최종 통합 실행 코드의 회귀 근거로 사용한다. 개발 서버/가짜 데이터 측정은 운영 속도·DB 비용 실측이 아니다.

## 커밋 경계

- APP-20260905-06 및 APP-20260906-01~13: 지시서별 고유 파일/문서/검증 파일을 14그룹으로 분리.
- DEPLOY-20260906-01: 여러 APP에 걸친 공유61파일의 최종 구현·연결과 배포 설정, 공통 실행계획을 통합한다. 배포 설정만 바뀌었다고 표현하지 않는다.
- 파일별 관련 작업번호는 동반 JSON에 보존한다. 중간 커밋은 독립 실행·검증 판이 아니며 마지막 통합 SHA만 공개 배포 후보로 삼는다. 과거 코드를 임의로 재구성하거나 과거 해시 검증을 현재로 꾸미지 않는다.
- 초기303변경 후보를 정확히 한 그룹씩 분류했다(APP236/DEPLOY67). 이 검증 기록과 인계 추가는 DEPLOY 메타데이터로 귀속한다.

## 외부 검증

- 미실행. 정확한 Preview 성공과 읽기 검증 후에만 main을 비강제 fast-forward한다.
- 2026-09-06 06:18:44 UTC Preview 배포6289876786 success. 실행 SHA a32dc35d55ad5812427eb0b81aca3e1f46d7cb33, tree9a55a45d467cbb8475d4293fa62fd7c506b7b03c. 위 미실행 항목 중 Preview 빌드와 관리자 읽기만 아래 근거로 갱신하며, 전체 T3 완료는 아니다.
- [Exact Preview](https://english-academy-student-547ni0f8j-thetilldawn-3859s-projects.vercel.app): 기존 Vercel 로그인 상태의 배포 화면에서 Visit으로 접속했다. MCP 404를 팀 부재로 단정하거나 새 연결·팀·우회키·보호해제를 만들지 않았다. Codex가 API 확인 주소의 직접 문서 이동을 차단해 preview-identity 본문 확인은 미검증이다.
- 기존 작업 기록의 Preview 전용 계정으로 관리자 로그인, 개요·학생·배정·내역 조회와 학생 재방문, 기존 테스트 학생 상세·계정 메뉴 읽기, 닫기 성공. 최종 브라우저 경고/오류0. 실제 캐시 요청 횟수·운영 지연시간은 미계측이며 로컬 수치를 대체하지 않는다. 계정 비밀은 이 문서/앱 Git에 저장하지 않는다.
- Preview ref wojxpruvbjzbhrpmsbuy의 최소 SELECT: 관리자 학생/배정/내역 읽기 RPC4/4 존재. 단어장 조회 public.get_student_assignment_study_v1(uuid,uuid), private.assignment_study_examples_v1, public.vocab_entry_pronunciations.display_snapshot 누락3. 실제 학생 코드 읽기/새 계정/배정/시험·점수/DB 변경0.
- 앞2개는 기존20260905051013 migration, 표시 열은20260905063836의 5~6행에 대응한다. 뒤 migration 전체에는 Production 승인표·활성화 수정도 있으므로 그대로 Preview에 적용하지 않는다. 구조 보완과 예문 원문 데이터 채우기는 별개이며 자료 정제·재생성을 임의로 재개하지 않는다.
- D5 사용자 결정 필요로 추가 구현·DB 적용·Production 반영을 보류했다. main은74e1a597f72dc9b8e72a41c215dd96a0390c79b7 그대로. Preview 구조 보완 승인 뒤 해당 영향만 설계·검사 계획을 갱신하고 진행한다. 마지막 학습 보기의 로컬 성공을 원격 검증 완료로 표시하지 않는다.
- Production은 해당 환경에서 별도 빌드한다. Preview 산출물을 곧바로 운영 주소에 연결하지 않는다.
- 캐시는 기본ON, 명시0/잘못된 값은OFF. 실제 배포에서 현재 인증 후 목록 재진입 경로를 확인한다.
- 복구는 각 CANARY=0 설정 후 재배포 또는 기본OFF 복구 커밋. 전체 복구는 이전 운영SHA의 정상 배포이며 force-push·DB 역변경 없음.

## D5 승인 후 Preview 구조 복구 (R4/T4)

- 사용자 승인: `ㅇㅇ 완성해서 프로덕션에얼려`. 적용 대상은 Preview `wojxpruvbjzbhrpmsbuy` 한 곳. Production DB/학생 기록/문항 승인·활성화/원문 생성·이식은 제외.
- 설계 관문 통과, 기존 학습 SQL 로컬 PGlite 8/8 통과. 실행 묶음의 한 트랜잭션·기존 행 NULL 보존·JSON 객체 허용·배열/숫자/참거짓/문자열 거절·NULL 복원 통과.
- 기존 migration 원본을 수정하지 않는 누락 복구 실행이다. 로컬에 새 순차 migration을 만들어 운영에 다시 적용하지 않는다. 원격 이력 이름 `preview_student_study_read_compatibility`로 한 번만 기록한다. 원격 version은 적용 후 기록한다.
- 원본051013 SHA256 `29ac0cc98c3d4add7047f585834e88049ad7820c10d18c4358d3484bed16b429`; 원본063836 SHA256 `512f6f08c61ecc26e4a51a7dd6dc452aef4f68e1169aa4f09d774a1aed0df448`; 표시 열 발췌 SHA256 `88a5bbfeb4f16ea4d6b6f12c7b3e1f6281ae2013eb9c91f815b5689a539f8907`.
- 실행 SQL SHA256(LF) `7668338b5f1ab3f709062794d0a836c94ec4b137ee5847ecd53480a4ebc5b51a`. 051013의 마지막 COMMIT 앞에 발음 표시 열 ALTER만 삽입한다. 원본063836 전체 적용으로 표시하지 않는다.
- 기존 학생80/배정178/배정학생177/문항5387/응시90/포인트합계21/이벤트638/발음60/스냅샷4751/canonical릴리즈9·문항2785 및 활성화 함수1의 전후 행/내용 지문을 비교한다. 발음표는 새 표시 열을 제외하고 비교한다. 보안 advisory 사전88건(INFO46/WARN42)은 기존 권한 계약 이력으로 보존하며 새 경고를 비교한다.
- 복구: 거래 실패시 전체 취소. 성공하면 빈 예문표·NULL 발음열 등 읽기 호환 구조를 보존한다. 자료 삭제/DROP/운영 역변경은 실행하지 않는다. 원문표0행은 Preview 원문 내용 검증 통과가 아니다.
- 공식 검토: [Supabase 변경 이력](https://supabase.com/changelog)의 새 표 API 자동노출 변경과 [Postgres 역할](https://supabase.com/docs/guides/database/postgres/roles)을 확인했다. 명시적 grant/revoke와 service_role 전용 계약을 유지한다.

### 실제 실행 SQL

```sql
begin;

-- Learning-only original examples. No changes to questions, attempts or scores.
create table private.assignment_study_examples_v1 (
  release_id uuid not null,
  vocab_entry_id bigint not null,
  quiz_mode text not null default 'canonical_example_to_headword'
    check (quiz_mode = 'canonical_example_to_headword'),
  question_item_id text not null,
  question_item_sha256 text not null,
  source_example_sha256 text not null,
  example_en text not null check (length(trim(example_en)) > 0 and example_en !~ '_{2,}'),
  imported_at timestamptz not null default now(),
  primary key (release_id, vocab_entry_id),
  foreign key (release_id, vocab_entry_id, quiz_mode)
    references word_index.app_canonical_question_preview_item(release_id, vocab_entry_id, quiz_mode),
  check (source_example_sha256 = encode(extensions.digest(convert_to(example_en, 'UTF8'), 'sha256'), 'hex'))
);
alter table private.assignment_study_examples_v1 enable row level security;
revoke all on private.assignment_study_examples_v1 from public, anon, authenticated, service_role;

create function public.get_student_assignment_study_v1(p_student_id uuid, p_assignment_id uuid)
returns jsonb language sql stable security definer set search_path = '' as $$
  with permitted as materialized (
    select a.id, a.title, a.quiz_content_mode
    from public.assignments a
    join public.assignment_students recipient on recipient.assignment_id = a.id
    join public.students student on student.id = recipient.student_id
    where a.id = p_assignment_id and recipient.student_id = p_student_id
      and recipient.cancelled_at is null and recipient.assigned_at <= now()
      and a.deleted_at is null and a.status in ('active', 'closed')
      and student.deleted_at is null and student.status = 'active'
  ), learning_rows as (
    select q.vocab_entry_id, e.dataset_id, e.source_row,
      jsonb_build_object(
        'entryId', q.vocab_entry_id,
        'headword', coalesce(nullif(s.headword_snapshot, ''), case when q.provenance_status in ('verified_v2','reviewed_for_preview_v1','preview_verified_v1') then nullif(q.headword_snapshot, '') end, e.headword),
        'meaning', coalesce(nullif(s.primary_meaning_snapshot, ''), case when q.provenance_status in ('verified_v2','reviewed_for_preview_v1','preview_verified_v1') then nullif(q.primary_meaning_snapshot, '') end, e.primary_meaning),
        'displayKo', coalesce(s.display_pronunciation_ko_snapshot, e.pronunciation_ko),
        'pronunciationSnapshot', s.pronunciation_snapshot,
        'dictionaryId', s.dictionary_id,
        'releaseId', coalesce(s.release_id, r.exam_use_release_id),
        'definition', case when q.eligibility_quiz_mode = 'canonical_definition_to_headword' then q.prompt end,
        'example', case when q.eligibility_quiz_mode = 'canonical_example_to_headword' then x.example_en end
      ) as item
    from permitted a
    join public.assignment_questions q on q.assignment_id = a.id
    join public.vocab_entries e on e.id = q.vocab_entry_id
    left join public.assignment_question_exam_use_snapshot s
      on s.assignment_question_id = q.id and s.provenance_status = 'reviewed_for_preview_v1'
    left join word_index.app_canonical_question_preview_release r
      on r.release_id = q.canonical_question_release_id_snapshot
    left join word_index.app_canonical_question_preview_item source
      on source.release_id = q.canonical_question_release_id_snapshot
      and source.vocab_entry_id = q.vocab_entry_id
      and source.quiz_mode = q.eligibility_quiz_mode
      and source.question_item_id = q.canonical_question_item_id_snapshot
      and source.question_item_sha256 = q.canonical_question_item_sha256_snapshot
    left join private.assignment_study_examples_v1 x
      on x.release_id = source.release_id and x.vocab_entry_id = source.vocab_entry_id
      and x.question_item_id = source.question_item_id
      and x.question_item_sha256 = source.question_item_sha256
      and x.source_example_sha256 = source.source_example_content_hash
  )
  select jsonb_build_object('assignmentId', a.id, 'title', a.title, 'mode', a.quiz_content_mode,
    'words', coalesce((select jsonb_agg(item order by dataset_id, source_row, vocab_entry_id) from learning_rows), '[]'::jsonb))
  from permitted a;
$$;
revoke all on function public.get_student_assignment_study_v1(uuid,uuid) from public, anon, authenticated;
grant execute on function public.get_student_assignment_study_v1(uuid,uuid) to service_role;
comment on function public.get_student_assignment_study_v1(uuid,uuid) is
  'Session-scoped server-only learning view: assigned targets in source order, never choices or answer indexes.';
alter table public.vocab_entry_pronunciations add column display_snapshot jsonb
  check (display_snapshot is null or jsonb_typeof(display_snapshot)='object');
commit;
```

### Preview 복구 후 확인

- 원격 migration `20260906071526_preview_student_study_read_compatibility` 성공. 보호11표+활성화 함수1의 행·내용 지문12/12 일치. RPC service_role만 실행 허용, anon/authenticated 거절; 예문표 RLS 및 API 역할 직접 읽기 거절; 예문0행/표시열 비NULL0행.
- 보안 advisory 88→89: 신규 WARN/ERROR0. 추가 INFO1은 private 원문표의 의도적인 RLS 무정책(모든 직접 접근 차단)이며 원래 코드와 같은 계약이다.
- IAB exact Preview 기존 테스트 계정 로그인→기존 배정 단어 보기→12개 단어/뜻/듣기→영어만 가리기→뜻만 가리기로 전환시 영어 복원→모달 표시 정상. 브라우저 warning/error0. 시험 시작·답 제출·배정·학생 수정 없음. 로그인으로 테스트 계정 접속 시각/세션은 정상 갱신되며 시험 기록 변경과 구분한다.
- 실제 Preview의 유효 배정은 교재 뜻/옛 교재 뜻 두 유형만 있다. 읽기 RPC 대표20/25단어 반환, 선택지·정답·출제순서 필드0. 예문/영영풀이 원격 배정을 새로 만들거나 자료를 이식하지 않는다. 두 유형은 로컬 APP13 검사/화면 근거와 운영의 기존 검토 데이터로 확인한다.
- 현재 Preview의 발음은 기존 음성만 있는 항목도 있다. NULL 표시열을 채운 것으로 주장하지 않는다. Production의 기존 발음 표시 데이터는 변경하지 않는다.

### 최종 재검사

- 제한 환경 전체 검사는 worker 비정상 종료로 실패했으며 통과로 세지 않았다. 별도 실행 계정의 전체 재검사는 자료 파일 EPERM으로8실패/14건너뜀(앱 코드 수정 없음). 기본 실행 계정에서 영향6파일26검사를 다시 실행해 전부 통과했다. 파일+검사명별 최신 결과 합산은419파일2618개 전부 통과, 남은 실패/건너뜀0이다. 단일 명령 성공으로 표현하지 않는다.
- 재실행 기록은 로컬 `.codex-tmp/DEPLOY-20260906-01-final-tests.json`과 `DEPLOY-20260906-01-source-retry.json`. 접근 권한이 다른 첫 실행의 실패도 보존한다. 원자료를 복사·수정하거나 검사 기대값을 낮추지 않았다.
- 타입/lint/기능지도/UI구조/설계+변경연결15건/요청캐시2회 재확인 통과. 실행 소스·SQL 원본·lock은a32dc35와 같으며, 현재 미커밋 변경은 배포 승인/검증 기록4개뿐이다. Preview 성공과 같은 a32dc35를 운영에 새 빌드한다.

## 운영 반영과 실제 화면 최종 확인

- main을 `74e1a59`에서 검증된 `a32dc35d55ad5812427eb0b81aca3e1f46d7cb33`로 비강제 fast-forward했다. 운영6290433405는2026-09-06 07:33:27 UTC success. Vercel은Production/Ready/main/a32dc35, 새 빌드1분7초를 표시했다.
- [운영 화면](https://english-academy-student-app.vercel.app/), [정확한 운영 배포](https://english-academy-student-f4kj1r7z6-thetilldawn-3859s-projects.vercel.app/), [배포 근거](https://vercel.com/thetilldawn-3859s-projects/english-academy-student-app/88eK8MK3s9gbxZXi14rEgvvFeASo).
- 운영의 기존 테스트 학생 세션 재사용. 실제학생 또는 새 배정/응시를 만들지 않았다. 일반46단어·영영풀이16단어·예문39단어의 기존 학습 목록을 읽었다. 발음 표시와 듣기 버튼이 표시된다. 일반 영어→뜻은 각각 가리며 반대쪽 선택시 복원. 영영풀이 본문 유지. 예문은 빈칸 없는 원문에서 abnormal/absent/absolute 등 해당 대상만 블러. 모달 닫기/재열기·상단 포인트 정상. 운영 실제시험 시작/답 제출/점수 변경0.
- 운영 관리자 학생→배정→내역→학생 조회 정상. 캐시 수명 만료의 `최신 시험 내역을 다시 확인해 주세요`와 `다시 시도` 복구가 실제로 동작했다. 최초 조회·현재 인증 대기는 남아 있으며 요청0회/로딩0초/운영비 절감을 실측한 것으로 주장하지 않는다.
- 운영 학생 목록도 시간이 지난 뒤 `최신 학생 목록을 다시 확인해 주세요`를 표시했다. 이 문구는 `useCachedStudentDirectory` 전용 상태로 현재 학생·내역 캐시 경로 활성화를 실제 화면에서 확인했다. 환경변수 원문을 읽은 것으로 대신 표현하지 않는다.
- 운영 학생/관리자 브라우저 warning/error0. 환경변수 화면 열기는 자동보안검토가 비밀값 노출 위험으로 차단했다. 해당 화면이나 API로 우회하지 않았으며 실제 캐시 UI·재조회 동작과 공개 배포 환경으로 확인했다. 개별 비공개 환경변수 원문은 읽지 않았다.
- APP13의 모바일499/PC1440·고정상단 스크롤·250ms/reduced-motion·음성중단은 로컬 코드/컴포넌트/실화면 근거를 유지한다. 이번 운영 확인은PC1280 실제 화면이며 250ms를 프레임별 계측하거나 음성을 직접 청취한 것으로 표시하지 않는다.
- 운영 DB DDL/문항 승인/활성화/원문 정제·이식 변경0. Preview만 승인된 읽기 구조3개를 복구했다. APP 전체 향후 개발·자료 정제·추가 기능은 이 배포 완료에 포함하지 않는다.
