# 학교 어휘 두 방향 등록 검증

2026-09-14 후속 운영 근거: 사용자 `ㄱㄱ` 승인 뒤 같은 SQL이 검토용20260914111607·운영20260914112515에 적용됐다. 루트 DEPLOY-20260914-04에 실제14회차432행864문항, 양쪽18774검사·독립20985/21040 오류0, 보호32/32·함수4개 일치, 운영 관리자 교재/회차·뜻 양방향25문항 출제 가능 확인을 연결했다. 실제 배정/응시/답 제출은0이며 아래 승인 거절·미적용 설명은 이전 로컬 검증 시점의 기록이다. 실제 원고와 운영 조회 자료는 앱 밖 학교 폴더에만 둔다.

기존 학교 자료 등록은 단어마다 네 방향 문항과 영영풀이를 요구했다. 새 변경은 승인 수량이 단어 수의2배인 경우에만 뜻 두 방향을 허용한다. 기존4배 승인의 필수 조건과 기존 entry 검사는 그대로 유지한다. 두 방향은 숨길 기존 교재가 없는 신규 교재에만 사용한다.

- T1: 학교 등록 실제 SQL 검사60개 통과. 기존46개와 새14개로 등록·활성화·동일 원고 재등록, 다중 회차, 두 방향별 가용성/배정·공부, 활성화 전 원문/분류 변조 거부, 없는 정의의 NULL 처리, 잘못된 모드/수량/숨김/원천 거부를 확인했다. 가짜4회차 데이터로 실행했으며 실제 학교14회차는 앱 밖 별도 검사다.
- T2: 기존 검토형 등록 회귀55개 통과. 합계115개 통과·0실패·0제외이며 실행 결과는 `tmp/performance-meaning-tests.json`에 있다. 학생 실데이터는 사용하지 않았다.
- T3: 타입 검사, 변경 검사 파일의 lint, 기능 지도44개 관리 migration/15개 흐름, UI 구조 검사,51개 작업 기록의 설계·변경 연결 검사를 통과했다. 공개 API·화면 코드는 바꾸지 않았다.

독립 구현 검토에서 원문·권한·기존 네 방향 회귀를 확인했다. 새 교재의 교과서 분류/연도 정보 복사 위험을 제거하여 자체 교재 분류를 만들고 활성화 시 해당 값도 검사한다. 정의 모드 거부 검사는 학교 정의가 실제 제공된 가짜 행으로 수정하여 의도한 등록 모드 검사에 도달하는 것을 확인했다. 마지막 수정 후115개를 다시 통과했다.

최종 SQL: `supabase/migrations/20260914085457_allow_school_handout_meaning_only.sql`, SHA-256 `5aca9b9c3a73d363ca91f706ae6963c6830a432501c18facfc72b05bcdaff8e9`. 초기 로컬 실행에서 기존 CHECK 이름을 잘못 지정한 오류와 검사 원고의 공유 참조 오류를 수정했고, 수정 전 결과를 통과로 계산하지 않았다. 최종 입력 확인값과 파일 목록은 APP-20260914-03 작업 기록에 저장한다.

실제 학교 원고는 앱 저장소 밖 영어 자료 구역에서432행·864문항 검토를 마쳤고, 별도 로컬 임시 DB에 등록·활성화·재등록하여14회차를 확인했다. 영영/예문 유형0, 두 방향 각432문항이며 실제 원고 내용 확인값은 `56f72485d22cea3aaf060b924271274c707d079d33b1879a6973809327348f38`이다. 앱 코드에는 실제 원고·정답·학생자료를 넣지 않았다.

검토용 DB 적용 요청은 자동 승인 검토에서 구체적 DB 변경 승인 부족으로 거절되었다. 양쪽 외부 DB 등록, 실제 관리자 화면, 학생 새 배정·응시 검사는 미수행이다. 새 DB 변경/교재 적용은 루트 DEPLOY-20260914-04의 명시 승인 뒤 진행한다. 기존 앱 코드 배포나 기존 교재·학생 기록 변경은 이번 적용 범위에 포함하지 않는다.
