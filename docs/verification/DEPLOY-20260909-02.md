# DEPLOY-20260909-02 전체 단어 발음 교정 배포 검증

사용자 승인한 전체 연결 점검·교정·Preview/Production 반영과 운영 후 관찰의 실제 결과를 기록한다.

## 실행 코드와 자료

- 코드 `be313fddd1c3bb32d60c8e59126ffd9124d73a1a`, 직전 운영 `541e546bc17baf92b31ad69dc1881f42facb1038`.
- 새 migration `20260908203803_add_entry_source_pronunciation.sql`, SHA `ba3b91b211da24073d11b4857f5a0f868410fd5f19c6f8b85c514728d7d204a8`.
- 자료324 SHA `5ae15f29d88e4079e0c577e591043298ecc0fb79d2492b20bdcfbf23bc9f55c7`.
- 제한 적용 SQL SHA `24c4c31b70c191883ab3b8c3ca6ee4a4e7e44db269fe373786c1b0fc9eb3d598`, 정확 취소 SQL SHA `bff9e4f456589e09d3d9b9d3bfc585259ea92d1c65e78da47739b1fc72cb6825`.
- 원자료/원격 응답은 앱 밖 v12 전체단어 발음연결 점검 폴더에 보관하며 Git에는 넣지 않는다.

## 로컬 검증과 연결 규칙

- T1: APP02 전체450파일 3088통과·실패0·기존 선택 외부자료 검사1미실행. 타입/lint/기능 지도/UI 구조/요청 캐시 검사와 Next16.2.12 격리34페이지 빌드 통과. 새 DB26+문항37 및 최종 공통 선택46 검사를 포함한다. 자세한 실행 시각/미실행 조건은 [APP02](APP-20260909-02.md)에 보존한다.
- 현재 APP02 입력849개 지문은 `9c6a0cd45b57e82ecccfe49d8bde25717c5d01cfd21b5cf06bd7c71fe071f029`. 이 배포 기록은 같은849개를 별도로 결속한 `9fda2a254cd5fe9e7736fecd26f650aac7d66116f55ad69c056b0aaec34e632e`이다.
- 서버가 정확 단어 행/원본 해시/실제 영어 문제 또는 선택지/품사별 음원 변이/음원 URL/단일 출처 증거를 함께 확인한다. 일치하지 않으면 교정값을 다른 단어에 적용하지 않는다. 단어보기·응시·결과가 같은 선택기를 사용하고, 저장된 과거 시험의 영어를 현재 사전 표제어로 바꾸지 않는다.
- 기존 사용자 지정 및 확정된647개를 우선 보호한다. 교정은 표시와 강세만 바꾸며, 음원·문항·답·점수·학생 상태를 다시 쓰지 않는다. 새로운 브라우저 요청/공유 캐시/음원 생성은 추가하지 않았다.
- 일반 변경연결 CLI는 지문을 변경파일마다 반복 계산해 장시간 무출력 상태가 되어 중단한 이력이 있다. 같은 검증 코드를 메모리에서만 지문 캐시한 동등 검사로34기록/609변경/오류0, 33개 지문을 다시 계산해 일치를 확인했다. 검증 조건이나 저장소 검사 코드의 지문 계산을 생략/수정하지 않았고 원래 중단 실행은 통과로 쓰지 않는다.

## Preview 적용

- T2: staging 새 교정 읽기 구조 적용 후 정확324행 등록. 실제 RPC 응답324개를 공통 선택기에 넣어 전체5389음원 불변·표기324 일치·기존647승인 보호를 확인했다. 반영 전후23개 보호표의 건수와 전체행 해시가 모두 동일했다.
- T3: `dpl_E5pzTS96BSAoqobkxSbyqYbhDVpA`, [Preview](https://english-academy-student-9s5nt9w4n-thetilldawn-3859s-projects.vercel.app), 브랜치 codex/local-assignment-check-20260905, be313fd, Ready. 운영과 따로 빌드했다.
- 프리뷰 루트의 실제 학생/관리자 로그인 화면은 열렸다. 연결정보 API 이동은 브라우저 ERR_BLOCKED_BY_CLIENT였고, 인증된 연결 도구는 배포 접근이 되지 않았으며, 비인증 HTTP는 Vercel 로그인으로 이동했다. JSON 확인이나 새프리뷰 학생 인증을 성공으로 계산하지 않는다. 팀/요금/환경/인증을 재설정하거나 접근 보호를 풀지 않았다. 서버 검증과 실제 인증 화면의 한계를 구분한다.

## Production 적용과 실제 화면

- T4: main을 비강제 fast-forward하여 같은 be313fd를 운영 환경에서 별도로 빌드했다. `dpl_5NGkUpYZhEJcUyZPpJkSm4ECSWk9`, 2026-09-09 06:43:17 KST Ready. [운영](https://english-academy-student-app.vercel.app) / [고정 배포](https://english-academy-student-pvn3wgs7a-thetilldawn-3859s-projects.vercel.app).
- 06:43:44 KST 정확324 자료 등록. 실제 RPC324와 최종 선택5389를 다시 검사해 교정324/기존승인647/기존음원5389가 모두 예상대로였다. 직후23개 보호표 전체 해시 불변, 기존 진행 중 시험2건 보존.
- 테스트학생 과거 형용사100: 표시 누락0/영어 스피커100. accurate=액큐릿, absent=앱슨트, active=액티브, actual=액추얼, artificial=아ː터피셜 유지.
- 모고 영영풀이→영어4: 표시/영어 스피커4, 문장 스피커0. 모고 영어→영영풀이12: 표시/영어 스피커12, 문장 스피커0, religious=릴리저스 교정 확인.
- 교과서 영어Ⅱ1과80: 표시 누락0/영어 스피커80, collect=컬렉트·parent=패런트·average=애버리지·develop=디벨럽 등 실제 교정 확인.
- 장문독해100: 표시 누락0/영어 스피커100, bold=보울드·tradition=트래디션 교정 확인.
- 5개 기존 단어보기에서 총296항목의 표시/스피커를 읽고 각 화면 브라우저 오류·경고0을 확인했다. 대표 장문독해 실제 화면도 확인했다. 새 배정/시험 시작/정답 제출/계정 변경 없음.
- 두 DB 모두 교정 함수 anon/authenticated 실행 불가, service_role만 실행 가능, 비공개표 RLS 유지. 기존 학생·시험 권한을 넓히지 않았다.

## 출처·원음·복구

- T6: 원본417파일+관련 입력6개 해시를 고정했다. 원본 대조/출처 증거/정확 적용 및 되돌리기18검사와 독립 검토를 통과했다. 기존 대상 키가 달라진 상태의 재적용을 거절하는 보호 검사를 포함한다.
- 9권5389출현은 VOCA3001·장문601·고2교과서320/189·구모고278·형용사500·고1교과서111/111·네방향모고278이다. 신규 표시 교정은 각각186/11/45/26/17/0/13/9/17이다. 형용사500은 앞선 승인 복구 상태를 유지한다.
- 고유 공개 음원3978개 HTTP/유형/내용 검사 통과, 그중 기존 생성음원744개의 알려진 파일 해시도 일치했다. MP3 파일을 교체하거나 새로 만들지 않았다. 전수 청음·모든 단어의 자연스러운 발음 품질 승인으로 표현하지 않는다.
- 새 모고20지문/278출현/네방향1112문항과 출처 태그를 보존했다. 모고 영영풀이278개는 지문 맥락에 맞춰 작성된 자료이며, 제공 스캔에서 나온 정의라고 잘못 표시하지 않는다.
- 취소는 이번324행 중 작업/키/출처/표시가 정확히 같은 행만 제거하는18번 SQL이다. 실제 취소는 하지 않았다. 앱 복구가 필요하면 직전541e546을 운영 환경에서 별도 빌드하고, 기존 데이터나 원음을 복구 명목으로 덮어쓰지 않는다.

## 최종 관찰과 인계

- T5: 등록15분40초 뒤 실제324응답 동일·전체5389최종선택 재통과·기존647보호,23보호영역 전체행해시 불변. 진행2/완료38/만료21 그대로다. 기존형용사100 실제 표시/강세/스피커는 첫 확인과 동일하고 브라우저경고/오류0이었다.
- 이번 운영 배포 ID로 제한한 최근30분 로그:200응답30건, Warning/Error/Fatal0, 해당 시간 범위에 추가 페이지 없음. 이는 관찰한 배포/시간의 결과이며 장기전체서비스 무오류 증명이 아니다.
- 별도 읽기 검토자가 운영324전체필드/417원본+6입력/양쪽23보호표를 재대조하고 공통선택기를 다시 실행해 불일치0을 확인했다. MP3 바이트 검사는 최초3978/744 검사 시점의 근거이고 운영 후에는 주소/변이·연결 보존을 검사했다. 전수 청음으로 표현하지 않는다.
- 문서 독립 검토에서 과거 원본 정책의 근거와 이번417파일 검산을 구분하고,400개 단위 조회가 순차이며 다른 발음 조회와 병렬이라는 설명을 정정했다. 코드/검사/설정849개 해시는 전부 동일하다. 설계 설명을 포함하는 지문만 APP9c6a0cd4/DEPLOY9fda2a25로 새로 계산했다. 과거 실행 결과를 새 실행인 것처럼 바꾸지 않았다.
- 마지막 변경은 지시서·검증 기록·인계·작업 기록뿐이며 로컬 문서 커밋으로 저장한다. 운영 실행코드 be313fd를 문서 저장 때문에 다시 빌드하지 않는다. 기존 사용자 단어장 탭을 유지하고 임시 점검 탭만 닫는다.
- 최종 문서 정정 후 독립 동등 변경검사35기록/612경로 오류0,780요청→34지문 계산·34개 새 재계산 전부일치,24.035초/종료0. 기록 묶음4dfcb5530c8830f4bb030102f98e95ed126519ab30da4117b2bc29fb61899189, 변경영향38256a78f4269c51624374e53a58dd3b2e53dc2d313928063357840498f23a7e, 구현1136파일 b09f5a2e0d208e8246b7433ff32453321d2ac937868a7d47d9acba6987e7b042. APP/DEPLOY완료지문 및 로컬 링크5개 일치. 이전34/609실행을 덮지 않고 별도 실제결과로 보존한다.
