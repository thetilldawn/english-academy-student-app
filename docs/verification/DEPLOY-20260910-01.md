# DEPLOY-20260910-01 단어배정 운영 반영 검증

사용자 2026-09-10 운영 배포 승인에 따라 A1~A6를 기존 사이트에 반영했다. 운영 배포와 학생 화면 확인은 완료했으며, 관리자 인증 후 배정 화면 확인은 남아 있다.

- T1: 구 화면/미지원 헤더에는 추가 수량 필드를 제거하고 새 화면에만 반환한다. 원본 미리보기·계획 서명·요청 본문·인증·개인 캐시는 보존한다. 관련 3파일21검사 통과. 구 커밋의 파서를 직접 실행한 검사는 아니며 응답 모양을 대조했다.
- T2: 최종 460파일, 3203검사 통과·기존 선택 검사1개 미실행. 최초 전체 실행에서 구 정책 검사2개와 작업 프로세스 종료3건을 발견했다. 구 예문 일정 제한과 인라인 오류 문자열 검사2개를 현재 A4/A6 정책에 맞췄으며, 중단된 미실행 항목까지 포함한 5파일215검사를 번들 Node24.19.0에서 다시 실행해 실패0으로 확인했다. 최초 완료 결과와 마지막 재검사를 파일별로 병합 검산했다.
- T3: 타입·수정 파일 ESLint·기능 지도와 변경 영향·UI 역할·전체42기록의 설계/변경 연결 검사 통과. 변경 연결 검사는 원래 검사 코드 그대로, 같은 실행 중 동일 파일 읽기만 메모리에 재사용했다. 검사 기준·조건은 생략하지 않았다.
- T4: Preview/Production 배포는 완료했다. T5/T6의 로그인 화면·운영 학생 화면 확인은 아래 실제 결과를 따르며, 관리자 인증 후 배정 미리보기·이력 확인은 미완료다. T8 전체 완료 관문도 이 확인이 끝난 뒤 실행한다.
- T7: 독립 범위 검토에서 누락/범위 밖 소스 변경 없음. 신규 migration·의존성·환경설정 변경 없음. 구 화면 strict 응답 호환 문제를 보완한 뒤 독립 재검토에서 추가 차단 결함 없음.

배포 파일은 각 APP의 마지막 책임 범위와 8개 배포 호환/회귀 파일로 나눠 커밋한다. 중간 커밋은 당시 구현 상태를 복원한 이력이 아니며, 최종 HEAD만 검증·배포한다. `tmp/`와 `docs/reviews/20260909_단어배정_점검/`는 로컬 전용이다. 영어 자료 구역의 원자료·예문 수정후보는 공개하지 않는다.

배포 직전 운영·복구 기준은 `be313fddd1c3bb32d60c8e59126ffd9124d73a1a`이다. 배포 전에 원격 main, 기존 Preview 브랜치와 Vercel 실제 운영 화면에서 같은 버전을 확인했다. 문제가 있으면 기존 Ready 배포로 되돌릴 수 있으며 이번 작업에는 DB 되돌리기가 필요 없다.

## 실제 배포 결과

- 실행 버전: `72de501a1e86f36fa770294138a4dca55db812b1`. Preview `dpl_7bKovWGCci2hAcDZy2HLV5edMVzC` / Production `dpl_FauoMzG8QQ67gDJk5iAarNcgHfWL`, 각각 Ready. GitHub 배포6355676380/6355718997도 success이다. 운영은2026-09-10 02:36:33KST 반영됐다.
- [운영 사이트](https://english-academy-student-app.vercel.app) / [고정 운영 배포](https://english-academy-student-6z0o2zyc1-thetilldawn-3859s-projects.vercel.app) / [Preview](https://english-academy-student-crl91fzw4-thetilldawn-3859s-projects.vercel.app). 기존 Preview와 운영 환경은 별도로 빌드했다. 새 DB migration·의존성·환경설정 변경은 없다.
- 실제 운영 테스트 학생: 공개 예정2·재시험/확인15·완료7·종료3 목록, 완료 시험의 학습 단어4항목과 새 “이 시험에 배정된 학습 단어” 안내 확인. 학생 화면/Preview 로그인 화면의 브라우저 오류0.
- 배포별 로그 02:36:44~02:37:53KST 관찰27건은200이고 Warning/Error/Fatal0이다. 장기 또는 전체 서비스 무오류로 확대하지 않는다. 실제 새 배정·응시·답안 제출은 없었다. 학생 화면이 자동 수행한 정상 세션 갱신과 알림 조회는200으로 확인했다.
- Vercel 빌드 경고는 기존 esbuild/unrs-resolver 설치 스크립트 허용 관련1묶음이다. 의존성/허용 설정을 변경하지 않았고 빌드는 성공했다. GitHub Preview public smoke는 기존 실행 조건으로 skipped이며 원격 자동 화면 검사 통과로 세지 않는다.

## 남은 실제 화면 확인

운영 배포는 완료됐다. Preview와 운영 모두 관리자 보호 경로는 로그인으로 이동하여, 인증 후 날짜·단어장·시간 전환/미리보기/배정 이력은 아직 실제 운영에서 확인하지 못했다. 해당 기능의 로컬 가짜 화면과 회귀 검사는 별도 통과 근거를 유지한다. 로그인 요청을 전달했으며 사용 가능한 관리자 세션이 생기면 같은 버전에서 읽기만 확인한다. 따라서 T5/T6는 미실행 부분이 남아 있고 전체 검증 완료 관문을 통과했다고 표시하지 않는다.

최종 코드/설계 입력1014개, 확인값 `7447d83f356fd5cf2aa6056ed786290d97e88bfca22468371484214b7e719ab2`. 후속 기록은 실행 코드를 바꾸지 않는 로컬 문서 변경이다.
