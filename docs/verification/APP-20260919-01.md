# 내역 목록 유지 — 수정·검사 결과

2026-09-19 / APP-20260919-01 / 로컬 구현·검증. 운영 배포는 하지 않았다.

## 바뀐 동작

- 현재 내역 목록을 단순 60초 만료 때문에 오류 안내로 바꾸던 경로를 수정했다. 상세 모달을 열고 닫아도 검색·더 본 행·목록 위치를 보존한다.
- 새로고침 버튼은 추가하지 않았다. 생성·수정·취소·숨김 성공 시 기존 변경 알림으로 갱신한다.
- 실제 조회 실패·401/403·계정 교체·페이지/탭 복귀 확인·거절된 더보기 커서 차단은 유지한다. 다른 기기의 변경을 실시간 수신하는 새 기능은 추가하지 않았다.

## 확인한 근거

| 검사 | 결과 |
| --- | --- |
| 오류 재현 | 새 검사 2개가 기존 코드에서 실패한 뒤 수정으로 통과 |
| 내역 검사 | 화면 13개 + 캐시 11개 = 24개 통과 |
| 실제 Next 모달 | 목록 60,001ms 대기 + 모달 120,001ms 대기 후 닫기 |
| 보존 결과 | 검색 `가짜`, 같은 입력·행 DOM, 스크롤 426→426, 추가 목록 요청 0, 오류 안내 0 |
| 가짜 자료 보호 | 지정 가짜 배정·두 학생만 상세 조회. 외부 요청·다른 식별자·생성 요청 거절. 관련 전체 129개 통과에 포함 |
| 독립 검토 | 수정·권한·변경 알림 및 가짜 상세 자료 보강 재검토, 미해결 발견사항 없음 |

전체 검사에서 3,557개 통과·기존 1개 미실행·기존 DB 검사 1개 시간 초과가 나왔다. 해당 DB 파일은 단독 재실행에서 60개 모두 통과했다. 최종 관련 검사 129개, 타입·전체 lint/마지막 검사 파일 lint·기능 지도·UI 구조·요청 캐시 검사와 격리된 37페이지 실행용 빌드도 통과했다. 작업 기록의 변경 연결·완료 검사는 별도 결과를 확인했다.

## 재확인 위치와 경계

- [요구·설계·검사 기록](../../architecture/work-records/records/APP-20260919-01.json): 기준 실행 `83d874e40060cdd8e83c0c75b8a7805fce8e0493`, 입력 232파일 확인값 포함.
- 재현: `node scripts/local-admin-baseline.mjs --history-cache --quiz-feedback --webpack` 후 `node .codex-tmp/verify-behavior-20260919.mjs`. 가짜 서버 전용이며 운영 URL에 실행하지 않는다.
- 화면: `.codex-tmp/APP-20260919-history-modal.png`, `.codex-tmp/APP-20260919-history-return.png`. 메인이 실제 이미지를 확인했다. 더보기 행 보존은 컴포넌트 검사에서 확인했고, 실제 모달 검사는 가짜 2행 기준이다.
- 브라우저 검사 스크립트 SHA-256: `223860200dbc63ca5a61d7c88f68ba00f17a03960314f8196750a9e9e5884266`.
- 실제 Next 슬롯이 아닌 모의 모달 검사만으로 완료 처리하지 않았다. 가짜 상세 응답이 없던 검사 환경을 보강해 실제 모달까지 확인했다.
- 첫 검사 서버의 종료 권한 부족은 해당 프로세스만 확인·종료하고 기존 빌드를 복원했다. 최종 검사 서버는 정상 종료됐고 검사 포트 3037/3038/3040 모두 닫힘·기존 빌드 복원까지 확인했다.
- DB·실제 학생·원자료·새 배포 변경 0. 현재 작업 폴더의 기존 미커밋 문서와 다른 작업 결과를 보존했다.
