# APP-20260909-02 검증 기록

## 결과
로컬 구현·검사 완료. 자료 원본과 음원은 바꾸지 않고 324개 원본 표기/강세를 정확한 단어·품사·음원에만 연결한다. 운영 반영 완료는 별도 DEPLOY-20260909-02 증거로 판단한다.

- 전체 단위/통합: 450파일, 3,088통과, 선택 검사1개 미실행. 선택 항목은 SCHOOL_PRONUNCIATION_RELEASE_DIRECTORY를 별도 지정해야 하는 이전 전체 릴리스 가져오기 검사이며 통과 수에 넣지 않았다.
- 실제 최종 DB 구조의 새 교정 계약26개 + 고정문항 계약37개 통과. 사용자 지정 v2/v3 보존, 과거 문항과 현재 단어 불일치, 권한·입력상한·해시·품사·음원 변조 차단.
- 타입/lint/기능지도·UI구조·React 요청캐시 통과. 로컬 실제 환경 파일을 복사하지 않은 격리 Next16.2.12 빌드34페이지 통과. 외부 fetch 차단, 가짜 값만 사용. 중첩 격리폴더 잠금파일 경고는 일반 운영 빌드 설정 변경으로 해결하지 않았다.
- 변경 연결 검사34기록/609파일 오류0. 기존 검사기는 지문을748회 반복해 오래 걸려 중단했다. 조건을 생략하지 않은 메모리 실행에서32기록으로 실행 내 계산을 재사용하고, 끝에32개를 새로 계산해 동일함을 확인했다(21.924초). 검사기 코드 변경 없음.
- 완료 기록·소유권 보완 뒤 동일 전체 검사를 한 번 더 통과했다:34기록/609경로 오류0,764회 요청→33회 계산 및33개 새 지문 재검산 일치,21.916초. 구현1,136파일 묶음01f61c96b190b82a0dc523921a2e509ba4974fa1ae0c58c03dd37b5eecffa39a. 검사기의 판단 조건은 변경하지 않았고 새 검사 파일을 기존 인벤토리에 등록한1줄만 추가했다.
- 독립 원본/코드/거래 검토 후 결함0. 자료 제한 거래18검사 통과.

## 보호와 복구
기존 승인647개, 원본 사전/PDF/추출값/MP3 보존. 과거 시험·결과·단어보기·영어선택지는 같은 선택기를 사용한다. 실제 문항 표제어/선택 음원과 다르면 복원을 적용하지 않는다. 개인 조회는 기존 인증/no-store를 유지하며 교정 조회는 기존 서버 요청 내부에서400개씩 병렬 처리한다. 교정 조회 실패는 기존 음원/시험을 지우지 않는다.

별도 private 표시 교정표는 단어 원본 행과 연결된다. 향후 단어 자체를 물리 삭제할 때 해당 교정행 정리가 필요하지만 현행 학생/배정 삭제는 상태 변경이므로 영향 없다.

자료 묶음 SHA256: 5ae15f29d88e4079e0c577e591043298ecc0fb79d2492b20bdcfbf23bc9f55c7.
반영 SQL SHA256: 24c4c31b70c191883ab3b8c3ca6ee4a4e7e44db269fe373786c1b0fc9eb3d598.
되돌리기 SQL SHA256: bff9e4f456589e09d3d9b9d3bfc585259ea92d1c65e78da47739b1fc72cb6825.
DB 변경 SHA256: ba3b91b211da24073d11b4857f5a0f868410fd5f19c6f8b85c514728d7d204a8.

## 한계
3,978개 공개 MP3 검사와744개 기존 해시 일치는 연결/파일 검증이다. 새 발음 생성, 전수 청음 승인, 공통사전 승격은 하지 않았다. 근거 없는4,049개는 기존 연결을 보존하고 발음을 임의로 새로 쓰지 않았다. Preview/운영 실제 조회·배포·학생보호 재대조는 아직 남아 있다.

완료 관문에서 새 단위검사의 소유권과 DB 검사의 실제 흐름을 정정하고, 구조72개·작업기록113개 및 지도/lint를 다시 통과했다. 앱 실행 코드의 추가 변경은 없다.

EOF 빈줄 정리 후 관련 선택기46검사를 재실행했다. 실행 로직 변경 없음.

코드 근거 a9f691e395fa33ea351084e48cc3ea5b978b6adf09b5655e806c0206441fb30b, 849개 파일. 검증 기록 자체는 순환 검사 대상에서 제외한다.
