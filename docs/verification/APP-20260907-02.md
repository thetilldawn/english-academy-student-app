# APP-20260907-02 검증 기록

[지시서](../work-orders/APP-20260907-02.md) · [R/D/T](../../architecture/work-records/records/APP-20260907-02.json)

입력 기준8db511e. 단어 수는 현재미리보기 reference만 받아 미확정시 빈칸이었고 focus가0을저장했다. 범위는 이름만있는가로버튼/범위개수요약이었으며 모바일화살표는숨겼다. 코드독립검토로5가지회귀를 확인했다. 수록·출제가능·희망수 구분, 현재요청만사용, 상태별안내와카드를 보완한다.

## 로컬 결과

- 최종 전체 426파일, 2,760검사 통과(118.76초). 기존 외부 원본 선택 검사 1건 skipped는 통과에 포함하지 않는다.
- 실제 단일/일괄 연결 23검사 통과. 새 2건은 수록20/가능16 구분, 미확정focus 무변경,503 원문 숨김, 재시도 성공, 희망8 보존, 범위해제 뒤 늦은 응답 무시를 실제 제어기와 UI로 확인했다. 요청 경계만 가짜이며 실제 배정 저장은0건이다.
- 타입·lint·기능지도·UI 구조·요청 캐시·설계와 변경 연결20건·최종 Next16.2.12 최적화빌드(정적33페이지) 통과. 독립 읽기 검토 P1/P2 지적0, diff 공백 검사 통과.
- 먼저 작성한 회귀 검사에서 개수/대기/초점/터치 문제8건을 재현했다. 통합검사에서 503 내부 원문 노출2건, 전체검사에서 허용하지 않은 글자 굵기1건을 잡아 수정한 후 전체를 다시 실행했다. 누락된 영향 흐름도 실제 지도에 맞췄으며 보호 검사는 완화하지 않았다.

## 실제 로컬 화면

- 최종 새 빌드로 앱브라우저 가짜 계정에서320/390/1280px를 확인했다. 짧은 범위2열, 긴 범위전체행, 선택체크, 2범위/수록40개 요약,16px 글자, 키보드 좌우 이동, 닫기 고정과 검사 입력 폐기를 확인했다.
-320px 첫 검사에서 짧은 이름까지 두 줄로 꺾여 좁은 화면의 안쪽 여백/체크 표시만 축소했다. 최종 재빌드 뒤 DAY1과 수록20개가 한 줄씩 표시된다. 카드 높이는 내용에 맞춰 늘어난다.
- 미확정 수량 focus는 빈값/전체사용을 보존하고 직접20 입력 시 수량20/직접모드로 바뀐다. 조건 미완성 안내는 정상0과 다르다. 콘솔 경고·오류0. 기존 사용자 탭/작성값은 변경하지 않았다.
- 로컬 가짜 서버는 쓰기와 미등록 API를 차단한다. 정상 가능수/재시도 성공은 실제 조립 단위검사에서 먼저 확인했으며 호스팅에서는 아래 별도 읽기 검증을 한다.
- 현재 관련 입력576개 지문: `04e06b04c6dce3e1ab32180bf08cacb858d88eac99913c8193d8b867164c5763`. 역할 등록 파일 별도 SHA-256: `6d7e3b43730787bc91ca903c5596af75344a79da2729fd92a8ccdc31ccebf47b`.

## 배포 경계

이 시점은 로컬 검증 완료이며 Preview/Production 미완료다. 기존 GitHub 자동 배포를 사용하고 환경/DB/보호 설정은 변경하지 않는다. Vercel 연결의 팀 읽기는 되지만 프로젝트 목록은 비어 있어 배포 상태는 GitHub의 커밋 연결 기록과 실제 앱브라우저를 사용한다. 전체 서비스 오류율까지 검증한 것으로 표현하지 않는다.
