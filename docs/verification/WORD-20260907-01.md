# WORD-20260907-01 검증 기록

## 로컬 검사와 적용 전 경계

- 설계관문·변경연결 통과, 사용자 미결정0. 수정 전 활성 발음 경로가 승인 표기를 건너뛰는 회귀를 재현했다. 독립검토에서 legacy Map의 자동값까지 확대될 위험을 찾아, 기존 우선순위는 보존하고 새 entry승인만 최종합성하도록 좁혔다.
- 전체430파일/2847통과/1선택외부자료 미실행(기존 skip). 이 작업 full-schema17 + 공통/조회/학습44 포함. 타입/lint/기능지도/구조/React요청cache 검사 통과. Next16.2.12 격리 fake-read-only-v1 빌드33경로 통과. 실제 DB자료를 가짜검사에 연결하지 않음.
- 자료영역 교정거래 로컬6사례 통과: 정확2행·동일재실행 불변·원자료 미일치 거절·기존승인충돌 보존·후속runtime증거 미일치 전체롤백·범위확대 거절. 이 검사는 격리된 거래제어 검사이며 실제 모든 migration을 적용한17사례와 구분.
- 자료 영역에 1009행 원본/현재사전/hash/품사/음원/segments/분류를 JSONL11묶음으로 보존. 사전표기차이115개는 별도 재검토목록. 자동984/기존승인25, 정확784/없음225/동일100/상이115; 기존조사·독립검산과일치. 범위 밖 현재본 YAML오류1건은 별도 기록하고 수정하지않음.
- 실제 사용자 교정자료는 자료영역 v7에만 저장하며 앱Git에 원천단어/학생자료를 넣지 않는다. 자료 v7의 01~07 파일 SHA를 별도적용기록에 결속한다.
- 변경 SQL: `supabase/migrations/20260907121138_add_entry_approved_korean_pronunciation.sql`, SHA256 `71c2d28f628f8b5510b272c7b995a0f0356dcb094dc195e9216c98b67a63f0fb`. 서버전용 읽기함수2개만 추가, 기존표/권한/업무함수 변경0. 서비스만 public wrapper 실행 가능, private 직접 실행 불가.
- 별도 DML: 기존승인표에 사용자 지시2행만 추가. source hash=immutable identity문서 hash(원음파일hash와구분). 승인과 원문/발음 연결이2행 모두 일치해야 거래성공. 기존승인 덮어쓰기0·다른교정/음원/학생/사전현재본 수정0.
- 적용전 양쪽18표(학생/시험/문항/점수/원문snapshot/발음identity·binding·release/단어entry) 지문을 로컬 ignored 근거에 보존했다.
- 공개음원3주소 모두HTTP200/audio-mpeg. Webster4842bytes hash b4ea6324ccb61dc8250ae2376a4f2d44bbee9907f68acebdf16ab295d40240ed, Google 양쪽6144bytes hash930e9f61c131c08899e39802295e63650f2737964b653d90a8fc28719a45b695. 전수청음/억양교정은 이작업범위아님.

## 배포와 복구 계획

1. Preview 한 SQL 적용→history/권한/읽기결과 검증→같은 두 교정행 거래→18표/음원/readback→검사된 commit을 Preview 배포.
2. 동일 서버 경로 및 필요한 앱브라우저 읽기검사 후 Production 같은 migration/교정거래→같은 code의 별도 Production build. Preview의 환경변수를 Production으로 승격하지 않음.
3. 소스/POS/원음 미일치·조회실패·권한확대·실학생 변화가 패치 때문이면 중지. 새교정행이 문제면 작업ID/사전ID/variant/hash/표기 일치 두행만 조건부승인취소. 실제 시험/원음/원문 snapshot을 되돌리지 않음.
4. 배포후15분에 보호지문/표기/공개음원/접근을 다시확인. 전체서비스 오류율/물리모바일/전수청음 미측정은 별도로남기고 통과라고표현하지 않음.

## Preview 데이터 적용

- 2026-09-07 21:41 KST 읽기 함수 추가 성공. 원격 migration `20260907124101`/`add_entry_approved_korean_pronunciation`; 로컬 SQL 해시는 위와 동일하다.
- 익명/일반 로그인 실행 불가, public wrapper는 service만 가능, private 직접 실행 불가를 실제 권한으로 확인했다.
- 정확 두행 거래를 취소 가능한 거래에서 먼저 실행해 표기/강세/출처 일치를 확인하고 rollback 뒤 저장0건을 확인했다. 이후 같은 거래로 정확2행을 반영했다.
- 읽기 결과2/2 지정값, 기존 Webster/Google 원음 식별자 일치. 보호18표 행수/전체내용 SHA 모두 불변. 보안 점검 기존89항목/이후89항목, 신규0. 기존 경고를 이 작업에서 해결한 것으로 표시하지 않는다.
- 독립 읽기 검토에서 Preview 적용을 막을 결함0. Preview 코드 배포·화면 확인 및 Production은 아래에 실행 결과를 추가한다.

## 양쪽 배포와 실제 확인

- 실행 코드 `aaa4b28d25dbf88694311268fe8f7b50ca6ce3a3`. Preview 배포6308956251은12:44:55Z, Production 배포6309054074는12:50:29Z success. 같은 소스를 환경별 별도 빌드했다. 운영 주소 https://english-academy-student-app.vercel.app .
- Production migration `20260907124802`/동일 SQL SHA. 정확두행 읽기 결과·강세·원음 식별자 일치, 함수 권한도 Preview와 동일. 보안점검 기존91/이후91·신규0. 기존 경고를 해소한 것으로 주장하지 않음.
- 운영의 최초 조사 이후 실제 사용자 배정/응시가 진행되어11표 지문이 달라졌다. 패치 전 발생한 변화이며 되돌리지 않았다. 교정 거래 **직전/직후** 새 기준의18표 지문은 모두 같았다. 진행응시2건에도 쓰기0. 최초값과 새기준은 별도 보존.
- 실제 앱브라우저: Preview 관리자 로그인/개요/학생 조회·콘솔오류0. 기존 Preview 테스트학생 두 계정에는 교정 대상 배정이 없으므로 새 배정을 만들지 않았고, 대상2개 표기는 서버RPC readback으로 검증했다.
- 운영 기존 테스트학생만 접속해 일반 단어장80개에서 지정 숙어, 영영풀이16개와 예문39개에서 지정 형용사의 표기와 강세를 확인했다. 스피커2개 클릭 후 콘솔오류0, 예문은 빈칸 없이 유지. 기존 완료 관리자 내역 조회 정상. 시험 시작/재시험 시작/답 제출/새 배정 저장0; 테스트 인증 세션 생성/종료는 별도다.
- 세 단어장500/320/189행의 `(source_row,entry_row_sha256,binding_content_sha256,identity_id,identity_content_sha256)`를 순서대로 묶은 SHA가 로컬/Preview/Production 모두 일치했다. 각각95b2ef437ebe45e9601a3a094375bca94d3f2b0c342cb35a280bf6affe7e98fb / 9c6733df76a1cb318b09ecfe992446759393f807bc3ad7d6f99fe2522c7199ed / cc36632d5f4c84ac7fc7d4a3a065ca634b5ef79ef49528d42d2c1e6e87b2bb3c.
- 원격 Preview public smoke34124070579는 운영 배포 대상이라 skipped. 로컬 검사와 실제 화면 검사를 대신 실행한 결과로 구분하며 CI 전부 통과로 표현하지 않는다. 전체서비스 오류율·모든 영어선택지의 실제 응시·전수청음·물리모바일은 미검증. 공통 선택지/결과 연결은 로컬 실제 서비스/순수선택기 회귀 검사로 확인했다.

## Ready 이후 재확인

- 13:10Z 이후(운영 Ready12:50:29Z보다15분 이상) 양쪽 표시2/2와 기존 공개음원3개 HTTP200/audio-mpeg/크기/SHA 동일을 재확인했다. 13:14Z GitHub 재조회에서도 양쪽 같은 실행 코드 success 유지.
- Preview 보호18표 동일. 운영17표 동일, assignment_students만22:00 마감 도달에 따른 missed_at 2건이 추가됐다. 두 행 모두 마감이 도달했으며, 읽기 계산에서 이 두 새 missed_at만 null로 간주한 전체SHA `e6805e5bf1240d5f4fdf9677dc3c0f68d05aa9bbd5e022016e0ec1a38c7a0b26`은 거래 직전과 정확히 같다. 실제 DB 값을 되돌리지 않았고 나머지 필드/행 변화0. 시험 답안/점수/음원 변경0.
- 검사 중 Codex 실행이 끊겼지만 기록 파일과 배포는 보존됐다. 재시작 뒤 관련525파일 지문 `55a3fcc4579233d3ac963d0995ff5cc30c27ec09c658e643adbf872523b4d36e`가 재시작 전과 일치했다. 중단된 변경검사는 통과로 간주하지 않고 재실행한다.
- 테스트 학생 접속 종료 확인, 검사 탭 정리. 운영/Preview DB 교정·배포 근거와 자료 전체 연결은 v7 적용 기록에 보존한다. 독립 최종 기록 검토도 두행/보호값/미검증 구분의 불일치0.

## 최종 기록 연결과 완료 확인

- 중단된 설계 변경검사는 재실행해24기록 통과했다. 완료검사에서 발견한 작업 범위/소유권 등록 누락을 정정했다. 실제 변경하지 않은 배정 수정 흐름을 제외하고 새 서비스 검사는 기존 구조검사 소유권 목록에 명시적으로 추가했으며, 결과 조회의 실제 SQL/검사 연결을 지도에 보완했다. 검사 규칙을 완화하거나 앱 동작을 바꾸지 않았다.
- 13:20:31Z 최종 관련5파일173검사, 기능지도, 변경 검사기 lint, UI구조 통과. 독립 검토에서도 검사 우회나 업무 코드 변경0. 전체2847검사·타입·격리 빌드는 배포 전 실행 근거이고, 마지막 기록 수정 후 전체를 다시 돌린 것으로 표현하지 않는다.
- 현재 실제 변경 범위와 검사 입력356개 지문은 `5fea936be7be8994e96df10ae3c26421e8b957ec84439d212950e1ffb488b7bd`. 이전525개 지문은 중단 전후 복구 확인의 당시 근거로 보존한다. 범위에서 무관한 배정 수정 흐름을 제거해 입력 수가 달라졌으며, 기존 실행 코드가 바뀐 것은 아니다.
- `check-work-records --phase complete --work WORD-20260907-01` 통과. 자동검사는 누락/불일치 검출일 뿐 설계·보안 전체의 완전성을 증명하지 않는다. 최종 인계/지도/검사 등록은 로컬 후속 커밋으로 저장하며 운영 실행 SHA는 계속 `aaa4b28`이다.
- Codex 중단 진단은 읽기 전용이었다. 오늘19:32:26과22:01:34 KST 모두 마지막 앱브라우저 탭 수가0이 된 직후 데스크톱 로그가 끊겼고,22:06:53에 실행 프로세스가 다시 시작됐다. 반복 화면 크기 알림 오류는 있었으나 종료 원인 코드·Windows 충돌 기록이 없어 탭 정리 결함은 의심이지 확정이 아니다. 재현을 위해 마지막 탭을 다시 닫지 않으며 설정 초기화/재설치/프로세스 종료는 하지 않았다.
