# 답 선택 0.15초 보완 — 2026-09-20

APP-20260919-02의 같은 기능 보완이다. 이전 300ms 검증은 [9월19일 기록](APP-20260919-02.md)에 그대로 남긴다.

- 마지막 유효 입력 후 150ms에 최종 답 하나만 보낸다. 그 전에 같은 답 또는 다른 답을 누르면 마지막 클릭부터 다시 센다.
- 선택 강조는 즉시 표시한다. 기존 정오답·음성·결과 표시·서버 제한시간은 바꾸지 않았다.
- 관련 129개 검사 통과: 시험 72, 내역 24, 가짜 자료 보호 33. 149ms 미전송·150ms 1회, 재시험, 연타, 마감3종, 느린 동기화, 이탈·복구, 음성을 포함한다.
- 타입·전체 lint/최종 주석 파일 lint, 기능 지도·변경 영향, UI 구조, 요청 캐시 검사 통과. 격리 빌드 37페이지 생성 성공.
- 실제 Next 가짜 시험: 재선택과 같은 답 연타 모두 149ms까지 전송0, 150ms에 마지막 답1회. 서로 다른 두 문항에 각각 index2/index1만 전송.
- 실제 내역: 목록60,001ms+모달120,001ms 뒤 닫아도 검색·행·스크롤426→426 보존, 추가목록조회0·오류안내0.
- 브라우저 예외0. 가짜 검사 서버가 의도적으로 막은 알림403 네 건은 실제 결함과 구분했다.
- 독립 검토에서 동작 결함 없음. 기록/주석의 옛300ms 표기2곳을 정정했다.
- 검사 서버 정상 종료,3037/3038/3040 열림0, 이전 `.next` 복원 및 임시 보관 경로 해제 확인.
- 전체 회귀: 488파일,3,559개 통과·기존1개 미실행. 515.66초 실행했고 실패는0이다. 변경 기록 전체 연결 검사는 별도 배포 결과에 남긴다.

## 재현 근거

`node scripts/local-admin-baseline.mjs --history-cache --quiz-feedback --webpack` 실행 후
`node .codex-tmp/verify-behavior-20260920.mjs`로 확인했다. 외부 연결을 차단한 가짜 자료 전용이다.
스크린샷 `.codex-tmp/APP-20260920-last-selection.png`의 선택 표시를 메인이 직접 확인했다.
브라우저 검사 스크립트 SHA-256: `2df6674c6a509059b474c9ea6140342dfa340fb3a52f84c1eaee62b9217176b5`.
이전300ms 스크립트와 이미지는 덮어쓰지 않았다.

운영 반영은 [별도 배포 지시](../work-orders/DEPLOY-20260920-01.md)와 결과에서 확인한다.
실제 학생 시험 시작·답 제출·DB·사전·음원 원본 변경은0이다.
