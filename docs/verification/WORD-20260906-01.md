# 학교 발음 연결 검증

## 운영 반영 최종 결과

- 최종 기록 검사에서 빠진 발음 계약 검사 2개를 정확한 소유 목록과 검사 인벤토리에 추가했다. 검사 기준을 완화하지 않았다. 실행 코드는 운영8c7bb64와 동일하다. 등록 변경 후 21:07 KST 전체422파일2,647검사(203.26초), 별도136검사·타입·lint·지도/구조·요청cache·최적화빌드 재통과. 최종 입력303파일 지문 `672232c0890577fce2dee4a6d7db8b2d93e5ed320c0a0ab96da85a5854c78142`. 아래 지문은 이전 실행 이력으로 보존한다. 등록 수정 중 과거 지문이 완료 선언과 맞지 않아 빌드 전 검사에서 차단됐으며, 완료 선언을 설계 상태로 되돌려 재검사한 후 현재 근거로 확정했다.

- 코드 `8c7bb64ce068b57a73538c49e489640866cdd270`. Preview6292525570(11:44:51 UTC)·Production6292579277(11:51:00 UTC) success. 기존 Git 연결을 사용했고 새 팀/요금제/외부 서비스는 만들지 않았다.
- 양 환경 6개 release active, 1,509행 bad/unbound0. 양쪽 학생 보호11표 내용·행수 동일. 운영 기존 발음 identity3,252개 내용 SHA `b42efd093f263fedf37ecd0e34c3568d809ed0d52377b6ec971b0f49ee83ba54` 동일. 학생 답/점수/시험 시작 변경0.
- 운영 기존 테스트학생 예문39단어 직접 페이지와 일반120단어 모달에서 한글 발음/강세·스피커·고정상단 확인. `thousands of` 숙어와 공식음원 버튼 사용 뒤 경고/오류0. 실제 전 음원 청취나 브라우저 음향 측정은 하지 않았다. 제공된 읽기 전용 브라우저는 performance API를 노출하지 않아 브라우저 전송량 측정은 제외했다.
- 기존 스냅샷 검사: 학교383문항·영어 선택지 ID383/383 완전. 저장된 재생 가능 음원0, 선택지1,280개도 동일하므로 활성 자료 우선 분기를 사용한다. 정답/학생 식별 내용을 반환하지 않았다.
- 마지막 EOF 빈 줄 정리 뒤 영향2파일37검사 통과, 최종 지문303파일 `1c65b9dd65ced87e6af75087d620f75b8d83e509c77dbe40d4ce3b4feac8ab90`. 아래 19ea 지문은 정리 전 이력이다. Vercel MCP 상세 접근404는 재연결하지 않고 공개 Git 배포 상태와 실제 앱으로 확인했다. 서버 로그 전체 감사로 표현하지 않는다.
- 새 학교 연결을 중단할 때만 해당6개 release를 retired로 되돌린다. 이전 운영 코드a32dc35, 기존 VOCA/MP3/identity는 보존한다. 자동 공개는 별도 APP14이며 이 작업의 완료로 합치지 않는다.

## 이전 단계별 기록

- 승인된 6개 source SHA·1,509행만 학교 가져오기 경로에서 허용한다. 옛 VOCA 가져오기는 여전히 VOCA 범위만 허용한다.
- 저장 경로 판정은 공용 순수 함수, 자료 검증은 가져오기 계약, 등록/검증/활성화는 기존 DB 절차를 재사용한다. 학생 화면별 새 발음 구현이나 데이터 조회 복제 없음.
- 기존 스냅샷 음원·표시 우선. 정확히 같은 음원/variant인 경우에만 빠진 표시를 보강한다. 다른 음원 바꿔치기 없음.
- 로컬 전체 422파일 2,643검사 통과, 이후 0 TTS 무변경 처리와 1,000개 이후 재사용/다른 SHA 거절 검사 추가. 전용 18검사·타입/lint·지도/구조·변경 연결·요청 캐시 통과. 마지막 전체 통합 실행 결과는 후속 기록한다.
- 실제 자료를 사용한 격리 DB 검증: 1,509개 연결과 기존 661개 identity 내용 보존, 마지막 스키마 파일 5검사 통과. 원자료는 앱 밖에서 읽으며 CI 필수 입력으로 만들지 않는다.
- 가짜 로컬 설정으로 Next16.2.12 최적화 빌드와 33개 정적 페이지 생성 성공. 첫 주소 미설정 실패는 별도 이력이다.
- Preview DB에서 6개 active release, bad/unbound 0. 학생 보호 11표 전후 동일. 공식 MP3 1,004 URL, 최종 TTS 339파일은 각각 SHA/형식/크기 확인. 27개 등록본의 생성 영수증과 최종 선택은 분리했다.
- 실제 전 음원 청취·음향 강세 측정은 미실행이며 파일 검증으로 대체해 통과를 주장하지 않는다. 기존 공용 표시/재생 회귀와 새 저장 경로 파싱 근거를 사용한다.
- 작업 지문: 303개 입력 `19ea63af37bf11616b002c683f45d8ec3426b5a3db23ddf500a0b77718fe808a`. Git 배포/Production 반영 전 단계이며 완료 관문은 아직 통과시키지 않는다.

## 마지막 로컬 통합

- 실제 6개 자료 검사 환경을 포함해 422파일 **2,647개 전체 통과**(2026-09-06 20:38 KST 시작, 208.46초). 실패/건너뜀 0. 자료 입력은 로컬 선택 검사이며 원자료가 없는 CI의 필수 외부 입력으로 만들지 않는다.
- 0 TTS 적용은 자격정보 없이 정상 무변경, 첫 페이지 뒤 같은 음원 재사용/다른 SHA 거절 회귀를 포함한다. 최종 코드 타입·전체 lint·구조·최적화 빌드 통과.
