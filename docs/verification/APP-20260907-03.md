# APP-20260907-03 검증

## 2026-09-07 Preview에서 발견한 조회 누락 보완

- 최종 실행 코드 `fbc79e4ee4c78f71347f76b24d806c95f4590129`. Preview6306347710 success(10:03:22Z), Production6306467363 success(10:10:58Z). 별도 운영 빌드로 반영했으며 Preview 환경을 운영에 alias하지 않았다.
- 실제 양쪽 앱브라우저의 테스트 학생 작성창에서 수록601/전체601/회차당500,100개씩 기본7회→요일1/남음6/배정100/남은단어501→해제7 확인. Preview 영어→뜻과 혼합, 운영 기본 혼합을 확인했다. 배정 저장0·초안 폐기·1280px 가로넘침0·콘솔 오류0. 기존390/1280 로컬 UI 파일은19c0794와 Git diff0이며 원격390 확인으로 바꾸어 말하지 않는다.
- 최종 관련입력582개 지문 `3faaaf8cc0d70a60b58e6fa146ca544589d73af06165561ebfba28904ff89afb`, 완료 관문 통과. 기록 환경값의 대소문자 오류로 한 차례 완료 검사가 실패했으나 허용값 preview로 고치고 별도 재실행 통과 후 동일 커밋만 운영에 반영했다.
- 배포 직후와 운영 Ready15분 후(10:26Z) 모두 양쪽 보호13표 행수/내용 지문 동일. 원시 근거 `.codex-tmp/apps0703-05-*-before.json`·`*-after.json`·`*-after15.json`. 운영 개요 제목 배지30개·가로넘침0·열린 검사창0·콘솔 경고/오류0 재확인. 운영용 Preview smoke는 대상 환경이 아니므로 skipped이며 통과에 포함하지 않는다. 전체 서비스 오류율·물리 모바일·실제 두 연결 경쟁은 미검증이다.

- 최초 구현의 로컬 검사는 이미 불러온601개 후보를 사용하여 외부 응답 상한을 놓쳤다. 실제19c0794 Preview에서는 수록601/출제가능500, 방향 필터 뒤 회차486 또는498로 재현됐다. 아래 이전 로컬 통과를 원격601개 검증 완료로 해석하지 않는다.
- Preview와 Production의 출처601개는 같고, 활성 출제 조회는 단어당 두 방향으로1202행이다. 실제 Supabase 클라이언트+가짜 HTTP1000행 제한에서 수정 전12검사 중7실패로 재현했다.
- RPC를 ID·방향 순서로 페이지 조회하고 수신 행 수만큼 이동해 빈 마지막 페이지까지 확인한다. 반복/역순/누락형 응답·401/403·중간 실패는 부분 성공이나 기존 자료 대체로 처리하지 않는다. 정상 첫0행만 기존 조회를 사용한다. 단일·일괄·수정·오답·연속배정의 공통 읽기 경로에 적용한다.
- T7 최종12검사 통과. 전체2820개 중2819통과·실패0·기존 외부자료 선택1건 skipped. 새 검사 파일의 타입 보완 후 전용12검사·타입 검사를 재실행했다. lint·기능 지도/구조·요청별 캐시·작업기록 변경 연결·Next16.2.12 격리 최적화빌드33경로 통과. 읽기 전용 독립검토 추가 확정결함0.
- 원시 결과 `.codex-tmp/app0703-pagination-tests.json`. DB상한/권한/캐시정책·학생 배정·응시·음원 변경0. 당시 로컬 검증만 끝난 시점에는 새 Preview의 실제601개 및 초기7회 확인이 남아 설계 관문을 유지했다. 이후 원격 확인·완료 관문 결과는 위 최종 근거와 구분한다.

## 이전 구현 당시 근거

기준 bcaac92f3e64bef160905f23aaae55a4b10ca85b. 로컬 구현·검사 완료, Preview/Production 반영은 아직 하지 않았다. DB/실학생 배정/응시/답/음원 변경0.

## 결과

- R1: 전체 후보601개와 회차당500개를 별도로 전달·표시한다. 영영풀이/예문 전체 모드도 기존 DB상한500을 지키며 나머지101을 보존한다.
- R2: 단어 수 입력은 모드 버튼 바로 아래. 단일/일괄 모두 날짜 선택 전 가능한 회차를 표시하고 날짜 변경 중 수량 집계만 유지한다. 날짜 미선택 계산용 계획은 실제 저장/가짜 일정 표시에서 차단한다.
- R3: 현재 학생·범위 한 건의 집계만 보관한다. 실제 저장은 최신 미리보기 검증을 그대로 사용한다. 인증/조회 실패, 범위·학생 변경, 갱신과 저장 시 폐기한다.

## 검사 근거

- T1~T3: 실제 작성창→계획→요청 스키마까지 연결한 단일/일괄 검사 포함, 전체2793개 중2792통과·실패0. 기존 외부 학교발음 자료 선택검사1건 skipped는 통과로 계산하지 않았다.
- T4: 기능 지도/구조·작업기록 변경 연결·요청별 캐시 검사 통과. 읽기 전용 독립검토의 463입력값 불일치, 오류 숨김, 날짜0 조회계약, 영영풀이/예문500상한 지적을 모두 보완하고 최종 재검토에서 추가 확정결함0.
- T5: 실제 로컬 앱브라우저390×844/1280×900. 가짜5범위100개를 2단위씩 묶어 날짜0 가능3회→월선택1회/남음2회→해제3회 확인. 단어 수 입력 바로 아래·PC직접입력100 보존. 긴 회차 안내는 다음 줄로 내려가며 요일 라벨과 고정 닫기/하단 버튼 보존.
- 로컬 읽기 전용 도구는 미리보기 API를 막는다. 기본403이 인증실패로 창을 숨기는 제약을 확인했으며, 명시 --capacity-read-error에서만 같은 출처의 미리보기 조회에503을 반환해 오류/입력 보존을 검사했다. 성공응답·DB쓰기·인증 우회는 추가하지 않았다. 정상601/500 수치와 단일/일괄 날짜0 저장 차단은 T3 실제 컴포넌트 통합검사 근거이며 운영 브라우저 확인으로 표현하지 않는다.
- T6: 전체 lint, TypeScript, 구조검사, Next16.2.12 최적화 빌드33정적 경로 통과. Node24.19.0/React19.2.8. 원시 검사 결과는 ignored .codex-tmp/app0703-tests-verified.json. 시각 검사 뒤 보호 도구29검사도 포함해 최종 전체 재실행했다.

최종 관련입력580개 SHA 결속: `2ccc89f0d1009c2d8ecc752fa267924eb467e18defdf5eb39befc47253ae7836`. 과거 결과는 당시 코드 근거로 보존하며 이번 결과에 합치지 않는다. 실제DB 비용/원격 브라우저/물리 모바일 장치는 미검증이다.
