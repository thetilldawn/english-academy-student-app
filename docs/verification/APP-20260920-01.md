# 단어장 탐색과 템플릿 편집 검증 기록

2026-09-20 외부 반영 결과: [DEPLOY-20260920-02](DEPLOY-20260920-02.md)에 실제 MCP 등록·검토용5개 생성·기존32영역 보존·동일1917ec7 운영 Ready와 검증 한계를 기록했다. 아래 APP 기록은 로컬 구현 근거이며, 이전 외부 미반영 표시는 각 검사 당시의 이력이다.

## MCP 등록과 전체 범위 생성 보완: D13~D25

실제 관리 연결은 postgres 세션과 프로젝트·원고·명령 전체 확인값에 묶인 허용 목록을 사용한다. 사용자 로그인/UUID를 가장하지 않는다. 기존 원사전·학교 원문·학생 기록을 변경하지 않고 별도 원범위/틀/구성에만 기록한다.

- 저장량이 큰 범위는 한 번에 최대500개 항목/문항씩 고정하고, 원래 검토 학교 문항은 모든 항목 저장 뒤 최대100개 대상씩 복사한다. 모든 검사가 끝난 마지막 거래에서만 출제 가능 상태로 공개한다. 범위·내용·요청 번호와 첫 문항 계획을 고정해 재개 시 새 문항을 섞지 않는다.
- 서버는 HTTP요청당 최대4단계, 화면은 검증된 진행 응답에만 다음 요청을 보낸다. 통신 결과를 모르면 자동 재전송하지 않는다. 원래 관리자 식별값을 고정해 사용자 변경을 차단하고 내부 부분 저장 뒤 권한 오류도 같은 요청으로 복구한다. 다른 관리자가 먼저 같은 틀을 완성하거나 문항 계획을 확정한 경우만 검증된 결과를 이어 사용한다.
- D25는 전체 보기 검사 조건을 유지하면서 잘못된 반복 검색 계획을 개선했다. 실제 검토 DB28,658문항·114,632보기 대조의 대상1.080초/보기6.076초·불일치0. 계획 검증과 첫500문항 저장18.061초 성공이다. 글로벌 시간제한·메모리·압축 설정은 바꾸지 않았다.
- 22:33:52 KST 최종8파일117검사 통과(11.68초). 앞선 관련14파일184검사 및 구조5파일189검사도 통과했다. 최신 타입·변경11파일 lint·설계·기능 지도·화면 구조 통과, 최종 격리 Next38페이지 빌드 성공. 직접 빌드의 APP_ORIGIN 누락은 격리 검사 설정으로 해결했다.
- 독립 검토: 잘못된501번째 문항13종을 계획 저장 전에 거절, 500+1 저장·실패 원복·최종 원자료/자격 변경 차단, 기존 검토 문항1112개/원필드2220개와100/101·500/501 경계 보존을 확인했다. 마지막 관리자/동시 생성 검토는 2026-09-20T13:17:52.557Z에 추가 결함0이었다.
- 가짜 검사 서버는 자체 종료 시 권한 오류가 났다. 이번 소유 프로세스만 확인해 종료했고 원래 빌드를 복원했다. 3037/3038/3040 대기 포트0. 새 검사 빌드는 비공개 임시 구역에 보관한다.
- 실제 자료 등록·5개 생성·기존 자료 보존·Preview/Production 배포는 DEPLOY-20260920-02의 외부 결과에서 각각 기록한다. 최신 로컬 검사를 실제 로그인 화면·학생 배정·실제 응시 성공으로 표시하지 않는다.

최종 관련 로그: `.codex-tmp/APP-20260920-01/mcp-final-targets.log`, `bounded-final-build.log`. 완료 입력 확인값: `7625f089892138cc7f8b2246ce60da87dddabfe1c43721080778cd6f2beb0587` (1119파일). 아래는 이전 검사와 중간 진행 이력이다.

## 13:11 후속: 준비한 구성 불러오기

기존 R3/R4/R7과 D12에 따라 검토한 구성 파일을 기존 편집기에 불러오도록 보완했다. 이름·태그·범위 순서와 요청 번호를 유지하며, 실제 저장은 기존 저장 버튼과 관리자 API를 사용한다. 새 API·DB 구조·권한은 추가하지 않았다.

- 13:08:38 KST 최종 UI/서버 2파일40검사 통과. 원순서 전달·자동 저장 없음·응답 유실 뒤 동일 요청·태그 초점 이동과 동일 필터 재클릭의 요청 유지·실제 내용 편집 시 새 요청·없는 범위/다른 버전/비활성 범위/잘못된 명령/2MB 초과 거절·미확정 빈 틀·읽는 동안 잠금·화면 종료/조회 변경 뒤 늦은 파일 무시를 포함한다.
- 마지막 타입·변경 3파일 lint·설계·기능 지도·화면 구조 검사 통과. 추가 검사에서 잘못 사용한 테스트 조회 옵션은 타입 검사로 발견해 수정했다. 실행 코드에 대한 독립 재검토 미해결0건이다.
- 마지막 보완 코드로 격리 Next production 빌드 성공. 실제 브라우저에서 가짜 파일을 불러와 태그 초점 이동·이미 선택된 전체 필터 재클릭→2026년/2024년 순서2범위8개→1판8개 저장을 확인했다. 준비 요청의 정확한 ID 보존은 실제 컴포넌트 검사에서 대조했다. 브라우저 경고·오류0건이다.
- 모바일390px에서 문서 폭/스크롤 폭390/390, 파일 입력 폭303.6px로 가로 넘침0. 화면 크기를 복원하고 검사 탭·서버를 종료했으며 기존 빌드를 복원했다. 가짜 파일·빌드는 비공개 임시 구역에만 보관한다.
- 로그: `.codex-tmp/APP-20260920-01/prepared-import-tests.log`, `prepared-import-final-browser.log`. 이전 전체 회귀의 근거와 남은 온라인 관리자·실제 자료 생성 검증은 아래와 분리해 유지한다.
- 후속 완료 관문1,105입력·확인값 `0faca7cd89c8030f008c7346e818701aa1a47ed64cbb37ec89fb807f91aa59e3` 통과. 각 T번호는 마지막 실행1건에 연결하고 이전 실행의 수치·실패 보완은 이 문서와 이전 커밋에 보존한다.

2026-09-20 최종 로컬 검증. 기준 d611b187ab72f38ba98eb7663c51d73f5c328f3b. 원범위 검색→템플릿 저장/편집/복사/버전→고정 단어장→기존 배정·오답·학생 조회 연결을 구현했다. 운영 적용과 실제 관리자 인증 확인은 별도6단계다. 아래 중간 이력의 미구현·미실행 표시는 당시 상태다.

## 확인한 결과

- 연도 구간·개별 연도·월·유형·문제번호와 교과서 과·DAY를 독립적으로 고른다. 검색을 바꿔도 담은 범위를 유지하고, 원행별 제외·겹치는 원행의 한 번 포함과 다른 원고의 보존을 확인했다.
- 템플릿 이름/태그 수정은 내용 버전을 유지한다. 범위 변경은 실제 추가·제거 비교 후 다음 버전으로 저장하고 이전 버전 복사를 지원한다. 미확정 기말 틀은 저장 가능하지만 배정 불가다.
- 일반 뜻 문항은 기존 생성기를 사용한다. 학교 검토 문항과 범위 밖 보기는 원문·순서·정답·발음 참조를 고정하며, 원판 비활성화 뒤에도 유지한다. 학교 밖 보기를 새 시험 대상으로 늘리지 않는다.
- 고정 자료를 학생 시험·결과·학습에서 조회하고, 미응답 역방향의 정답 단서는 숨긴다. 기존 사전값을 새 구성에 나중에 덮어쓰지 않는다. 직접 배정·예약 전달·정확 오답·설정 수정의 공통 보호를 검증했다.
- 기존 장문 모고2025년9월[41·42], 시행2025년/학년도2026년 수능[43·44·45], 미분류를 공통 단원 조회에 전달하는 성공·실패 회귀를 추가했다. DB에서는 등록된 부분 범위의 부모 원행 전체가 맞아야 새 분류를 반환한다. 구 word_index 호환 분기는 최신 기존 태그를 전달하며 이번의 원행 재검증 경로와 구별한다.

## 검사 실행과 발견한 문제

| 검사 | 실제 결과와 근거 |
|---|---|
| 전체 회귀,08:56:44 KST | 495파일·3,630검사 중3,615통과/14실패/기존1skip. 실패는 아래 두 원인으로 한정됐으며 수정 후 해당 파일 전체를 재검사했다. 한 번의 전체 실행이 모두 통과했다고 표기하지 않는다. |
| 최종 화면·서버·회차 수량,09:06:39 | 5파일129통과. CSS/화면 역할·템플릿·서버·회차별 후보 수를 포함한다. |
| 최종 DB,09:07:34 | 2파일37통과. 기존 배정 최종 스키마26건과 구성 문항/오답/수정11건. |
| 장문 전달·기존 문항 직접 보존,09:21:38 | 3파일64통과. 기존 문항1개의 모든 기존 필드 전후 동일·새4열 null, 기존 장문/수능/미분류 전달, 부모 수량 불일치 차단과 원복 재성공을 포함한다. 별도 발음 사전 표의 전후 검사는 아니다. |
| 마지막 연결 검사,09:29:21 | 선택·버전·발음·템플릿 화면·서버의5파일52통과. 작업 기록 T1/T2/T4/T7/T8에 실제 명령을 연결했다. |
| 타입/lint/기능 지도/화면 구조/작업 관문 | 전체 lint는09:18 오류·경고0. 마지막 검사 추가분의 타입·lint와 기능 지도 통과. 완료 관문1,105입력 파일의 확인값 `dafe5a4db5d92c53626a17734fad91ca5b77a4162ee719710af603f2196c1d8a` 통과. |
| 실제 Next 빌드와 브라우저 | 격리한 실제 production 빌드 성공. PC1280×900·모바일390×844에서 아래 흐름 확인. 외부망 차단과 가짜 인증/응답으로 실제 DB 쓰기0. |

첫 전체 실패의 CSS2건은 공용 색/모서리 기준으로 수정했다. 기존 자료 보존12건은 새 nullable 구성 열2개가 추가돼 행 전체 JSON 확인값이 달라진 경우다. 독립 전후 대조에서12시나리오·기존 배정84행의 기존 값 변경0, 추가값168개 전부 null을 확인했다. 검사는 정확히 그2키만 제외하고 null도 단언하도록 보완했다. 모든 null을 지우거나 문항/학생 검사를 제거하지 않았다.

독립 검토에서 발견한 회차별 의미 충돌·미배정 범위 수량·확정 저장 후 화면 연결 재시도를 보완했다. 날짜2회는 전체12/배정8/남음4,1,503후보 중500배정/1,003남음, 확정 저장 POST1회/화면 연결2회 복구를 확인했다. 요청 ID는 환경·내용에 고정해 같은 요청의 중복 생성을 막는다.

검사 로그는 앱 내부 비공개 `.codex-tmp/APP-20260920-01/`의 `full-tests.log`, `final-focused-tests.log`, `final-db-tests.log`, `legacy-tag-preservation-tests.log`, `final-eslint.log`, `completion-*.log`, `browser-server-final.log`에 보관한다. 실제 원자료·학생 자료는 Git에 포함하지 않는다.

## PC와 모바일에서 수행한 흐름

1. 기존 단어 배정 창→단어장 찾기→템플릿 찾기/범위로 새로 만들기. 미확정 틀과 저장한 틀을 구별했다.
2. PC에서2024~2026 주제3범위12행을 담고2026 장문 검색으로 전환해 숨은 선택을 보존했다. 장문[41·42]를 더해4범위16행의1판을 저장했다.
3. 찾기 태그를 수정해1판16행을 유지한 뒤 교과서1과·DAY1을 추가했다. 추가8행/제거0을 확인하고2판24행을 저장했다.1판16행은 그대로다.
4. 모바일에서1판16행을 복사하고 새 이름/태그로 검색했다. 실제 생성 결과를 기존 배정 설정으로 연결해 원래4단원과16행,3개 연도·주제/장문·23/41/42번호를 확인했다. 배정 버튼은 누르지 않았다.
5. 모바일 문서 폭390/스크롤 폭390, 대화상자 내부351/스크롤351로 가로 넘침이 없었다. 화면 크기 복원과 임시 탭 종료를 완료했다.3037/3038/3040 서버 종료를 확인한 뒤 테스트 빌드를 별도 보관하고 기존 빌드를 복원했다.

브라우저는 실제 앱 코드와 가짜 서비스의 조합이다. 생성 완료 응답을 배정 설정에 연결하는 흐름까지 확인했으며 실제 문항 생성/최종 고정 거래는 DB 검사로 따로 검증했다. 실제 원자료의 온라인 전체 생성, 실제 관리자 화면, 실제 학생 배정 성공으로 확대하지 않는다.

## 실제 자료와 성능의 범위

등록 전 원고는26단어장608범위19,596행, 포함19,155/보류439/제외2와9개 템플릿이다.48묶음·선택 근거37파일109,969값을 등록기에서 직접 검산했고 독립 P2/P3/P4 전수 대조 차이0이다. 원뜻/원품사/후보 승인 상태와 기존 장문601음원 참조를 유지했다. 음원 접근·전수 청음은 별도다.

가짜14,329행→28,658문항 생성은148.86초, 잘못된 문항0이다. 같은 점수의 후보를 모두 유지한 성능 검사이며 실제 원자료 전체의 온라인 생성 제한 통과는 아니다.6MB 이상 응답의64KB 분할·원문 복원·취소는 통과했다. 검토 환경에서 전체 범위의 실제 생성/전송 시간을 추가 확인한다.

## 독립 검토와 외부 적용 경계

자료 전수 대조, 템플릿/UI/API 복구, 공통 배정·기존 문항 보존을 읽기 전용으로 나누어 검토했다. 메인이 수정하고 지적사항의 회귀와 재검토를 완료했다. 원사전·원자료·기존 음원·실제 학생 기록 쓰기0이며 새 구조/범위/템플릿의 Preview·Production 등록은 아직0이다. 환경별 구조·등록·앱 배포·인증 화면은6단계에서 각각 기록한다.

## 이하 중간 이력

## 첫 중간 확인: 07:22 KST

- T1/T3: 실제 전체 스키마에 새 범위 등록과 템플릿 저장을 추가한 DB14건, 선택 계산5건 통과. 원자료 승인과 해시·등록 상태·같은 원행 중복·다른 원고·충돌·멱등·표시정보 편집·새 버전·원범위별 연속 단원 계획을 검사했다.
- T7/T8: 실제 화면 부품9건 통과. 숨은 선택 유지, 탭 이동과 새 틀 입력 초기화,409 후 최신 버전 비교·재저장, 저장 결과 미확정 뒤401·다른 관리자·원관리자 재인증, 늦게 도착한 조회가 권한 거절을 덮지 않는 경우를 포함한다. API10건 통과.
- 기존 고3 저장 DB8건, 기존 선택/조합 검사와 혼합 단원 선택2건도 통과했다. 새 기능 전체 회귀 검사 완료를 뜻하지 않는다.
- 타입 검사 통과 후 연속 단원 계획과 일부 회귀 검사를 추가했으므로 최종 타입·lint는 다시 실행한다. 기능 지도는 명시 기준 커밋으로 최종 재검사한다.
- T4/T5/T6 구현 중: 실제 단어장 생성·학교 검토 문항·범위 밖 보기의 고정 음원·배정 및 학생 학습 연결은 아직 완료되지 않았다. 두 번째 migration은 이 연결을 위한 파일이다.
- T9 전체 회귀/빌드/최종 독립 검토, T10 실제 PC/모바일 브라우저, Preview/Production 적용은 미실행이다.

검증에 사용한 원자료는 앱 밖 1~4단계 산출물에서 확인하며 앱 Git에 넣지 않는다. 현재 활성 여부와 다음 우선순위는 루트 작업 대장만 관리한다.

## 구현 연결 및 두 번째 중간 확인

- T4~T6 구현을 연결했다. 원범위 등록·템플릿 저장 DB14건과 고정 문항·직접 배정·오답·설정 수정 DB11건이 앞선 로컬 검사에서 통과했다. 학교의 원판이 비활성화된 뒤에도 고정 문항·범위 밖 보기·발음 보존, 오답1/3개 재배정과 수정/실패 원복/재전송을 포함한다.
- 08:54:50 KST 선택/화면/서버/학생 조회 5파일92건 통과. 별도 회차의 같은 표제어·다른 뜻을 보존하며 날짜2회에는 전체12/배정8/남음4, 전체범위 동시 출제에는 모호한 대상 제외,1,503후보의300행 페이지 제한을 검사했다.
- 저장 성공 후 화면 연결 실패를 독립 검토에서 재현했다. 확정 결과를 보관해 연결만 다시 시도하도록 수정했고 실제 UI에서 POST1회·연결2회·성공 복구를 확인했다. 자료 등록기의 템플릿 명령 ID도 동일 환경·내용에 대해 고정했다.
- 고정 발음·뜻은 학생 응시/결과/학습에 사용하며 현재 사전은 기존 행만 조회한다. 역방향 미응답에는 대상 발음/정답 위치를 숨긴다. 고정 자료 누락은 오류로 반환한다.6MB 이상 한글 JSON의 조각 크기·원문 복원·취소도 통과했다.
- 3,000행 가짜 성능: 저장3.832초/해석0.667초/준비4.001초/최종 고정14.122초(당시SQL2판). 최종 생성기 별도 가짜14,329행→28,658문항148.86초, 유효하지 않은 문항0. 같은 점수 후보를 모두 보존한 조건이다. 실제 전체 자료의 서버 생성시간이나 배포 환경 상한 통과를 뜻하지 않는다.
- 등록 전 검사는 실제26단어장608범위19,596행·9틀,37개 선택근거 파일109,969개 값 대조 통과. 포함19,155/보류439/제외2. 독립 전수 대조도 P2/P3/P4와 차이0. 원뜻/원품사/후보 승인/장문601음원 참조를 보존했으며 음원 청음은 하지 않았다.
- 전체 회귀 검사, 실제 PC/모바일 화면, 최종 타입/구조/빌드는 진행 중이다. Preview/Production 적용 및 실제 관리자 확인은 미실행이다.
