# 학생 조건 자동 선택과 필수 정보 검증

2026-09-21. 입력은 `2c9741f12f478f201305e56b5bec85f18f9b1a1b`와 사용자 추가 요구 및 후속 진행 지시다. 이 결과는 로컬 구현에 한하며 운영 적용 결과가 아니다.

## 구현 결과

- 학생 신규 등록·정보 수정에서 이름·학교·학년을 필수로 검사한다. 빈 값과 공백만 있는 값에는 입력별 오류와 오류 테두리를 표시하고 저장을 막는다. 학교 직접 입력도 중1~고3을 명시 선택할 수 있다.
- 학생 배정창은 공통 학교·학년과 현재 한국 날짜의 학기를 기본 선택한다. 서로 다른 학생 조건은 전체로 두며, 학기 미분류 공통자료도 찾을 수 있다. 다른 학년 기출을 직접 고를 수 있다.
- 같은 학생의 검색어·직접 선택·작성 범위는 재열기와 정보 보완 뒤에도 유지한다. 정보 저장 결과에서 바뀐 기본 조건만 반영하며 기존 저장 템플릿의 범위를 덮어쓰지 않는다.
- 정보가 빠진 기존 학생은 새 배정 전에 정보를 보완해야 한다. 화면·미리보기와 최종 DB 저장에서 검사한다. 과거 시험·기록 조회와 완료된 요청의 결과 재확인은 유지한다.

## 검사 근거

| 검사 | 확인 결과 |
|---|---|
| T1 필수 입력 | 공백/유니코드 공백, 신규·수정 입력, 서버 검사, 저장 충돌·결과 확인 회귀 통과 |
| T2 최종 저장 | 격리한 가짜 DB의 공개 배정 생성 경로 17개 모두 누락 정보를 거절. 학생·배정·요청 행 증가 없음. 과거 완료 응답 4종 재확인과 기존 시험 수정·삭제·재시험 회귀 보존 |
| T3 자동 선택·초안 | 공통/혼합 학생, 학생 변경, 같은 창 재열기, 검색어 및 수동 조건 보존, 정보 보완 뒤 범위·회차 입력 유지 통과 |
| T4 새 구성·공통자료 | 학기 경계, 분류 없는 자료, 새 구성 대상 기본값 갱신, 직접 수정 보존, 기존 템플릿 불변 통과 |
| T5 실제 화면 | 가짜 자료를 쓰는 로컬 실제 앱에서 신규 필수 오류와 저장 활성화, 학교 직접 입력, 학생 학교/고1/2학기 자동 선택, 수동 조건 재열기 보존 확인. PC 1440px와 모바일 390px 확인; 모바일 문서 폭/내용 폭 390/390px |
| T6 구조·빌드·검토 | 관련 26파일의 서로 다른 검사 348개 전부 통과. 전체 DB 검사 37개 통과 후 완료 응답 보존을 추가한 관련 4개 재검사 통과. 타입·변경 소스 lint·기능 지도·화면 구조·38페이지 격리 빌드 통과. 두 읽기 전용 독립 검토의 확정 지적을 반영하고 재검토 완료 |

실행 원본은 Git 제외 경로 `.codex-tmp/APP-20260921-02/`의 검사 JSON과 빌드 기록으로 보관한다. 중복 실행은 검사 수에서 제외했다. 일부만 재실행한 보고서의 건너뜀 33개는 앞서 전체 실행에서 통과한 검사다. 최초 가짜 자료의 학교·학년 누락으로 난 회귀 실패는 정상 프로필 자료와 새 기본 필터의 기대 동작으로 보완 후 재검사했다.

처음 빌드는 외부 의존 폴더 연결과 Turbopack의 경로 제한으로 실패했다. 기존 검증 도구의 webpack 경로로 격리 빌드를 완료했으며 기존 빌드를 복구했다. 검사용 서버·탭을 종료하고 화면 크기를 복원했다.

## 확인 한계와 보존

실제 학생 추가·정보 변경·배정 저장·시험 시작·답안 제출은 0건이다. 운영/검토 DB 변경과 새 배포도 하지 않았다. 화면 확인은 가짜 자료이며 실제 관리자 계정의 운영 흐름 검증으로 표시하지 않는다. 기존 빈 정보는 일괄 보정하지 않는다. 오답 목록, 템플릿 관리와 수능 원고 등록은 별도 후속 지시의 범위다.

[요구·결정·검사](../work-orders/APP-20260921-02.md), [구체 반영 명세](APP-20260921-02_반영명세.md), [입력 확인값과 완료 검사](../../architecture/work-records/records/APP-20260921-02.json)를 함께 대조한다.
