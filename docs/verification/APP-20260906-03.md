# APP-20260906-03 일정 표시·계산·입력 분리 검증

- [지시서](../work-orders/APP-20260906-03.md), [실행 근거](APP-20260906-03.json), [R/D/T 기록](../../architecture/work-records/records/APP-20260906-03.json).
- 기준: bc48313 위 기존 로컬 변경 보존. Next16.2.12/React19.2.8, 로컬만. 실제 DB·학생·자료·의존성·커밋·업로드·배포 변경 없음.

## 적용한 책임 분리

- 일정 요약 수치는 기존 `domain/vocab-schedule.ts`의 순수 함수가 계산한다.
- 단어장·범위 이름과 회차 표시 행은 `presentation/vocab-schedule-view.ts`가 만든다.
- 시간 양식 이름·성공 비움·실패 보존·알림은 전용 입력 훅이 맡는다. 저장 요청은 기존 제어기로만 전달한다.
- 실제 단일/일괄 조립 계층이 두 일정 UI에 필요한 값·오류·콜백만 전달한다. 전체 제어기, API 요청, UI 안 계산을 제거했다.
- 날짜 숨김은 기존 ConditionalReveal의 마운트 수명을 보존한다. 오답 탭으로 완전히 나가면 이름 입력이 초기화되는 기존 동작도 유지했다.

## 실행 결과

| 검사 | 결과 |
|---|---|
| 관련 검사 | 5파일 38개 통과 |
| 전체 회귀 | 381파일 2,219개 통과, 05:52:20 KST 시작·112.58초 |
| 리뷰 후 보완 | 시간 쌍 양방향 보존 assertion 추가, 05:56 UI 18개 재통과 |
| 타입/lint/구조/변경 연결 | 통과 |
| 요청 캐시 | calls=2, 기존 요청 단위 재사용 통과 |
| 새 격리 Next 빌드 | 통과. 최종 보완은 검사 assertion만이며 실행 앱 소스 동일 |
| 독립 읽기 리뷰 | 신규 P1/P2 0, 검사 보완 제안 반영 |
| 완료 관문 | 현재 관련 입력 546개와 근거 일치, 통과 |

지문: `b020f5ab0a8b9be0e956be9f123a31c880424694169e5144c70d303e9d653dba`.

## 실제 로컬 화면

- 가짜 학생·자료를 쓰는 실제 Next에서 단일 DAY2→DAY1의 역순 표시를 확인했다.
- 단일/일괄 모두 시간 양식 이름을 입력하고 시험일을 숨겼다 펼쳐도 입력값을 보존했다.
- 단일 오답→단어 시험 전환에서는 양식 이름이 빈 값으로 돌아왔다.
- 단일의 명시 폐기와 일괄 닫기 후 열린 창 0개. 검사 탭34 닫기, 서버 정상 exit0, 포트3037/3038/3040 종료 확인.

미리보기 API는 로컬 안전 대역에서 차단된다. 화면에 보인 해당 차단 문구를 실제 서버 오류로 판단하거나 저장 완료로 기록하지 않는다.
회차 수치·시간 수정·양식 저장 성공/실패/중복 잠금은 자동검사 근거이며 실제 DB 저장은 하지 않았다.
운영 인증·DB 비용·성능 개선·모든 화면 폭 시각 검사는 미측정이다. 기존 APP02 근거는 당시 코드 지문으로 보존했다.

이번 단계는 유형 정책·남은 Preview 안내·목록 조회·새 캐시를 바꾸지 않았다. 후속 범위는 전체 실행계획과 루트 대장에서 구분한다.
