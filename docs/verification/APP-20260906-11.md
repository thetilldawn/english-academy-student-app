# 내역 중복 조회 검사

- 입력: APP10 로컬 완료본, APP11 R1~R3 / D1~D4 / T1~T3.
- 구역 3개가 같은 변경 영수증을 받으면 HTTP 첫 목록 1회를 공유한다. 부분 취소는 다른 소비자를 보존하고 마지막 이탈은 취소한다. 완료된 응답은 캐시하지 않는다.
- 오래된 행·개수·커서 제거, 정상 빈 결과와 조회 실패 구분, 공유 401/403의 전체 개인정보 숨김을 실제 React 합성 검사로 확인했다.
- 독립 검토에서 찾은 같은 밀리초 내 과거 DB 시각 허용 문제는 소수초/시간대 비교로 고쳤다. 재검토 P1/P2 없음.
- 전체 412파일 2,521검사, 타입·lint·구조·변경 연결, 가짜 로컬 백엔드의 새 Next.js 16.2.12 빌드 통과.
- 초기 전체 검사의 실패 두 건: Set.delete 이름을 DB 삭제로 오인한 탐지는 검사 완화 없이 소비자 참조 배열로 해결. 동적 가져오기 첫 컴파일 검사의 대기만 5초로 변경. 앱 지연을 넣지 않았다.
- 근거 해시: `f6c90baf8d097357a93bf08d1b6069e4cba0b7fc445c3337feb8b4e34462a9ec` (747개 파일). 검사 기록 자체는 해시 대상이 아니다.
- 이번 구역 갱신은 코드·합성 검사로 검증했으며 실제 브라우저 조작이나 운영 DB 실측으로 표현하지 않는다. 배정 화면 실측은 APP10의 별도 근거다.
- DB·실제 학생·자료·배포 변경 없음. 내역 재방문 보관 캐시는 이 작업에 포함하지 않는다.
