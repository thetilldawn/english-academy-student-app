# APP-20260906-14 자동 공개·보류 검증

## 최종 확인 — 2026-09-07 02:39 KST

- 운영 준비 17:23:59 UTC부터 17:39:32 UTC까지 확인했다. 마지막 실제 학생 목록 재조회 정상·브라우저 warn/error0, 보호13표 내용 SHA 전부 동일, 운영 잠김2건의 원문 노출0이다. 서버 전체 오류율/비용이나 연속 계측을 대신하는 기록은 아니다.
- R1~R5의 로컬/실제 환경 근거를 T1~T9에 연결하고 완료 관문을 통과했다. 관련 입력916개 지문은 최종 로컬 검사와 동일하다. 실행 코드506dc7d 이후 앱 코드를 수정하지 않았다.
- 독립2연결 잠금 경쟁·기본 확인창 취소/HTTP 개수 실측은 미검증으로 남기며, 기존 Preview CI의 브랜치 사전조건 실패도 보존한다. 이 항목들을 통과로 계산하지 않는다. 실제 사용자 요구의 보류/공개/우회 차단·기존 시험 보존 결과와 구분한다.
- 최종 원시 확인값은 ignored `.codex-tmp/app14-production-final-check-20260907.json`이다. 실제 학생 시험 시작/답 제출0, 로컬 검사 서버0, 원래 빌드 복구·검사 산출물 보존. APP15 구현은 이 확인 뒤 별도 기록으로 시작한다.

## 2026-09-07 양쪽 DB·앱 적용과 실제 화면 확인

- 코드 `506dc7dcf433ad86925d0d9e4426819c101a8f25`. Preview `6295628502`와 Production `6295834958` 배포 success. Production은 17:23:59 UTC 새 운영 환경 빌드이며 Preview 산출물을 승격하지 않았다. 운영 주소는 `https://english-academy-student-app.vercel.app`.
- 동일 SQL을 Preview migration `20260906165531`, Production `20260906172107`로 적용했다. 각 환경 적용 직전/직후 보호13표의 행수·내용 SHA 모두 일치한다. 운영 첫시험 진행0/재시험2는 보존했다. Preview 관계22/잠김20건, 운영 관계2/잠김2건에서 학습 내용 노출0. 구 목록 v1 세 정의·권한 동일, 새 목록 v2는 service_role 전용, raw 표 직접권한0과 비관리자 v3 거절 확인.
- Preview 기존 테스트학생의 2회차 건너뛰기는 `deferred`/보류 이벤트1건, `completed_at=null`로 남았다. 1회차 완료 기록과 3회차 공개·마감은 정확히 동일하다. 다음 날짜가 이미 지나 `attention`/일정 확인으로 표시되며 날짜 이동이나 추가 보류는 없었다. 옛 2회차 ID를 다시 보낸 DB RPC는 `40001`로 거절됐다.
- 앱브라우저의 기본 확인창 처리 도구가 일시적으로 응답하지 않았지만 이후 원래 관리자 창의 보류 표시와 실제 이벤트를 확인했다. 취소 확인창·HTTP 요청 개수의 실측은 하지 않았고, 이 두 항목은 로컬 컴포넌트 검증과 구분한다. 오래된 대상 요청을 자동 재처리하거나 다음 회차를 추가로 건너뛰지 않았다.
- 실제 Preview: 일정 충돌 카드의 단어 보기 비활성, 직접 단어장 주소의 안전한 안내/제목/닫기와 학습 단어0 확인. 이미 응시한 시험 단어장46개·발음·영어/뜻 가리기는 정상 유지. 운영: 테스트학생의 다음2회차 공개 예정·앞 첫시험 완료 안내·단어 보기 비활성·직접 단어장 잠김/닫기 확인, 브라우저 warn/error0. 학생 시험 시작/답 제출0.
- Preview 화면 점검 뒤에도 보호13표 내용 불변이다. 테스트용 큐의 명시적 보류 이력만 추가됐으며 실제 학생·답안·점수는 바꾸지 않았다. 기존 Preview 전용 계정을 과거 기록에서 재사용했고 비밀번호/팀/연결 설정은 바꾸지 않았다.
- 자동 Preview smoke `34047404926`은 승인 전용 브랜치 불일치 사전조건에서 실패하여 앱 검사를 실행하지 않았다. 기존 CI 허용 범위를 넓히거나 통과로 기록하지 않는다. 로컬 전체 검사와 현재 앱브라우저/DB 검증은 별도 근거다. Vercel 상세 로그 전체·서버 오류율/실제 비용은 측정하지 않았다.
- 두 MCP SQL 호출로 독립 잠금 경쟁을 확인하려 했지만 조회가 겹치지 않아 `lock_contention_not_observed`로 끝났다. 쓰기0/잠금 거래 종료, 실제 2연결 동시성 통과로 계산하지 않는다. 기존 로컬 거래·정확 대상 재시도 보호 검증과 구분한다.
- 로컬 검사 서버는 정상 종료 요청 후 하위 프로세스 종료 확인에 실패했다. 정확한 부모/명령/전용포트의 검사 프로세스2개만 종료하고 원래 `.next`를 복구했다. 새 검사 빌드는 ignored `.codex-tmp/app14-local-verified-next-20260907`에 보존했다. 삭제0.

## 2026-09-07 실행 승인·구버전 호환 보완

- Preview DB 최종 후보 적용 성공: 원격 migration `20260906165531`, 이름 `add_first_attempt_assignment_release`. 관계22건, 미공개 학습 응답20건에 words/answers/audio/definition/example 필드0. 보호13표의 적용 직전/직후 행수·내용 확인값 모두 동일.
- 구 목록 3개 함수의 정의가 적용 전과 정확히 동일하다. 새 private 표 RLS=true/일반역할 직접권한0, 신규/교체/첫완료 trigger 활성 확인. 신규 권한 WARN1은 명시된 관리자 RPC v3의 SECURITY DEFINER 경고로, 함수 첫 admin 검사와 비관리자 실제 거절을 확인한다. 기존 v1/v2 실행권한 해제는 유지한다. [권한 경고 기준](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable). INFO1은 직접접근을 막은 private 표의 정책없음이다.

- 사용자는 기존 미응시를 포함한 이 DB 변경의 Preview 검증 뒤 Production 적용 질문에 `ㄱㄱ`로 승인했다. 전날 실행 차단은 이력으로 보존한다.
- 독립 배포 검토가 발견한 구버전 학생 목록 구역 충돌을 로컬에서 수정했다. 원래 목록 v1은 그대로, 새 판정과 최초/완료 조회는 v2로 분리했다. 새 앱의 서버 조회와 로컬 가짜 응답만 v2로 연결하며 실패 시 v1 자동 우회는 없다.
- 실제 Supabase 역할 조회에서 service_role의 BYPASSRLS=true, anon/authenticated=false를 확인했다. 새 DB 검사는 롤백되는 가짜 거래 안에서만 이 서비스 역할 조건을 맞춘다. 원격 권한을 넓히지 않았다.
- 최초 검사 2건은 가짜 DB Date/JSON 변환과 서비스 역할 설정 차이 때문에 실패했다. 실제 HTTP JSON·실제 역할 조건에 맞춘 뒤 관련2파일20검사 통과. 새 순서가 기존/신규 목록·새응시 차단·권한을 함께 검사한다.
- 최종 전체425파일 **2,691통과/선택 외부자료1건 skipped**, 타입·전체lint·구조·요청캐시·격리 최적화빌드33페이지 통과. 로컬 앱브라우저 실제 학생 목록3건 정상 표시. 독립 재검토 P1/P2 없음.
- 입력916개 지문 `ad9b41f2ebb7112538bd8e9c2d5b39c88b715080fff44e97f40bee2d8ef8fe99`, Preview 적용 최종 migration SHA `790a375052a12feafc678995100f73dd0ae73b823381029ac0bcbdc5e5cc8a06`. 이전 후보 해시와 구분한다.
- 적용 전 정의·권한·트리거·제약과 학생 보호13표 확인값은 ignored `.codex-tmp/app14-20260907-before-<ref>.json`에 보관했다. 운영 첫 시험 진행0/재시험2, Preview 최초1/재시험1. 원격 적용/새 배포는 아직 완료로 기록하지 않는다.

## 이전 실행 이력: 2026-09-06 로컬 통과, DB 실행 차단

- **Preview DB 적용은 실행 전에 자동 안전 심사로 거절됐다.** 심사는 과거 문서정비 계획의 DB 변경 제외와 이 구체적 구조 변경 승인 부족을 근거로 들었다. 다른 도구/SQL 경로로 우회하지 않았다.
- 현재 자동 공개 지시서와 최근 R1~R5 합의를 대조했다. 업무 규칙을 다시 묻지 않으며, 외부 DB 실행 경계만 추가 승인을 요청한다.
- 거절 뒤 새 표 없음/해당 migration 기록0과 보호13표 전후 일치를 재확인했다. Preview/운영 DB·배포·실제 학생 시험·APP15 패치 미변경이다.
- 기록 관문의 실제 소유/흐름 연결을 정정하고 관련 계약 검사3개의 정확한 소유를 등록했다. 검사 기준 완화 없이 지도·4파일124검사·관련lint 통과.
- 최종 관련 입력 915개 지문 `400c09391c5c977e4c74dc8b07fd54923edaf4d32c19ff24140d635d39009048`. 로컬 T1~T6/T8/T9 통과, T7은 실패(실행 차단)로 보존하며 완료 관문은 통과시키지 않는다.
- 검사 서버의 정상 종료 요청이 권한 문제로 실패하여 PID/부모/전용 포트를 다시 확인한 뒤 해당2프로세스만 종료했다. 남은 포트0, 기존 빌드 복구 확인. 새 검사 빌드는 ignored `.codex-tmp/app14-local-verified-next-20260906`에 보존하고 삭제하지 않았다.

기준: [작업 지시서](../work-orders/APP-20260906-14.md). 이전 작업의 검사 결과를 재사용하지 않는다.
실제 진행 상태·우선순위는 루트 작업 대장, 구현·검사 대상과 지문은 앱 내부 작업 기록이 기준이다.

## 2026-09-06 로컬 구현 검사

- 첫 완료·앞 실제 마감+12시간·보류와 날짜 보존을 한 서버 판정으로 연결했다.
- 기존 1회차·단독 예약 배정의 사전 공부는 유지하며, 미공개 후속 회차만 잠근다.
- 새 DB 검사 13개 통과: 구 스키마 저장/다단계 교체/첫 완료/명시 skip 소급, 원본 학생·배정·수신·응시·문제·요청 결과 SHA 불변, 첫 미달 후 재시험 보존, 시간 경계, 보류/취소 분리, 잘못된 관계 차단, 새 INSERT 차단.
- 학생 카드·단어장 서버 조립·시험 명령 3파일 26개 통과. 잠김 자료를 reader에 넘기지 않음, 제목/닫기 유지, 첫 완료 대기 갱신0회, 확정 공개시각 갱신1회.
- 기존 전체 스키마 회귀: review-assignment 33개, exam-use projection 10개 통과. 최초 완료 시각 없는 임의 completed fixture는 실제 계약에 맞춰 수정했다.
- 별도 읽기 리뷰에서 자동 상태 전이 시 옛 날짜 보정이 개입할 수 있음을 확인하여 수정했고, 유효 일정이 최초 계획과 다른 경우 두 날짜의 정확한 보존을 검사했다.
- 타입/지도/표시 부품 구조 중간 검사 통과. 전체 단위·lint·빌드는 최종 코드에서 별도 실행한다.

## 이전 로컬 단계에서 검증하지 않은 경계

- Preview와 운영 DB 변경·배포 및 실제 화면 확인은 이 단계에서 미실행이다.
- 로컬 PGlite는 순차 트랜잭션 검사다. 실제 독립 연결의 동시 행 잠금 검증을 의미하지 않는다.
- 잠금 거절 시 전체 롤백, 대상 없는 구 관리자 처리 차단, 실제 학생의 진행 중 응시 보호는 배포 단계에서 따로 확인한다.
- 실제 학생 시험 시작·답안 제출 금지. 검증용 학생만 사용한다.

## 로컬 최종 및 원격 사전 확인

- 23:14 KST 최종 전체: 425파일 통과, 2,690검사 통과. 외부 원자료를 요구하는 별도 선택 검사 1개는 건너뜀으로 분리했으며 이번 기능의 필수 검사는 모두 실행했다.
- Next16.2.12 격리 최적화 빌드/33개 정적 페이지 생성·타입·lint 통과. 빌드는 가짜 로컬 설정이며 Preview나 운영 배포를 의미하지 않는다.
- Preview: 저장 요청71건·그룹22건·큐41개/112회차. 운영: 저장 요청2건·그룹1건·큐1개/5회차. 양쪽 배열 오류/회차 중복·누락/원 수신 누락/교체 분기0.
- 운영의 최초시험 진행0, 재시험/검토 대기2를 읽기 전용으로 확인했다. 실제 학생을 대신해 응시/답안을 제출하지 않았다.
- 학생·배정·수신·단어·응시·답·점수·오답 관련13표의 행수와 SHA를 양쪽에서 보관했다. 원문 개인정보는 검증 문서에 넣지 않았다.
- 적용 후보 `20260906130402_add_first_attempt_assignment_release.sql` SHA-256:
  `734754f1c4d6673400ebc1cccf4647b412a7f060d312b4e42d5b3431655939ff`.
- 영향: 새 private 후속 연결 표, queue 보류 열/상태, 첫 완료/신규응시/요청·교체 트리거, 공개 판정/목록/단어장/미응시/큐 함수. 기존 학생·배정·시험 답·점수 수정은 하지 않는다.
- 적용 전 함수 정의·권한·트리거·제약은 앱 Git 밖의 ignored `.codex-tmp/app14-before-<환경ref>.json`에 저장했다.
- 실패 기준: 소급 관계 불명, 잠금 거절, 보호13표의 설명 불가능한 변화, 공개 전 자료 노출, 관리자 보류 대상 불일치 중 하나라도 있으면 운영 반영을 멈춘다.
- 두 SQL 도구 요청으로 Preview 테스트학생 행 잠금을 동시에 관측하려 했으나, 관측 시 활성 잠금0이었다. 잠금 확인 조건이 충족되지 않아 migration 호출 자체를 하지 않았다. 이것을 두 연결 동시성 통과로 계산하지 않는다. 데이터 쓰기0, 잠금 거래는 롤백 종료했다.
- 복구 시 새 공개 정책만 비활성화/직전 함수·트리거 복원 대상으로 하며, 이미 생성된 보류 기록·학생/답/점수를 삭제하지 않는다. 운영앱은 직전 실행 코드8c7bb64로 복구할 수 있다. Preview용 환경을 운영으로 단순 승격하지 않는다.
