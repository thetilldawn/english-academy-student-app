# 마지막 답 선택 0.3초 — 수정·검사 결과

현재 동작은 2026-09-20 요청으로 **0.15초**로 변경했다. [보완 검사 결과](APP-20260919-02_20260920.md)를 먼저 본다. 아래는9월19일 당시300ms의 검증 이력이다.

2026-09-19 / APP-20260919-02 / 로컬 구현·검증. 운영 배포는 하지 않았다.

## 바뀐 동작

- 마지막으로 누른 답을 바로 강조하고, 그 뒤 300ms 동안 추가 입력이 없으면 그 답만 한 번 제출한다. 다른 답이나 같은 답을 다시 눌러도 대기는 마지막 입력부터 다시 계산한다.
- 대기 중에는 선택을 바꿀 수 있고 정오답은 보이지 않는다. 실제 전송이 시작되면 기존 잠금을 적용한다.
- 다음 문항 준비가 늦으면 서버 준비와 300ms를 모두 기다린다. 복구·이탈·시험/단계/문항 변경 때 이전 예약을 버린다.
- 기한 전에 고른 답을 자동 무응답/전체 만료 요청이 먼저 덮지 않게 한다. 제한시간 자체를 늘리지 않으며 최종 유효성은 기존 서버 판정에 맡긴다. 0초 뒤 새 선택은 받지 않는다.
- 기존 결과 표시·음성·다음 문항 전환은 유지했다. 대기 중 문제 발음을 다시 재생하는 경우도 실제 전송 시 현재 음성을 확인한다.

## 확인한 근거

| 검사 | 결과 |
| --- | --- |
| 화면·상태 검사 | 65개 통과: 299/300ms, 다른 답·같은 답 연타, 첫 시험·재시험, 제출 후 잠금, 마감 3종, 느린 전환, 같은 문항 복구, 이탈, 음성·실패 복구 |
| 도메인 검사 | 7개 통과: 선택 300ms 및 기존 시간·판정 계약, 대기/피드백 분리·복구 세대 |
| 실제 Next 가짜 시험 | 선택 변경 뒤 299ms 전송 0회, 300ms에 마지막 답(index 2) 1회. 다음 문항의 같은 답 연타도 마지막 index 1을 1회만 전송 |
| 실제 화면 | 대기 선택 강조·전송 후 잠금·음성 넘김·다음 문항 확인, 브라우저 실행 예외 0 |
| 독립 검토 | 대기 중 새 문제 음성을 놓치는 경로 1건 발견 → 수정·회귀 검사 → 재검토에서 미해결 발견사항 없음 |

최종 관련 검사 129개 통과. 전체 검사와 빌드 근거는 [내역 수정 검사 기록](APP-20260919-01.md)에 함께 적었다. 전체 3,557개 통과 후 기존 DB 검사 1개 시간 초과는 단독 60개 재검사에서 통과했으며 기존 1개 미실행은 통과로 세지 않았다.

## 재확인 위치와 경계

- [요구·설계·검사 기록](../../architecture/work-records/records/APP-20260919-02.json): 기준 실행 `83d874e40060cdd8e83c0c75b8a7805fce8e0493`, 입력 137파일 확인값 포함.
- 브라우저 재현은 위 내역 기록과 같은 로컬 가짜 서버·스크립트를 쓴다. 실제 학생 계정이나 운영 시험에는 실행하지 않는다.
- `.codex-tmp/APP-20260919-last-selection.png`: 299ms 대기 상태를 메인이 실제 이미지로 확인했다. 정오답 표시 없이 마지막 보기만 강조된다.
- 브라우저의 알림 요청 4개 403은 가짜 검사 서버가 의도적으로 제외한 경로다. 실제 앱 오류 0과 분리해 기록했다. 모든 시험 API의 정오답·서버 시각 계약은 그대로다.
- DB·실제 학생·음원 원본·새 배포 변경 0. 운영 반영과 운영에서의 확인은 이번 로컬 완료에 포함하지 않는다.
