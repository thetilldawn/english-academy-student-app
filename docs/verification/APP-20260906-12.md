# 내역 개인 캐시 검증

- R1~R3 / D1~D7 / T1~T4를 로컬에서 검증했다. 근거는 [작업 기록](../../architecture/work-records/records/APP-20260906-12.json)과 [측정 원본](APP-20260906-12.json)이다.
- 현재 서버 인증은 매번 유지하고 동일 로그인·조건의 첫 목록만 15초 재사용한다. 60초 표시 제한/120초 비활동 폐기, 검색·상태 기억, 더보기 분리, 계정·로그아웃·탭 복귀·늦은 실패 격리를 검사했다. 포인트는 저장하지 않는다.
- 학생/배정과 내역이 같은 수명 helper를 쓰되 저장소 인스턴스와 DTO/필터/최소 DB 시각은 각 기능이 소유한다. 서버 인증은 lib/auth에 남는다.
- 변경 신호는 성공당 한 번, 내역 ON은 첫 목록 1회/구역 0회다. 더보기400 후 첫 목록 복구 성공·실패 및 왕복에서도 옛 커서를 쓰지 않는다. 독립 P2 지적을 보완했고 재검토 P1/P2 없음.
- DB쓰기 검사기의 Map.delete 오탐은 실제 const native Map/Set 연결만 구분했다. DB/unknown/let/import/동명이름 가림은 여전히 차단한다. 전용8개+기존7개 통과. 파일 허용 예외는 추가하지 않았다.
- 전체 **416파일 2,559검사**, 타입/lint/구조/요청 캐시/변경 연결 및 격리 Next.js16.2.12 **새 OFF·ON 빌드** 통과.

| 로컬 실제 앱브라우저 | OFF | ON |
|---|---:|---:|
| 최초 40회 중앙값 | 56.80ms | 52.75ms |
| 최초 40회 p95 | 339.30ms | 115.60ms |
| 최초 최대값 | 347.90ms | 340.20ms |
| 실제 Link 왕복 10회 중앙값 | 322.60ms | 52.15ms |
| 실제 Link 왕복 p95 | 327.90ms | 355.50ms |

- 최초10회 분산 확인 후 양쪽을 준비1회+40회로 확장했다. 예비·부하·이상치 모두 원본에 남겼다. 왕복은 준비1왕복만 제외했다. 왕복 중앙값은 83.83% 개선했지만 p95는 악화했으므로 모든 이동이 균일하게 빨라졌다고 표현하지 않는다.
- ON 준비 포함 11왕복은 현재 인증22/관리자 확인22, 학생 목록1/내역1, 포인트10이었다. 목록 RPC 두 건은 최초 준비 진입과 각각 일치하며 측정10회 내역 RPC는0이다. 현재 인증 응답 뒤 표시 최대22ms. 실제 DB의 실행비·네트워크·운영 성능을 재현한 결과는 아니다.
- 첫 중앙·p95 10% 비악화, 왕복 중앙30%개선, 인증뒤100ms 및 안전 관문 통과로 로컬 채택한다. 스위치는 아직 기본OFF이며 운영 활성화는 마지막 기능 및 배포 검증과 분리한다.
- 공용 서버 캐시는 **조건부 비채택**: APP07 이후 탐색 공용자료 읽기0, 등록 준비는10회 열기 중1회이며 남은 큰 반복 공용조회 근거가 없다. 전체 수정 경로의 버전·갱신 연결도 없으므로 개인 자료를 섞거나 기각된 R8 설정을 복원하지 않는다.
- 근거 해시 `513c32b5479546b49d137b0cbb437dcbc24efac5b49b1b10a32293f3e60d93dd` (812파일). APP08/10/11의 당시 해시는 덮지 않고 공통 helper 전환 뒤 이번 전체검사로 통합 회귀를 확인했다.
- DB·실제 학생·원자료·의존성·운영 배포 변경 없음. 학생 단어 가리기는 별도 마지막 작업이다.
