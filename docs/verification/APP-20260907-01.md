# APP-20260907-01 배정 중 목록 만료 보완 검증

[지시서](../work-orders/APP-20260907-01.md) · [R/D/T](../../architecture/work-records/records/APP-20260907-01.json). 입력 기준 3a904b1. 상태·우선순위는 루트 대장 한 곳에서만 관리한다.

## 변경과 원인

- 정상 목록 조회 뒤 60초가 지나면 개인 목록의 표시 상한이 배정 편집창까지 숨기고 있었다. 신규 실제 조립 검사에서 단일/일괄 창 닫힘과 갱신 중 닫힘 3건을 먼저 재현했다.
- 배정 소비자만 현재 화면에서 성공한 자료를 유지한다. 성공 자료와 당시 진입 식별값을 묶어 보관하므로, 탭 복귀 요청이 실패했는데 옛 자료가 새 인증처럼 다시 나타나지 않는다.
- 만료는 중립 안내, 실제 읽기 실패는 실패 안내, 인증 실패는 자료 제거다. 공용 Dialog와 배정 입력·계산·최종 서버 검증은 그대로이며 서버/API/DB/학생 시험/점수/자동 공개 변경은 없다.
- UI 역할 지도에는 공통 한국어 문구의 직접 참조 1개만 등록했다. 넓은 import 예외나 기존 검사 기준 완화는 없다. session/students 안내에도 배정 전용 예외와 인증 보호를 기록했다.

## 로컬 검사

- 최종 전체 426파일 2,743검사 통과. 기존 선택 외부원본 검사 1건은 skipped이며 통과 수에 포함하지 않는다.
- 실제 배정 조립 21검사 중 신규 10건: 단일/일괄 60·120초 값·초점·선택 보존, 같은 화면 재시도 대기/503/성공, 복귀503 숨김, 현재401/403, 오래 열린 창의 계정/로그아웃, 늦은200/401.
- 학생·내역의 기존 60초 숨김은 유지하고 만료 뒤 실제 조회 실패 안내를 우선하는 검사 각1건, 캐시 배정 UI 상태 검사1건을 추가했다.
- 타입·lint·기능지도·UI구조·요청 캐시·설계+변경 연결19건·Next16.2.12 최적화빌드(정적33페이지) 통과. 독립 읽기검토 차단 결함0; 검토자는 파일·외부 변경 및 별도 실행을 하지 않았다.
- 첫 전체 검사에서 공통 문구 import의 역할 등록 누락1건이 잡혔다. 정확한 경로만 검토·등록하고 전체를 재실행했다. 작업 기록의 영향 흐름도 실제 지도와 일치시킨 후 변경 연결 검사를 통과했다.

## 실제 로컬 화면

- 앱브라우저에서 가짜 계정으로 실제 단일 배정창을 열고 시간 이름과 통과점수85를 입력했다. 입력 후129초에 창·값·초점이 그대로였고 콘솔 경고/오류는0이었다.
- 장시간 대기 중 자동 directory 요청0, 준비 조회1. 작성창을 닫을 때 기존 입력 폐기 확인이 나왔으며 검사 입력만 폐기했다. 목록에는 중립 안내가 남았고 명시적 다시 불러오기1회로 정상 갱신됐다.
- 로컬 helper의 외부 통신/배정 저장 차단을 유지했다. 실제 계정·학생 자료를 사용하거나 배정·시험을 저장하지 않았다. 화면을 새로고침해 사용자의 기존 Preview 작성 상태를 건드리지 않았다.
- 관련 입력777개 지문: `44665d05af8865c360895c8d66614ba52fb8bb1e59430d28f41ab2523afff7db`.
- UI 역할 등록 파일의 별도 SHA-256: `ceac5969162f26d9790f185bee8a3479b27f94836a4986a500f77105644e775a`. 자동 지문 외의 이 메타파일도 실제 전체 검사 입력으로 검토했다.
- 검사 서버의 일반 종료는 권한 제한으로 실패했다. 정확한 두 PID와 부모/명령을 재확인하여 종료하고 원래 빌드를 복원했다. 새 빌드는 ignored `.codex-tmp/app0701-local-verified-next-20260907`에 보관하며 사용자 자료 삭제는 없다.

## 배포 경계와 한계

- 위 결과는 로컬 검증이다. Preview 배포 근거는 실제 반영 후 아래에 추가하며 운영 배포 완료로 합치지 않는다.
- 실제 배정 저장/학생 시험/전체 서비스 오류율을 검사한 것으로 표현하지 않는다. 이번 변경은 브라우저 작성 상태이며 저장 권한·충돌 검증은 기존 서버 경로를 사용한다.
- 복구 시 이번 변경 커밋만 되돌린다. 서버·DB·환경변수·접근 보호·요금제를 변경할 필요가 없다.

## Preview 반영

- 구현 `413c0a3def5d0ffa1f9d531224a1643ef2b574e6`을 기존 기능 브랜치에만 일반 push했다. Preview 배포6297735044가 2026-09-06 20:40:31 UTC success이며 [수정 프리뷰](https://english-academy-student-i1p4nj27i-thetilldawn-3859s-projects.vercel.app/admin/assignments)에 연결된다. 운영 main은 변경하지 않았다.
- 원격 smoke run34058727182/job101555286794는 ‘Preview 이벤트와 GitHub 배포 대조’ 사전 단계에서 실패했다. 뒤의 설치·브라우저 검사들은 skipped이며 통과로 계산하지 않는다. 이 오류 보완을 이유로 CI 승인 브랜치/보호 설정을 바꾸지 않았다.
- 처음 사용한 운영 계정은 Preview에서 거절됐다. 같은 작업의 2026-09-06T19:21:54.480Z 로그인 기록에서 기존 Preview 전용 계정을 확인해 정상 로그인했다. 비밀번호/계정/팀/연결 설정 변경 없이 기존 계정을 재사용했으며 비밀값을 앱 문서나 Git에 저장하지 않는다.
- 비로그인 HTTP의 JS 주소 읽기는 배포 보호 HTML을 반환했으므로 JS 바이트 일치 검사로 계산하지 않는다. 배포 SHA와 실제 앱브라우저 확인을 별도로 사용한다.
- 새 Preview에서 기존 테스트 학생의 단일 배정창에 시간 이름/통과점수85를 입력하고105초 뒤에도 창·값·초점 유지와 콘솔 경고/오류0을 확인했다. 닫기 확인을 거쳐 이번 검사 입력만 폐기했다. 목록은 중립 안내와 정상 학생 행을 유지했고 원래 오류 화면으로 바뀌지 않았다. 배정 저장·시험 시작·답 제출은 하지 않았다.
- 기능 브랜치 HEAD413c0a3/운영 main189e5d3을 최종 읽기 확인했다. 변경 배포는 Preview만이며 운영은 이전 코드다. 원래 사용자 작성 탭과 운영 학생 탭은 그대로 유지하고 수정 Preview 탭을 별도로 남겼다.
- 최종 완료 관문은 동일 코드/설계 지문으로 재확인했다. 이후 추가는 검증·인계 문서뿐이며 최종 기록 때문에 같은 코드를 다시 배포하지 않는다.
