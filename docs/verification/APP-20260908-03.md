# 공용 입력·배정 표시 로컬 검사

2026-09-08. 로컬 승인 범위만 확인했으며 운영 배포·DB 적용은 하지 않았다.

- 숫자 편집/유효성/계산/요청 경계를 분리했다. 빈칸·미완성 숫자는 이전 숫자로 제출하지 않으며001→1, 휠로 숫자를 바꾸는 native number input을 제거했다. 점수0과0.5분은 정상 수치다.
- 미선택·미지원·조회실패를 구분한다. 미지원 탭의 호버/초점/터치 도움말은 공용 Tabs/HelpTip이며, 해당 유형을 선택한 것으로 처리하지 않는다.
- 연속 DAY·과만 축약하고 서로 다른 모의고사 범위는 별도 태그로 유지한다. 미리보기는 배정학생·회차 옆 개수·실제 범위·공개/마감 열이다. 회차 보류의 서버 동작은 변경하지 않았다.
- 로컬 IAB390px: 가짜100개가20개씩5회차로 실제 계산, 날짜 없음/각 범위 보존 확인. 바구니0/1/2명 높이165.6px 동일. 확인창은352x231px, 배경 blur3px.1240px 내역 날짜2행 x977 동일, 가로넘침0.
- 화면 검사에서 native dialog의 auto 높이가824px로 늘어나는 문제를 발견했다. fit-content로 바꾸고 정적 회귀검사와 실제231px 높이 확인을 추가했다. 실패를 통과로 덮지 않았다.
- 학생 상세의 미저장 학교입력에서 뒤로 가기→취소는 값/초점 보존, 승인만 실제 이동했다. 독립 검토의 승인직후 저장·다른대상·복원중 저장성공·늦은완료 경합을 보완했다.
- 최종 전체442파일2974검사 통과/기존 선택외부자료1skipped. 타입·lint·지도·구조·React요청캐시·격리 Next16.2.12 최적화빌드33페이지·변경기록 연결 통과. 빌드와 과다동시 검사를 겹친 첫 실행의2실패는 보존하고 maxWorkers4 전체 재검사를 통과했다.
- 가짜 자료 도구의 준비 RPC를 최신v2로 맞추고, 가짜단어장11의 검토풀이 읽기만 추가했다. 인증·실제 Next 계산 경로를 유지하며 배정저장·외부DB·미등록ID는 계속 차단한다. 검사 자료를 실제 서비스 검증으로 표현하지 않는다.

물리 터치 장치·원격 학교 필드/저장·Preview 통합은 미실행이다. 실제 학생·답안·점수·음원 변경0. 현재 상태와 다음 순서는 루트 작업 대장이 기준이다. [지시서](../work-orders/APP-20260908-03.md), [코드 지문과 검사 기록](../../architecture/work-records/records/APP-20260908-03.json).
