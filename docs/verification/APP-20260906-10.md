# 배정 대상 목록 캐시 검증

- 결과: 로컬 채택, 기본 스위치 OFF. 전체411파일/2,509검사·타입·lint·기능 지도·요청 캐시·새 ON/OFF Next 빌드·완료 관문 통과. 관련777개 지문 `6cac987e3a0a9426b5ac140b195937d4237a731031760a91201b94b033c4c6b3`.
- [원계측](APP-20260906-10.json)은 실패·워밍업·외부 변동도 보존한다. IAB 실제 Link 왕복 각10회: 배정 중앙값 324.90→67.10ms(79.35% 감소), p95 327.30→74.70ms. 최초10회 중앙값55.35→55.60ms(+0.45%), p95 348→71.30ms. 첫 표시 악화10%/왕복30% 기준 통과. 기존 호출 때 문서/JS 지연이 섞이므로 전부 DB 지연으로 설명하지 않는다.
- ON 11왕복(첫1회씩 준비 포함)의 현재 인증22/권한프로필22, 목록RPC2, 현재포인트20. 목록2회는 각각 첫 학생/배정 준비에 속한다. 이후 측정10왕복의 목록RPC0. OFF는 목록12/포인트10. 최초 ON은 인증10/프로필10/목록10, 추가 브라우저 목록 읽기0. 실제 DB 비용·운영 성능을 측정한 것은 아니다.
- 배정 작성/선택은 표시 경계가 잠시 숨겨도 유지한다. IAB에서 점수85 입력→폐기확인창→60초만료 시 두 창/목록숨김→재확인 후 확인창과 초점복원→계속작성 점수85/부모닫기초점→버리고닫기 목록배정버튼초점을 확인했다. 스크롤 잠금 균형은 실제 Dialog 두 창 부품검사로 확인했다.
- 준비 요청 중 숨김/복귀, 직접 제출 차단, 현재401/403 전체해제, 일반503 복구/입력보존, 취소된401 무시, StrictMode/소유자교체/해제된 저장응답 잠금 금지, 소비화면별 조건·커서·210명규칙 회귀검사 포함.
- 같은 탭 로컬+BroadcastChannel 중복 알림을 난수별로 제거한다. 실제 통합에서 변경1건의 새목록1회 확인. 구독자별128건 제한, 다른 탭/구독자 수신 보존, 개인식별자 전송 없음.
- 독립검토 신규 P1/P2 없음. 처음 전체검사는 보호목록의 새 인증 연결 경로 미등록1건으로 실패했고, 좁은 단일 경로만 등록·인증경계 React-only 검사 후 전체 재실행으로 통과했다. 최초 전용검사의 StrictMode fixture 위치 문제도 수정·재실행했다.
- 운영/DB/실학생/자료/비용/의존성 변경 없음. 두 로컬 서버 exit0로 종료·이전 빌드 보존. 현재 이후 변경은 새 기록으로 검증하며 이 지문을 재서명하지 않는다.
