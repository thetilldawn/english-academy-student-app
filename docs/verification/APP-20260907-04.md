# APP-20260907-04 검증

## 확인 결과

- 새 공개 계산은 앞 첫시험 완료와 다음 자체 예약 중 늦은 시각을 따른다. 앞 마감+12시간만 제거했고 재시험 완료/합격을 기다리지 않는다. 자체 마감·보류·취소·다른 학생 차단·이미 시작한 시험 보호는 유지한다.
- 전체 2,805건 중 2,804 통과·실패0·기존 외부자료 선택 검사1건 skipped. 새 최종 스키마 검사26건 포함. 타입/lint/기능지도/구조/변경 기록 연결/React 요청 캐시 및 가짜 로컬 설정의 최적화 빌드33페이지 통과.
- 첫 검사에서 가짜 자료의 상태·시험방향·단어 단위 필수값 누락을 발견해 fixture만 바로잡았다. 최초 직접 빌드는 APP_ORIGIN 미설정으로 실패했다. 해당 실패를 통과로 계산하지 않고, 기존 로컬 검사 보호장치와 가짜 연결값으로 전체 검사/빌드를 재실행했다. 최종 원시 결과는 ignored `.codex-tmp/app0704-tests-verified.json`이다.
- 독립 SQL 검토에서 잠금 사이 신규 대상 유입 위험을 확인했다. 최초 학생·묶음 ID 집합 고정, 모든 잠금/최종 조회의 동일 집합 제한, 항목별 실제 마감 재확인을 반영했다. 재검토에서 추가 확정 결함0. 실제 PostgreSQL 두 연결 동시성 실험은 하지 않았으며 순차 PGlite 검증/정적 보호 검사와 구분한다.

## DB 적용

- 로컬 파일과 실제 적용 SQL의 SHA256은 모두 `a134db3dd30e1e32d3dcbc06046a899cb6dee1522ff6707171f068157ff4cef0`이다. 기존 136개 migration은 수정하지 않았다. 새 표/컬럼/권한/외부 의존성 추가0.
- Preview `wojxpruvbjzbhrpmsbuy`: migration `20260907091150` 적용. 대상 두 함수의 12시간 분기0, 기존 권한/search_path 동일. 보호13표 행수·내용 SHA 전부 동일, 진행응시2건 보존. 기존 충돌1건은 좁은 복구조건 불충족으로 유지했으며 자동복구0.
- Production `xdxhswjgksukjmpbzqgz`: migration `20260907091459` 적용. 사전 함수 정의가 검증된 Preview 이전 정의와 정확히 일치함을 확인한 뒤 동일 SQL만 적용했다. 적용 후 두 환경 함수 본문·권한 정확히 일치, 보호13표 전부 동일. 최초시험 진행0/재시험 진행2 보존, 기존 충돌0/자동복구0.
- 전체 수신자를 기준으로 미응시 잠금 Preview21/운영4건의 단어·정답 노출0. Preview의 이미 시작한 시험1건은 기존 보호 규칙에 따라 학습 단어가 계속 보이며 정답 필드 노출0이다. 처음의 잠금 집계가 이미 시작한 예외까지 포함해 1건을 표시한 것은 별도 조회로 확인했다. 이를 새 자료 노출 결함이나 미응시 잠금 통과로 섞어 계산하지 않는다.
- 실학생 시험 시작·답 제출·점수 변경0. 적용 전후 원시 함수/보호값은 ignored `.codex-tmp/app0704-*-before.json`, `*-after.json`에 보존했다. 사용자에게 학생 개인정보/비밀키는 출력하지 않았다.

## 남은 구분

- 공개 규칙의 DB 적용은 양쪽 완료. 새 안내 문구/앞선 개수 UI의 앱 배포는 이 기록 시점 대기이며, 단어장 배지 작업과 같은 최종 코드 검증 후 별도 배포한다. DB 반영을 앱 배포 완료로 표현하지 않는다.
- 전체 서비스 오류율·비용·실제 두 연결 경쟁·물리 터치 장치 검증은 이 작업의 통과 근거에 포함하지 않는다. 과거 Preview CI 브랜치 사전조건 실패도 보존한다.
