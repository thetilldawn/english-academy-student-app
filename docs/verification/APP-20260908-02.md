# 학생 화면 정상화 검사

2026-09-08 · 로컬 구현/검사 결과이며 전체 완료·배포 기록이 아니다.

- 원인: @detail 주소 변경을 배경 children 이동으로 처리했고, 60초 표시 만료가 배경 목록을 제거했다. 두 수명을 분리했다. 현재 인증 ticket 밖의 자료와 401/403 자료는 계속 숨긴다.
- 저장: 성공/충돌/미인증/조회장애/결과불명을 구분하여 폼 안에 표시한다. 결과불명은 읽기 확인 전 재전송을 막는다. 추가 입력·늦은 응답·학생 전환을 보존한다. 운영 DB 버전/권한의 읽기 대조에서 장애 원인은 확인되지 않았으므로 실제 저장 실패를 DB 시간 오류로 단정하지 않는다.
- 목록 유지 뒤 갱신 응답이 안쪽 state에 반영되지 않는 회귀를 독립검토에서 찾아 보완했다. 같은 행 DOM을 유지하며 새 이름·인원·커서를 받는다. 옛 필터의 응답은 새 검색을 덮지 않는다.
- 최종 전체 438파일 2,940검사 통과, 기존 선택 외부자료 검사1건 skipped. 타입/lint·기능지도·변경연결26건·공용 UI 구조·React 요청 캐시·Next16.2.12 최적화빌드33페이지 통과.
- 로컬 앱브라우저1078px: 77초 이후 배경 목록 유지, 상세2회 열기/닫기에서 목록 추가 조회0. 가짜 학생 학교 저장1회, 저장 뒤 목록 조회1회, 헤더·목록 학교/인라인 성공 안내 확인. 배정 학생 이름→공용 상세 내역 기본선택/배정링크 확인. 401/409/503/응답유실은 로컬 자동검사로 검증했으며 실제 서비스에 장애를 만들지 않았다.
- 학교/학년 SQL은 신규 forward migration으로 준비했다. 기존 조회 열만 목록 JSON에 추가하며 삭제학생 null/권한/함수 설정 보존. 로컬 축약 함수 통합검사2건 및 Preview 함수 메타 읽기 대조만 완료, 실제 DB 적용0.

## 남은 실제 환경 검사

- T9: Preview 배포/목록 학교 필드 적용 후 승인된 테스트학생의 정상 저장·후속 조회·원래 입력 복구. 실제 운영 장애 원인 미확정은 유지한다.
- 로컬 오답/배정 큐 API는 격리 도구가 차단한다. 내역 기본 탭·링크 확인과 실제 원격 내역 성공을 구분한다.
- 모바일 실화면은 다음 공용 UI 단계의 대표 화면 검사에 포함한다. 실제 학생 변경·음원·정답·점수·Production 쓰기0.

[설계/검사 입력 지문](../../architecture/work-records/records/APP-20260908-02.json)은 당시 코드와 연결한다. 필수 T9를 미실행으로 남겼으며 완료 관문을 통과한 것으로 표시하지 않는다.
